---
phase: 13-production-readiness
plan: 07
type: execute
wave: 2
depends_on: [13-06]
files_modified:
  - tests/TestSpatialOperations.cpp
  - tests/TestForwardDynamics.cpp
  - tests/TestInverseDynamics.cpp
  - README.md
autonomous: true
requirements: [VEC-01, UTL-03, ABA-01, ABA-02, TST-07]

must_haves:
  truths:
    - "Zero-mass and zero-torque edge cases produce correct results or appropriate errors"
    - "Core operations (cross product, ABA, RNEA) produce stable finite results for valid inputs in release mode"
    - "README.md documents gravity API, removed cross product overloads, Vector3d namespace change, and umbrella header"
    - "API usage examples in README.md match the actual library API (no stale signatures)"
  artifacts:
    - path: "tests/TestSpatialOperations.cpp"
      provides: "Zero-mass edge case tests for cross product"
      contains: "ZeroMassEdgeCase|ZeroVectors"
    - path: "tests/TestForwardDynamics.cpp"
      provides: "Zero-mass ABA edge case test"
      contains: "ZeroMassEdgeCase"
    - path: "tests/TestInverseDynamics.cpp"
      provides: "Zero-mass RNEA edge case test"
      contains: "ZeroMassEdgeCase"
    - path: "README.md"
      provides: "Updated API documentation"
      contains: "gravity"
  key_links:
    - from: "README.md"
      to: "include/SpatialAlgebra.h"
      via: "Documentation references umbrella header include pattern"
    - from: "README.md"
      to: "include/ForwardDynamics.h"
      via: "Gravity parameter in computeAccelerations signature"
---

<objective>
Add edge case coverage (zero mass/inertia, release-mode stability) and update API documentation.

Purpose: Addresses remaining MEDIUM review concerns from 13-REVIEWS.md:
- "No mention of edge cases like zero mass/inertia" (MEDIUM, 13-04)
- "Release-mode NaN verification - behavior stays stable when inputs are valid" (MEDIUM, 13-03)
- "No explicit documentation updates for changed APIs" (MEDIUM, cross-plan)
- "README code examples reference nonexistent API (fd(2), setLink())" (MEDIUM, cross-plan)

Output:
1. Zero-mass/inertia edge case tests in TestSpatialOperations, TestForwardDynamics, TestInverseDynamics
2. Release-mode stability verification tests in TestForwardDynamics, TestInverseDynamics
3. Updated README.md with gravity API, removed methods, namespace usage, umbrella header
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-production-readiness/13-REVIEWS.md
@.planning/phases/13-production-readiness/13-RESEARCH.md
@.planning/phases/13-production-readiness/13-03-SUMMARY.md
@.planning/phases/13-production-readiness/13-04-SUMMARY.md
@.planning/phases/13-production-readiness/13-05-SUMMARY.md

<interfaces>
Key updated API signatures:
```cpp
// ForwardDynamics::computeAccelerations - now with gravity parameter
void computeAccelerations(const Eigen::VectorXd& tau,
                          const Vector3d& gravity = Vector3d::Zero());

// InverseDynamics::computeTorques - now with gravity parameter
Eigen::VectorXd computeTorques(const Eigen::VectorXd& qddot,
                               const Vector3d& gravity = Vector3d::Zero());

// MotionVector::crossForce removed; ForceVector::crossMotion removed
// Vector3d is now in namespace SpatialAlgebra (not global scope)
// Umbrella header: include/SpatialAlgebra.h
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zero-mass and zero-inertia edge case tests</name>
  <files>tests/TestSpatialOperations.cpp, tests/TestForwardDynamics.cpp, tests/TestInverseDynamics.cpp</files>
  <read_first>
    @tests/TestSpatialOperations.cpp
    @tests/TestForwardDynamics.cpp
    @tests/TestInverseDynamics.cpp
    @include/ForwardDynamics.h
    @include/InverseDynamics.h
    @include/SpatialUtils.h
  </read_first>
  <action>
    Append tests before main() in three files:

    In tests/TestSpatialOperations.cpp (add 2 tests):
    A: CrossForceZeroVectors - Test cross(ForceVector, ForceVector) with
    all-zero vectors. Verify result is exactly zero (all 6 components
    within EPSILON). Tests the edge case where zero input produces zero output.

    B: CrossForceZeroLinear - Test cross with one operand having zero
    linear component (pure torque: f=0). Verify angular = tau1 x tau2,
    linear = tau1 x 0 - tau2 x 0 = zero. Structural invariant.

    In tests/TestForwardDynamics.cpp (add 1 test):
    C: ZeroMassEdgeCase - Single link with RigidBodyInertia(mass=0,
    COM=Zero, lt::Zero(3)). Verify computeAccelerations produces
    finite output (does not crash on degenerate inertia).

    In tests/TestInverseDynamics.cpp (add 1 test):
    D: ZeroMassEdgeCase - Single link with mass=0. Verify computeTorques
    returns finite values (near zero for zero acceleration).

    Per reviewer: "edge cases like zero mass/inertia" (MEDIUM, 13-04).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -1 && cd build && ./TestSpatialOperations --gtest_filter="*Zero*" 2>&1 | tail -5 && ./TestForwardDynamics --gtest_filter="*ZeroMass*" 2>&1 | tail -3 && ./TestInverseDynamics --gtest_filter="*ZeroMass*" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 2 new tests in TestSpatialOperations: CrossForceZeroVectors, CrossForceZeroLinear
    - 1 new test in TestForwardDynamics: ZeroMassEdgeCase
    - 1 new test in TestInverseDynamics: ZeroMassEdgeCase
    - All 4 new tests pass
    - Zero-mass ABA produces finite output (no crash)
    - Zero-mass RNEA produces tau approx 0 for zero acceleration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add release-mode stability verification tests</name>
  <files>tests/TestForwardDynamics.cpp, tests/TestInverseDynamics.cpp</files>
  <read_first>
    @tests/TestForwardDynamics.cpp
    @tests/TestInverseDynamics.cpp
    @src/ForwardDynamics.cpp
    @src/InverseDynamics.cpp
  </read_first>
  <action>
    Add 2 tests that verify solvers produce stable finite output with valid
    inputs regardless of NDEBUG mode. These document that release builds
    (where NaN/Inf assertions are compiled out) still produce correct results.

    In tests/TestForwardDynamics.cpp (add 1 test):
    A: ReleaseModeStability - Single link with identity inertia, Z-axis
    revolute joint, tau=[1.0]. Call computeAccelerations twice: no gravity
    and with gravity=(0,0,-9.81). Verify both return finite, positive
    accelerations. Works in debug and release mode (inputs are valid).

    In tests/TestInverseDynamics.cpp (add 1 test):
    B: ReleaseModeStability - Single link, identity inertia, Z-axis joint.
    Test qddot=[1.0] without gravity and with gravity. Verify both return
    finite torques. Documents gravity API doesn't introduce NaN/Inf for
    valid inputs in any build mode.

    Per reviewer: "release-mode tests verifying behavior stays stable when
    inputs are valid" (MEDIUM, 13-03).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -1 && cd build && ./TestForwardDynamics --gtest_filter="*Release*" 2>&1 | tail -3 && ./TestInverseDynamics --gtest_filter="*Release*" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 1 new test in TestForwardDynamics: ReleaseModeStability
    - 1 new test in TestInverseDynamics: ReleaseModeStability
    - Both tests produce finite non-NaN output for valid inputs
    - Tests pass in both debug and release build modes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update README.md and fix stale API examples</name>
  <files>README.md</files>
  <read_first>
    @README.md
    @include/SpatialAlgebra.h
    @include/ForwardDynamics.h
    @include/InverseDynamics.h
    @include/SpatialVector.h
    @examples/*.cpp
  </read_first>
  <action>
    Update README.md to reflect all Phase 13 API changes:

    1. Add gravity parameter to ForwardDynamics and InverseDynamics API
       descriptions. Include note: "Optional gravity parameter (default
       Vector3d::Zero()) for Featherstone gravity formulation."

    2. Add a "Gravity Support" feature bullet covering:
       - gravity-aware computeAccelerations() and computeTorques()
       - base acceleration formulation (a0 = -g per Featherstone)
       - default zero preserves backward compatibility

    3. Add SpatialAlgebra.h umbrella header to the project structure tree.

    4. Fix all code examples that reference nonexistent APIs:
       - Forward Dynamics example: replace ForwardDynamics fd(2) and
         fd.setLink(...) (nonexistent) with correct Link struct approach
         matching tests/TestForwardDynamics.cpp pattern.
       - Include paths: change SpatialAlgebra/MotionVector.h to MotionVector.h.
       - Remove crossForce(wrench) from vector example (removed per D-04).
         Replace with cross(wrench, wrench) free function or similar valid API.

    5. Add section "API Changes in v1.1" listing:
       - Gravity parameter on computeAccelerations/computeTorques
       - MotionVector::crossForce and ForceVector::crossMotion removed
       - Vector3d moved into namespace SpatialAlgebra
       - Umbrella header SpatialAlgebra.h available
       - Empty stub files removed (src/RigidBodyInertia.cpp, etc.)
       - OpenMP dependency removed from LowerTriangular
       - GTest FetchContent fallback for CI builds

    Per reviewer: "No explicit documentation updates for changed APIs"
    (MEDIUM cross-plan), "mixed scope / stale API references" (MEDIUM 13-05).
  </action>
  <verify>
    <automated>grep -c "gravity" README.md && grep -c "SpatialAlgebra\.h" README.md && grep -c "API Changes" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md references gravity parameter in ForwardDynamics and InverseDynamics sections
    - Forward Dynamics example uses correct Link struct pattern (not nonexistent fd(2) or setLink())
    - Include examples use bare header names (MotionVector.h, not SpatialAlgebra/MotionVector.h)
    - crossForce/crossMotion removals documented in "API Changes in v1.1" section
    - SpatialAlgebra.h umbrella header listed in project structure
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All changes are to test files and README.md documentation.

## STRIDE Threat Register

No applicable threats. No new dependencies, no network, no external services.
</threat_model>

<verification>
cmake --build build && cd build && ctest --output-on-failure
Expected: 179+6=185 passing total (3 pre-existing CR-02 failures unchanged)
6 new tests: 4 edge case (zero-mass, zero-vector cross) + 2 release-mode stability
</verification>

<success_criteria>
- 6 new tests pass: 4 edge case (zero-mass, zero-vector cross) + 2 release-mode stability
- README.md documents gravity API, removed methods, namespace cleanup, and umbrella header
- All code examples in README.md use correct library API signatures
- Test suite grows from 182 to 188 total tests (185 passing + 3 pre-existing CR-02 failures)
</success_criteria>

<output>
Create `.planning/phases/13-production-readiness/{phase}-07-SUMMARY.md` when done
</output>
