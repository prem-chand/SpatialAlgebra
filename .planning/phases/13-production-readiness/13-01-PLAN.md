---
phase: 13-production-readiness
plan: 01
type: execute
wave: 1A
depends_on: []
files_modified:
  - include/SpatialUtils.h
  - src/SpatialVector.cpp
  - src/ForceVector.cpp
  - include/ForceVector.h
  - src/MotionVector.cpp
  - include/MotionVector.h
  - include/ArticulatedBodyInertia.h
  - include/PluckerTransform.h
autonomous: true
requirements:
  - VEC-01
  - UTL-03
  - INR-01
  - PLX-04
user_setup: []
must_haves:
  truths:
    - "Force×force cross product is correct and consistent across all call sites"
    - "Force×force cross product is anti-commutative (a×b = -(b×a))"
    - "MotionVector::crossForce and ForceVector::crossMotion are removed and no callers remain"
    - "ArticulatedBodyInertia::operator+(RigidBodyInertia) passes correct arguments with mass multiplier"
    - "PluckerTransform::apply() returns explicit types, not auto-deduced"
    - "No stale references to removed methods exist anywhere in the tree"
  artifacts:
    - path: "src/SpatialVector.cpp"
      provides: "crossForce delegates to canonical free function"
      contains: "cross\\(ForceVector\\(\\*this\\), ForceVector\\(other\\)\\)"
    - path: "src/ForceVector.cpp"
      provides: "crossForce delegates via cross(*this, other); crossMotion removed entirely"
      contains: "cross\\(\\*this"
    - path: "include/ForceVector.h"
      provides: "crossMotion declaration removed"
      does_not_contain: "crossMotion"
    - path: "include/MotionVector.h"
      provides: "crossForce declaration removed"
      does_not_contain: "crossForce"
    - path: "src/MotionVector.cpp"
      provides: "crossForce implementation removed"
      does_not_contain: "crossForce"
    - path: "include/ArticulatedBodyInertia.h"
      provides: "operator+ passes Inertia, skew(com)*mass, Identity*mass in correct order"
      contains: "getMass\\(\\) \\* skew\\(other\\.getCom\\(\\)\\)"
    - path: "include/PluckerTransform.h"
      provides: "apply() returns ForceVector and MotionVector explicitly"
      count: "2 explicit return types (no auto)"
  key_links:
    - from: "src/SpatialVector.cpp crossForce"
      to: "include/SpatialUtils.h cross(ForceVector,ForceVector)"
      via: "function call cross(ForceVector(*this), ForceVector(other))"
      pattern: "cross\\(ForceVector\\(\\*this\\), ForceVector\\(other\\)\\)"
    - from: "src/ForceVector.cpp crossForce"
      to: "include/SpatialUtils.h cross(ForceVector,ForceVector)"
      via: "delegation cross(*this, other)"
      pattern: "cross\\(\\*this"
    - from: "include/ArticulatedBodyInertia.h operator+"
      to: "include/SpatialUtils.h skew"
      via: "coupling term with mass multiplier"
      pattern: "skew\\(other\\.getCom\\(\\)\\)"
---
<objective>
Fix 3 critical bugs (cross-product inconsistency, ABI argument swap, Plücker auto-return-type hazard) and remove 2 deprecated methods. After this plan, all force×force cross product call sites use a single canonical implementation, ABI+RBI accumulation produces correct inertias, and PluckerTransform has no linkage hazards. Includes compile-fail/grep verification that removed methods have no remaining callers anywhere in the tree.

**Purpose:** Eliminate silent numerical errors in core spatial algebra operations that make the library unreliable for robotics use (CR-01, CR-03). Addresses review concern HIGH #1: "Removing deprecated methods may be a breaking change if internal tests/examples still use them."
**Output:** Correct, single-sourced cross-product; correct ABI accumulation; safe Plücker API; verified no stale references to removed APIs.
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-production-readiness/13-CONTEXT.md
@.planning/phases/13-production-readiness/13-RESEARCH.md
@.planning/phases/13-production-readiness/13-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Key types from SpatialUtils.h — current canonical cross() formula -->
From include/SpatialUtils.h:115-125:
```cpp
inline ForceVector cross(const ForceVector& v1, const ForceVector& v2) noexcept {
    const Vector3d& t1 = v1.getAngular();
    const Vector3d& f1 = v1.getLinear();
    const Vector3d& t2 = v2.getAngular();
    const Vector3d& f2 = v2.getLinear();
    return ForceVector(
        t1.cross(t2) + f1.cross(f2),
        t1.cross(f2) - t2.cross(f1)
    );
}
```

From include/SpatialUtils.h:77-87 (motion×force — keep, unchanged):
```cpp
inline ForceVector cross(const MotionVector& v1, const ForceVector& v2) noexcept {
    const Vector3d& w1 = v1.getAngular();
    const Vector3d& v1_lin = v1.getLinear();
    const Vector3d& f1 = v2.getAngular();
    const Vector3d& f2 = v2.getLinear();
    return ForceVector(w1.cross(f1), w1.cross(f2) + v1_lin.cross(f1));
}
```

From include/SpatialUtils.h:96-106 (motion×motion — keep, unchanged):
```cpp
inline MotionVector cross(const MotionVector& v1, const MotionVector& v2) noexcept {
    const Vector3d& w1 = v1.getAngular();
    const Vector3d& v1_lin = v1.getLinear();
    const Vector3d& w2 = v2.getAngular();
    const Vector3d& v2_lin = v2.getLinear();
    return MotionVector(w1.cross(w2), w1.cross(v2_lin) + v1_lin.cross(w2));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-validate no stale callers before removing deprecated methods</name>
  <files>include/ForceVector.h, src/MotionVector.cpp, include/MotionVector.h</files>
  <read_first>
    (No pre-removal code read needed — this is a discovery-only task)
  </read_first>
  <action>
    Per review concern HIGH #1: Before removing any method, verify no callers exist.

    Run three grep searches across the entire tree (excluding .planning/ and .git/) to confirm no code references the methods scheduled for removal:

    1. `grep -rn "crossMotion" include/ src/ tests/ examples/ --include="*.h" --include="*.cpp"` — should return hits only in ForceVector.h (the declaration) and src/ForceVector.cpp (the implementation). If any other file references `crossMotion`, log the filenames as pre-existing callers that must be updated.

    2. `grep -rn "MotionVector::crossForce\|\.crossForce(" include/ src/ tests/ examples/ --include="*.h" --include="*.cpp"` — should return hits only in MotionVector.h (declaration) and src/MotionVector.cpp (implementation). If any test or example file calls `.crossForce()` on a MotionVector, note the file:line.

    3. `grep -rn "ForceVector::crossMotion\|ForceVector::crossForce\|\.crossForce(" include/ src/ tests/ examples/ --include="*.h" --include="*.cpp"` — verify that ForceVector.crossForce is used in tests (acceptable) and ForceVector.crossMotion is not used anywhere except its own declaration/impl.

    **If any unexpected callers are found:** List them in the SUMMARY deviations section as "pre-existing callers automatically updated to use free function cross()". Then proceed with Task 2 removals, then update those callers to use `cross()` free function.

    **If no unexpected callers:** Confirm in the SUMMARY that removal is safe — no callers exist outside the method's own declaration/implementation files.

    This addresses review concern HIGH #1: "compile-fail or grep-based validation that removed methods are no longer referenced anywhere in tree."
  </action>
  <verify>
    <automated>grep -rn "crossMotion" include/ src/ tests/ examples/ --include="*.h" --include="*.cpp" 2>/dev/null | grep -v "planning" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for `crossMotion` across include/, src/, tests/, examples/ returns only declaration/definition in ForceVector.h and ForceVector.cpp
    - grep for `MotionVector::crossForce\|\.crossForce(` returns only MotionVector.h and MotionVector.cpp (the to-be-removed files)
    - Any unexpected callers are documented and updated
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Unify force×force cross product to canonical implementation and remove deprecated overloads</name>
  <files>
    src/SpatialVector.cpp
    src/ForceVector.cpp
    include/ForceVector.h
    src/MotionVector.cpp
    include/MotionVector.h
  </files>
  <read_first>
    include/SpatialUtils.h:115-125  (canonical cross() formula — source of truth)
    include/SpatialVector.h:68-173  (SpatialVector class interface — crossForce declaration at line 155)
    include/ForceVector.h:71-157    (ForceVector class interface — crossMotion at line 130, crossForce at line 140)
    include/MotionVector.h:69-154   (MotionVector class interface — crossForce at line 137)
    src/SpatialVector.cpp:42-54     (current crossMotion/crossForce implementations)
    src/ForceVector.cpp:30-46       (current crossMotion/crossForce implementations)
    src/MotionVector.cpp:31-41      (current crossMotion/crossForce implementations)
  </read_first>
  <action>
    Per D-01/D-02/D-04. Make these specific changes:

    **src/SpatialVector.cpp — crossForce (lines 48-54):**
    Replace the body of `SpatialVector::crossForce` to delegate to the canonical free function:
    `return ForceVector(cross(ForceVector(*this), ForceVector(other)));`
    The function returns `SpatialVector` (base class return type). Include `SpatialUtils.h` at top of file if not already available. Keep the existing `crossMotion` implementation unchanged.

    **src/ForceVector.cpp — crossForce (lines 40-46):**
    Replace the body to delegate: `return cross(*this, other);`
    The free function `cross(ForceVector, ForceVector)` is declared in SpatialUtils.h. Ensure resolution via include or transitive chain. Argument-dependent lookup resolves since both args are ForceVector.

    **src/ForceVector.cpp — crossMotion (lines 31-38):**
    Delete the entire `ForceVector::crossMotion(const ForceVector &other) const` method implementation (lines 31-38). It has no physical meaning per D-04.

    **include/ForceVector.h — crossMotion declaration (line 130):**
    Delete the `ForceVector crossMotion(const ForceVector &other) const;` declaration at line 130. Keep the `crossForce` declaration at line 140.

    **src/MotionVector.cpp — crossForce (lines 38-41):**
    Delete the entire `MotionVector::crossForce(const MotionVector &other) const` method (lines 38-41). It has no physical meaning per D-04.

    **include/MotionVector.h — crossForce declaration (line 137):**
    Delete the `MotionVector crossForce(const MotionVector &other) const;` declaration at line 137. Keep the `crossMotion` declaration at line 129.

    **include/SpatialUtils.h:**
    Verify the `cross(ForceVector, ForceVector)` free function at lines 115-125 already uses the correct formula (τ1×τ2 + f1×f2 for angular; τ1×f2 − τ2×f1 for linear). If it already correct, leave unchanged — it is the canonical implementation.
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -5 && cd build && ctest --output-on-failure -R "TestSpatialVector|TestSpatialUtils|TestSpatialOperations" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `build/TestSpatialVector` compiles and runs, all force×force cross tests pass
    - `build/TestSpatialUtils` compiles and runs, all cross tests pass
    - `build/TestSpatialOperations` compiles, all crossProductForce tests pass
    - ForceVector.cpp has no `ForceVector::crossMotion` method
    - MotionVector.cpp has no `MotionVector::crossForce` method
    - ForceVector.h has no `crossMotion` declaration
    - MotionVector.h has no `crossForce` declaration
    - SpatialVector.cpp crossForce body calls `cross(ForceVector(*this), ForceVector(other))`
    - Build succeeds with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Grep-validate no stale callers remain after removal + fix ABI/Plücker</name>
  <files>include/ArticulatedBodyInertia.h, include/PluckerTransform.h</files>
  <read_first>
    include/ArticulatedBodyInertia.h:150-155  (operator+ — verify correct)
    include/PluckerTransform.h:179-190        (apply() auto return types)
    include/SpatialVector.h:68-173            (SpatialVector base class)
    include/ForceVector.h:71-157              (ForceVector class)
    include/MotionVector.h:69-154             (MotionVector class)
  </read_first>
  <action>
    Per D-20/D-21 (fixes CR-01) and review concern HIGH #1 (post-removal validation).

    **Part 1: Verify ABI operator+ correctness (D-20, CR-01)**
    
    Read `include/ArticulatedBodyInertia.h:150-155`. The `operator+(const RigidBodyInertia &other) const` must pass arguments in order: (Inertia + other.inertia, H + mass*skew(com), M + Identity*mass). The coupling term must include `other.getMass() * skew(other.getCom())` (mass multiplier). If the code is already correct, leave it. If args are swapped or mass multiplier is missing, fix it.

    **Part 2: Fix PluckerTransform::apply() auto return types (D-21)**

    In `include/PluckerTransform.h`:
    - Line 179: Change `auto apply(const fv& v) const` → `ForceVector apply(const fv& v) const`
    - Line 188: Change `auto apply(const mv& v) const` → `MotionVector apply(const mv& v) const`
    Verify `PluckerTransform::multiply()` at line 172 already returns `PluckerTransform` explicitly.

    **Part 3: Post-removal grep validation (review HIGH #1)**
    
    After all removals from Task 2, run:
    ```
    grep -rn "crossMotion" include/ src/ tests/ examples/ --include="*.h" --include="*.cpp"
    ```
    This should return zero hits. If any remain, investigate and fix.

    ```
    grep -rn "\.crossForce(" src/ tests/ examples/ --include="*.h" --include="*.cpp"
    ```
    For MotionVector, this should return zero. For ForceVector (crossForce is legitimate), ensure it remains and delegates correctly.
  </action>
  <verify>
    <automated>grep -rn "crossMotion" include/ src/ tests/ --include="*.h" --include="*.cpp" && echo "UNEXPECTED: crossMotion still referenced" || echo "PASS: no crossMotion references remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "crossMotion" include/ src/ tests/ examples/` returns zero results (method fully removed, no dangling references)
    - `include/ArticulatedBodyInertia.h:150-155` has correct argument order: Inertia (1st), H with mass*skew (2nd), M (3rd)
    - `include/PluckerTransform.h:179`: `ForceVector apply(const fv& v) const` (explicit return type)
    - `include/PluckerTransform.h:188`: `MotionVector apply(const mv& v) const` (explicit return type)
    - No `auto` return type on any PluckerTransform method
    - Build succeeds, TestPluckerTransform tests pass: `cd build && ctest -R TestPluckerTransform`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-product call sites → canonical function | Callers depend on mathematical correctness of single canonical implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | cross(ForceVector, ForceVector) | mitigate | Single canonical implementation eliminates 3-bug inconsistency; D-01 enforces delegation |
| T-13-02 | Elevation of Privilege | auto→explicit return types | mitigate | Explicit return types at declaration prevent ODR/linkage errors in external translation units (D-21) |
| T-13-BC | Tampering | Removed crossForce/crossMotion callers | mitigate | Grep-based pre-removal scan (Task 1) and post-removal scan (Task 3) ensure no stale references |
| T-13-SC | Tampering | No external packages | accept | No package installs required for this plan |
</threat_model>

<verification>
- `cmake --build build` succeeds with no errors or warnings
- All test executables registered in CTest pass
- No `auto` return type remains on PluckerTransform methods
- Grep for `crossMotion` across include/, src/, tests/, examples/ returns zero results
- Grep for `crossForce` on MotionVector across all source/test files returns zero results
- Every removed method has a corresponding entry in SUMMARY.md deviations section if callers were found
</verification>

<success_criteria>
1. Force×force cross product is single-sourced in SpatialUtils.h, with both SpatialVector::crossForce and ForceVector::crossForce delegating to it (D-01, D-02)
2. MotionVector::crossForce and ForceVector::crossMotion are removed and grep-confirmed zero callers remain (D-04, review HIGH #1)
3. ABI operator+(RigidBodyInertia) has correct argument order and mass multiplier (D-20)
4. PluckerTransform::apply() has explicit return types (D-21)
5. All tests pass
</success_criteria>

<output>
Create `.planning/phases/13-production-readiness/13-01-SUMMARY.md` when done
</output>
