---
phase: 04-spatial-utilities
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/SpatialOperations.cpp, include/SpatialOperations.h]
autonomous: true
requirements: [UTL-04]

must_haves:
  truths:
    - "User can call SpatialOperations::crossProductMotion()"
    - "User can call SpatialOperations::crossProductForce()"
    - "User can call SpatialOperations::transformInertia()"
  artifacts:
    - path: "src/SpatialOperations.cpp"
      provides: "Implementation of SpatialOperations static methods"
      contains: "crossProductMotion, crossProductForce, transformInertia"
    - path: "include/SpatialOperations.h"
      provides: "Declarations for SpatialOperations class"
      exports: ["SpatialOperations class"]
  key_links:
    - from: "src/SpatialOperations.cpp"
      to: "include/SpatialOperations.h"
      via: "implementation file includes header"
      pattern: "#include.*SpatialOperations.h"
    - from: "src/SpatialOperations.cpp"
      to: "include/SpatialUtils.h"
      via: "uses free functions skew, cross, dot"
      pattern: "#include.*SpatialUtils.h"

user_setup: []
---

<objective>
Implement SpatialOperations static utility class methods

Purpose: Provide a static class interface for common spatial algebra operations alongside free functions
Output: Fully implemented SpatialOperations.cpp with all declared methods
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@include/SpatialOperations.h
@include/SpatialUtils.h
@include/PluckerTransform.h
@include/RigidBodyInertia.h
</context>

<interfaces>
<!-- Key types from existing codebase -->

From include/SpatialOperations.h:
```cpp
class SpatialOperations {
public:
    static SpatialVector crossProductMotion(const SpatialVector& v1, const SpatialVector& v2);
    static SpatialVector crossProductForce(const SpatialVector& v, const SpatialVector& f);
    static RigidBodyInertia transformInertia(const RigidBodyInertia& inertia, 
                                            const PluckerTransform& transform);
};
```

From include/SpatialUtils.h (after Plan 01):
```cpp
inline ForceVector cross(const MotionVector& v1, const ForceVector& v2);
inline ForceVector cross(const ForceVector& v1, const ForceVector& v2);
inline MotionVector cross(const MotionVector& v1, const MotionVector& v2);
```

From include/RigidBodyInertia.h (stub - need to check if implemented):
```cpp
class RigidBodyInertia {
    // May be incomplete - check during implementation
};
```

From include/PluckerTransform.h (may be incomplete):
```cpp
class PluckerTransform {
    // May be incomplete - check during implementation
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SpatialOperations.cpp implementation file</name>
  <files>src/SpatialOperations.cpp</files>
  <action>
    Create new file src/SpatialOperations.cpp with implementations:
    
    1. crossProductMotion(const SpatialVector& v1, const SpatialVector& v2):
       - Delegate to v1.crossMotion(v2) method
       - Return SpatialVector result
    
    2. crossProductForce(const SpatialVector& v, const SpatialVector& f):
       - Delegate to v.crossForce(f) method
       - Return SpatialVector result
    
    3. transformInertia(const RigidBodyInertia& inertia, const PluckerTransform& transform):
       - Check if RigidBodyInertia and PluckerTransform classes exist and are complete
       - If complete: implement using Plücker transform formula: I' = X * I * X^T
       - If incomplete: add TODO comment and return default-constructed RigidBodyInertia
       - Document dependency on Phase 5 (Plücker transforms) and Phase 6 (Inertia)
    
    Include necessary headers: SpatialOperations.h, SpatialVector.h, SpatialUtils.h
    Add Doxygen comments for each method with @brief, @param, @return, @details
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | grep -E "(error|warning)" | head -20</automated>
  </verify>
  <done>
    - src/SpatialOperations.cpp created
    - crossProductMotion() implemented and compiles
    - crossProductForce() implemented and compiles
    - transformInertia() has implementation or TODO with documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CMakeLists.txt to include SpatialOperations.cpp</name>
  <files>CMakeLists.txt</files>
  <action>
    Update CMakeLists.txt to compile SpatialOperations.cpp:
    
    The CMakeLists.txt uses: file(GLOB SOURCES "src/*.cpp")
    This automatically includes all .cpp files in src/
    
    Verify:
    1. src/SpatialOperations.cpp exists (created in Task 1)
    2. file(GLOB ...) pattern will pick it up automatically
    
    If GLOB pattern already exists (line 19), no changes needed.
    If manual source list, add src/SpatialOperations.cpp to the list.
  </action>
  <verify>
    <automated>grep -n "GLOB SOURCES\|SpatialOperations" CMakeLists.txt</automated>
  </verify>
  <done>
    - CMakeLists.txt will compile SpatialOperations.cpp (automatic via GLOB or manual addition)
    - Build includes new source file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify transformInertia implementation strategy</name>
  <what-built>
    Task 1 created SpatialOperations.cpp with crossProductMotion and crossProductForce implemented.
    transformInertia may have TODO if dependencies are incomplete.
  </what-built>
  <how-to-verify>
    1. Check if RigidBodyInertia class is complete:
       - Run: grep -c "class RigidBodyInertia" include/RigidBodyInertia.h
       - If has methods beyond constructor, it's partially implemented
    
    2. Check if PluckerTransform class is complete:
       - Run: grep -c "transformMotion\|transformForce" include/PluckerTransform.h
       - If methods exist, PluckerTransform is implemented
    
    3. Decision needed:
       - If BOTH complete: implement transformInertia now
       - If EITHER incomplete: leave TODO, document in SUMMARY, defer to Phase 5/6
    
    Expected: Both classes are likely incomplete (Phase 5 and 6 not started).
    Recommendation: Leave TODO comment, proceed with test suite for crossProductMotion/Force only.
  </how-to-verify>
  <resume-signal>
    Type "approved" if transformInertia should remain TODO (dependencies incomplete),
    or describe implementation approach if classes are complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static class methods → Free functions | Delegation must preserve mathematical correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | crossProductMotion delegation | mitigate | Unit tests verify output matches direct method calls |
| T-04-04 | Tampering | crossProductForce delegation | mitigate | Unit tests verify output matches direct method calls |
| T-04-05 | Repudiation | transformInertia TODO | accept | Document dependency clearly; Phase 5/6 will implement |
</threat_model>

<verification>
- SpatialOperations.cpp compiles without errors
- crossProductMotion and crossProductForce implemented
- transformInertia has implementation or documented TODO
- CMakeLists.txt includes source file in build
</verification>

<success_criteria>
1. SpatialOperations::crossProductMotion() delegates to SpatialVector::crossMotion()
2. SpatialOperations::crossProductForce() delegates to SpatialVector::crossForce()
3. SpatialOperations::transformInertia() has implementation or documented TODO with phase reference
4. All methods have Doxygen documentation
5. Build succeeds with new source file
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-utilities/04-02-SUMMARY.md`
</output>
