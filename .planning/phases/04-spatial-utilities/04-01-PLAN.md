---
phase: 04-spatial-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [include/SpatialUtils.h]
autonomous: true
requirements: [UTL-01, UTL-02, UTL-03]

must_haves:
  truths:
    - "User can create skew-symmetric matrix from Vector3d"
    - "User can compute dot products for any spatial vector combination"
    - "User can compute cross products for motion×motion, force×force, motion×force"
  artifacts:
    - path: "include/SpatialUtils.h"
      provides: "Free functions: skew, dot, cross"
      exports: ["skew(Vector3d)", "dot(...)", "cross(...)"]
  key_links:
    - from: "include/SpatialUtils.h"
      to: "include/SpatialVector.h"
      via: "includes and operates on SpatialVector types"
      pattern: "#include.*SpatialVector"

user_setup: []
---

<objective>
Complete SpatialUtils.h free functions for skew, dot, and cross products

Purpose: Provide standalone utility functions that operate on spatial vectors without requiring class methods
Output: Fully implemented SpatialUtils.h with all cross product overloads
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@include/SpatialUtils.h
@include/SpatialVector.h
@include/MotionVector.h
@include/ForceVector.h
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From include/SpatialVector.h:
```cpp
namespace SpatialAlgebra {
    using Vector3d = Eigen::Matrix<double, 3, 1>;
    
    class SpatialVector {
    protected:
        Vector3d angular;
        Vector3d linear;
    public:
        Vector3d getAngular() const;
        Vector3d getLinear() const;
        double dot(const SpatialVector &other) const;
    };
}
```

From include/MotionVector.h:
```cpp
class MotionVector : public SpatialVector {
    using mv = MotionVector;
};
```

From include/ForceVector.h:
```cpp
class ForceVector : public SpatialVector {
    using fv = ForceVector;
};
```

<!-- Existing SpatialUtils.h already has: skew(), dot() overloads, cross(MotionVector, ForceVector) -->
<!-- This plan adds: cross(MotionVector, MotionVector), cross(ForceVector, ForceVector) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add cross product overloads to SpatialUtils.h</name>
  <files>include/SpatialUtils.h</files>
  <action>
    Add two new cross product free functions to SpatialUtils.h:
    
    1. `cross(const MotionVector& v1, const MotionVector& v2)` - motion cross motion
       - Formula: [ω1×ω2; ω1×v2 + v1×ω2]
       - Returns: MotionVector
       - Implementation: Extract angular/linear components, compute 3D cross products, return MotionVector
    
    2. `cross(const ForceVector& v1, const ForceVector& v2)` - force cross force
       - Formula: [τ1×τ2 + f1×f2; τ1×f2]
       - Returns: ForceVector
       - Implementation: Extract angular/linear components, compute 3D cross products, return ForceVector
    
    Place these functions after the existing cross(MotionVector, ForceVector) function (around line 88).
    Use inline noexcept for consistency with existing functions.
    Add Doxygen comments following existing pattern (@brief, @param, @return, @details with formula).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | grep -E "(error|warning)" | head -20</automated>
  </verify>
  <done>
    - SpatialUtils.h compiles without errors
    - Two new cross() overloads added with correct formulas
    - Doxygen comments present on all new functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify skew() and dot() functions are complete</name>
  <files>include/SpatialUtils.h</files>
  <action>
    Audit existing SpatialUtils.h to verify completeness:
    
    1. skew(Vector3d) - already implemented (lines 24-29), verify formula:
       - Should produce: [0, -vz, vy; vz, 0, -vx; -vy, vx, 0]
    
    2. dot() overloads - already has 4 versions (lines 38-68):
       - dot(SpatialVector, SpatialVector)
       - dot(MotionVector, MotionVector)
       - dot(ForceVector, ForceVector)
       - dot(MotionVector, ForceVector)
       - Verify all cast correctly to base SpatialVector and delegate
    
    3. cross() overloads - after Task 1, should have 3 versions:
       - cross(MotionVector, ForceVector) - existing
       - cross(MotionVector, MotionVector) - added in Task 1
       - cross(ForceVector, ForceVector) - added in Task 1
    
    No code changes needed if all functions present and correct.
  </action>
  <verify>
    <automated>grep -c "inline.*cross\|inline.*dot\|inline.*skew" include/SpatialUtils.h</automated>
  </verify>
  <done>
    - skew() function present (1 function)
    - dot() overloads present (4 functions)
    - cross() overloads present (3 functions after Task 1)
    - Total: 8 inline utility functions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library API → User code | Pure mathematical functions, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | cross() formulas | mitigate | Use Featherstone formulas exactly as documented; add property-based tests in Plan 02 |
| T-04-02 | Information Disclosure | noexcept functions | accept | Pure functions with no side effects, no sensitive data |
</threat_model>

<verification>
- Build succeeds: `cmake --build build` produces no errors
- All 8 inline functions present in SpatialUtils.h
- Formulas match Featherstone textbook definitions
</verification>

<success_criteria>
1. skew() creates correct 3x3 skew-symmetric matrix
2. dot() has 4 overloads covering all vector type combinations
3. cross() has 3 overloads: motion×motion, force×force, motion×force
4. All functions are inline noexcept for performance
5. All functions have Doxygen documentation
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-utilities/04-01-SUMMARY.md`
</output>
