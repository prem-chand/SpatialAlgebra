---
phase: 20-test-model-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-models/robot_model.h
  - tests/test-models/robot_solver.h
  - tests/test-models/CMakeLists.txt
  - CMakeLists.txt
autonomous: true
requirements:
  - TML-01
  - TML-03
  - TML-05

must_haves:
  truths:
    - "robot_model.h compiles with only Eigen3 includes — no SpatialAlgebra types transitively reachable"
    - "robot_solver.h compiles with only Eigen3 includes — abstract interface, no implementation binding"
    - "CMake INTERFACE target test_models links Eigen3::Eigen only — no SpatialAlgebra dependency"
    - "Root CMakeLists.txt adds test-models subdirectory after enable_testing() — CMake configuration succeeds"
    - "All public declarations have Doxygen @brief/@details following project conventions"
  artifacts:
    - path: "tests/test-models/robot_model.h"
      provides: "JointSpec struct, RobotModel struct, JointType enum"
      contains: "struct JointSpec"
    - path: "tests/test-models/robot_solver.h"
      provides: "RobotSolver abstract base class with full API per D-02"
      contains: "class RobotSolver"
    - path: "tests/test-models/CMakeLists.txt"
      provides: "INTERFACE target test_models + sa_test_adapter library + compile_smoke_test executable"
      contains: "add_library(test_models INTERFACE)"
    - path: "CMakeLists.txt"
      provides: "Root build entry with add_subdirectory(tests/test-models)"
      contains: "add_subdirectory(tests/test-models)"
  key_links:
    - from: "tests/test-models/CMakeLists.txt"
      to: "Eigen3::Eigen"
      via: "target_link_libraries(test_models INTERFACE Eigen3::Eigen)"
      pattern: "target_link_libraries\\(test_models INTERFACE Eigen3::Eigen\\)"
    - from: "tests/test-models/robot_solver.h"
      to: "tests/test-models/robot_model.h"
      via: "#include \"robot_model.h\""
      pattern: "#include \"robot_model\\.h\""
    - from: "CMakeLists.txt"
      to: "tests/test-models/CMakeLists.txt"
      via: "add_subdirectory(tests/test-models) after enable_testing()"
      pattern: "add_subdirectory\\(tests/test-models\\)"
---

<objective>
Create the core data types (RobotModel, JointSpec, RobotSolver) and CMake infrastructure for the zero-dependency
test model library.

Purpose: Establish the compilation firewall (INTERFACE target with only Eigen3) and define the pure-Eigen data
contracts that all chains/ headers and adapters will build upon. The compile_smoke_test target is wired but the
smoke test .cpp is updated in Plan 04.

Output: `robot_model.h` (POD structs), `robot_solver.h` (abstract interface), `test-models/CMakeLists.txt`
(INTERFACE + compiled targets), root `CMakeLists.txt` (add_subdirectory entry).
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-test-model-library/20-CONTEXT.md
@.planning/phases/20-test-model-library/20-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from the codebase. -->

From include/ForwardDynamics.h (reference for JointSpec→Link conversion in Plan 04):
```cpp
struct Link {  // aka ForwardDynamicsLink
    int parent;
    PluckerTransform X;
    RigidBodyInertia I;
    MotionVector S;
    double q, qdot, qddot;
    MotionVector v, c;
    ForceVector f;
    ArticulatedBodyInertia Ia;
    ForceVector pa;
};
```

From include/InverseDynamics.h (reference for JointSpec→InverseDynamicsLink conversion in Plan 04):
```cpp
struct InverseDynamicsLink {
    int parent;
    PluckerTransform X;
    RigidBodyInertia I;
    MotionVector S;
    double q, qdot, qddot;
    MotionVector v, a;
};
```

From benchmarks/common/model_factory.cpp (reference conversion pattern — lines 19-50 for FD, 52-81 for ID):
```cpp
// FD link construction pattern:
link.parent = i - 1;
link.X = PluckerTransform(Rotation(Eigen::Matrix3d::Identity()), cfg.translation);
link.I = RigidBodyInertia(cfg.mass, cfg.com, lt::Identity(3));
link.S = cfg.axis;  // MotionVector
```

From include/RigidBodyInertia.h:
```cpp
RigidBodyInertia(double mass, const Vector3d &com, const lt &inertiaMatrixLT);
// lt = LowerTriangular
```

From include/LowerTriangular.h:
```cpp
static LowerTriangular Identity(int size);
static LowerTriangular fromFullMatrix(const Eigen::MatrixXd &fullMatrix);
```

From Root CMakeLists.txt (lines 12, 54-55):
```cmake
find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)    # line 12
enable_testing()                                      # line 55
```
Existing test executables registered at lines 58-160.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create robot_model.h — JointSpec, RobotModel, JointType</name>
  <files>tests/test-models/robot_model.h</files>
  <read_first>
Read these files BEFORE implementing — do not assume:
- tests/test-models/robot_model.h (target file; will not exist yet — create from scratch)
- .planning/phases/20-test-model-library/20-CONTEXT.md (D-07 JointSpec field layout, D-08 struct definition, D-09 JointType enum)
- .planning/phases/20-test-model-library/20-RESEARCH.md (lines 617-686: canonical RobotModel/JointSpec definition with field defaults)
- benchmarks/common/model_factory.h (lines 32-51: JointConfig struct pattern — a prior art for per-link struct design; compare field names and defaults)
- include/ForwardDynamics.h (lines 82-114: Link struct fields — reference for SA-side field naming conventions like parent, com, mass)
  </read_first>
  <action>
Create a NEW file at tests/test-models/robot_model.h with #pragma once guard. Place everything in
namespace test_models. The file must include ONLY Eigen headers (Eigen/Dense, vector, string) —
NO SpatialAlgebra headers per D-07/D-12.

Define three types, each with full Doxygen @brief/@details per project convention:

1. enum class JointType { REVOLUTE, PRISMATIC, FIXED } — Per D-09. Doxygen: document that
   REVOLUTE uses jointAxis as rotation axis (omega), PRISMATIC uses jointAxis as translation
   direction (v), FIXED means rigid 0-DOF connection.

2. struct JointSpec — Per D-08. Fields with EXACT names, types, and defaults:
   - int parent = -1
   - Eigen::Matrix4d parentToJoint = Eigen::Matrix4d::Identity()
   - Eigen::Vector3d jointAxis = Eigen::Vector3d::UnitZ()
   - JointType type = JointType::REVOLUTE
   - double mass = 1.0
   - Eigen::Vector3d com = Eigen::Vector3d::Zero()
   - Eigen::Matrix3d inertia = Eigen::Matrix3d::Identity()
   - std::string name (empty default)

   Doxygen MUST document: that parentToJoint uses homogeneous [R|t; 0|1] convention in parent
   frame, that inertia is 3x3 dense rotational inertia at COM (not lower-triangular packed),
   and reference Featherstone (2008).

3. struct RobotModel — Per D-07/D-08. Field:
   - std::vector&lt;JointSpec&gt; joints

   One accessor method:
   - int getDOF() const returning static_cast&lt;int&gt;(joints.size()) — Per RESEARCH.md
     Option B, derives DOF from joints array size (1 DOF per joint).

   Doxygen: document topological ordering requirement (parents before children), note support
   for branching trees (multiple children per parent with consecutive indices).

Use namespace-level type alias inside namespace test_models (NOT at file scope outside namespace)
for type aliases as needed. The file ends with a closing namespace brace and comment.
  </action>
  <acceptance_criteria>
- File tests/test-models/robot_model.h exists
- Contains: enum class JointType with REVOLUTE, PRISMATIC, FIXED values
- Contains: struct JointSpec with all 8 fields matching D-08 types and defaults exactly (parent, parentToJoint, jointAxis, type, mass, com, inertia, name)
- Contains: struct RobotModel with std::vector&lt;JointSpec&gt; joints member
- Contains: int getDOF() const returning static_cast&lt;int&gt;(joints.size())
- Doxygen @brief on JointType enum, JointSpec struct, RobotModel struct, and getDOF() method
- Includes ONLY Eigen/Dense, vector, string — grep for "SpatialAlgebra" in the file returns zero matches
- All declarations inside namespace test_models
- Uses #pragma once (matching LowerTriangular.h convention used in this repo)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#include" tests/test-models/robot_model.h | head -1 — verify only Eigen/STL includes; grep -c "SpatialAlgebra" tests/test-models/robot_model.h — must be 0</automated>
  </verify>
  <done>robot_model.h exists with JointSpec (8 fields), RobotModel (joints vector + getDOF()), JointType enum, Doxygen on all, zero SpatialAlgebra includes.</done>
</task>

<task type="auto">
  <name>Task 2: Create robot_solver.h — RobotSolver abstract base class</name>
  <files>tests/test-models/robot_solver.h</files>
  <read_first>
Read these files BEFORE implementing — do not assume:
- tests/test-models/robot_solver.h (target file; will not exist yet — create from scratch)
- tests/test-models/robot_model.h (created by Task 1 — read for JointType, JointSpec, RobotModel types consumed by this header)
- .planning/phases/20-test-model-library/20-CONTEXT.md (D-01 abstract base class, D-02 full API surface, D-03 dynamic Eigen types, D-04 hybrid state, D-05 builder pattern, D-06 naming)
- .planning/phases/20-test-model-library/20-RESEARCH.md (lines 689-796: canonical RobotSolver definition with all method signatures)
- include/ForwardDynamics.h (lines 137-203: ForwardDynamics class — reference for computeAccelerations signature, gravity parameter convention)
- include/InverseDynamics.h (lines 120-176: InverseDynamics class — reference for computeTorques signature, gravity parameter convention)
  </read_first>
  <action>
Create a NEW file at tests/test-models/robot_solver.h with #pragma once guard. Place everything in
namespace test_models. The file must include ONLY robot_model.h, Eigen/Dense, memory, vector —
NO SpatialAlgebra headers per D-12.

Define class RobotSolver per D-01: abstract base class with public pure virtual destructor and
10 pure virtual methods per D-02. All methods use D-03 dynamic Eigen types (VectorXd, MatrixXd,
Vector3d, Matrix4d) — NO templates on scalar or DOF count.

Method signatures (EXACT — copy these types):

1. virtual ~RobotSolver() = default;

2. virtual void setState(const Eigen::VectorXd& q, const Eigen::VectorXd& qdot) = 0;
   - Doxygen: Loads joint configuration and velocity. Rebuilds internal cache as needed per D-04.

3. virtual Eigen::VectorXd computeTorques(const Eigen::VectorXd& qddot,
       const Eigen::Vector3d& gravity = Eigen::Vector3d::Zero()) = 0;
   - Doxygen: Inverse dynamics (RNEA). Input qddot size must equal getDOF().
     Returns joint torques tau. May throw std::invalid_argument on size mismatch.

4. virtual Eigen::VectorXd computeAccelerations(const Eigen::VectorXd& tau,
       const Eigen::Vector3d& gravity = Eigen::Vector3d::Zero()) = 0;
   - Doxygen: Forward dynamics (ABA). Input tau size must equal getDOF().
     Returns joint accelerations qddot. May throw std::invalid_argument on size mismatch
     or std::runtime_error on singular configuration.

5. virtual void forwardKinematics() = 0;
   - Doxygen: Recomputes link-to-world transforms from current joint positions.
     Per RESEARCH.md open question 1, implement auto-call pattern in adapter:
     setState() marks dirty flag, first query method triggers forwardKinematics().
     The abstract interface declares the method; dirty-flag behavior is in adapter.

6. virtual Eigen::Matrix4d getJointTransform(int idx) const = 0;
   - Doxygen: Returns world-frame 4x4 homogeneous transform for link idx.
     May throw std::out_of_range if idx out of bounds.

7. virtual Eigen::MatrixXd computeMassMatrix() = 0;
   - Doxygen: Joint-space inertia matrix H(q), DOF x DOF symmetric.
     Research recommends RNEA-column method (call computeTorques with unit vectors).

8. virtual Eigen::VectorXd computeGravityTorques(const Eigen::Vector3d& gravity) = 0;
   - Doxygen: Gravity compensation torques. Equivalent to computeTorques(zero_qddot, gravity).

9. virtual Eigen::MatrixXd computeJointSpaceJacobian(int idx) = 0;
   - Doxygen: 6 x DOF geometric Jacobian for link idx in world frame.
     Research recommends column-by-column method using cached link transforms.

10. virtual Eigen::Vector3d getLinkCOM(int idx) const = 0;
    - Doxygen: World-frame center of mass position for link idx.

11. virtual int getDOF() const = 0;
    - Doxygen: Total degrees of freedom.

Every method MUST have Doxygen @brief with parameter documentation.
Class-level Doxygen: document the abstract solver pattern, state management via setState(),
builder pattern via build(const RobotModel&) static factory per D-05 (each adapter subclass
provides its own build() — not declared here since return type varies).

Do NOT declare build() in RobotSolver — each adapter (SpatialAlgebraAdapter in Plan 04,
future PinocchioAdapter) provides its own static build() returning std::unique_ptr of the
concrete type. The RobotModel reference is passed to each adapter's build().
  </action>
  <acceptance_criteria>
- File tests/test-models/robot_solver.h exists
- Contains: class RobotSolver with virtual ~RobotSolver() = default
- Contains: 10 pure virtual methods matching D-02 API surface exactly (setState, computeTorques, computeAccelerations, forwardKinematics, getJointTransform, computeMassMatrix, computeGravityTorques, computeJointSpaceJacobian, getLinkCOM, getDOF)
- All method parameters use dynamic Eigen types (VectorXd for vectors, MatrixXd for matrices, Vector3d for gravity, Matrix4d for transforms) — NO templates, NO fixed-size parameters for joint-space vectors
- computeTorques and computeAccelerations have const Eigen::Vector3d& gravity = Eigen::Vector3d::Zero() default parameter
- Doxygen @brief on class and every method with @param documentation
- Includes ONLY robot_model.h, Eigen/Dense, memory, vector — grep for "SpatialAlgebra" returns zero
- All declarations inside namespace test_models
- Uses #pragma once
  </acceptance_criteria>
  <verify>
    <automated>grep -c "#include" tests/test-models/robot_solver.h | head -1; grep -c "SpatialAlgebra" tests/test-models/robot_solver.h — must be 0; grep -c "virtual.*= 0" tests/test-models/robot_solver.h — must be at least 10</automated>
  </verify>
  <done>robot_solver.h exists with RobotSolver abstract class, 10 pure virtual methods per D-02, Doxygen on all, zero SpatialAlgebra includes, no templates.</done>
</task>

<task type="auto">
  <name>Task 3: Create test-models CMakeLists.txt + update root CMakeLists.txt</name>
  <files>tests/test-models/CMakeLists.txt, CMakeLists.txt</files>
  <read_first>
Read these files BEFORE implementing — do not assume:
- tests/test-models/CMakeLists.txt (target file; will not exist yet — create from scratch)
- CMakeLists.txt (root — read for exact line placement of add_subdirectory)
- .planning/phases/20-test-model-library/20-CONTEXT.md (D-13 INTERFACE target, D-14 sa_test_adapter, D-15 no conversion of existing tests)
- .planning/phases/20-test-model-library/20-RESEARCH.md (lines 547-612: CMake integration strategy, exact CMakeLists.txt content)
</read_first>
  <action>
TWO file modifications:

FILE 1 — CREATE tests/test-models/CMakeLists.txt:

Define these targets in order:

1. INTERFACE library test_models (D-13):
```cmake
add_library(test_models INTERFACE)
target_include_directories(test_models INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(test_models INTERFACE Eigen3::Eigen)
```
NOTE: test_models links ONLY Eigen3::Eigen — no SpatialAlgebra. This enforces D-12 compilation firewall.

2. Compiled library sa_test_adapter (D-14):
```cmake
add_library(sa_test_adapter sa_adapter.cpp)
target_link_libraries(sa_test_adapter PUBLIC test_models SpatialAlgebra)
target_include_directories(sa_test_adapter PRIVATE ${CMAKE_SOURCE_DIR}/include)
```
NOTE: sa_test_adapter links BOTH test_models AND SpatialAlgebra. sa_adapter.cpp is created in Plan 04.
This target declaration is needed NOW so the CMake configuration succeeds even before sa_adapter.cpp exists —
CMake only evaluates the source list at build time, not configure time.

3. Smoke test executable compile_smoke_test:
```cmake
add_executable(compile_smoke_test
    ${CMAKE_SOURCE_DIR}/tests/compile_smoke_test.cpp
)
target_link_libraries(compile_smoke_test PRIVATE test_models)
add_test(NAME compile_smoke_test COMMAND compile_smoke_test)
```
NOTE: Links ONLY test_models, NOT SpatialAlgebra — this verifies the zero-dependency guarantee.
The compile_smoke_test.cpp source is updated in Plan 04. The target declaration here ensures
CMake configuration succeeds; the old .cpp includes SpatialAlgebra.h which will fail at build
time until Plan 04 updates it. This is intentional — the build failure is a guard.

4. Install rule (TML-05 standalone installability):
```cmake
install(TARGETS test_models EXPORT SpatialAlgebraTargets)
install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/
    DESTINATION include/test-models
    FILES_MATCHING PATTERN "*.h"
)
```

Add a comment block at the top explaining: "Zero-dependency test model library. test_models
INTERFACE target links only Eigen3. sa_test_adapter is the sole bridge to SpatialAlgebra."

FILE 2 — MODIFY root CMakeLists.txt:

Add ONE line after enable_testing() (line 55) and before the existing add_executable(TestSpatialVector...)
block (line 58):
```cmake
add_subdirectory(tests/test-models)
```

Placement rationale per RESEARCH.md lines 547-562: Eigen3 already found at line 12,
enable_testing() at line 55, so CMake variables are available. Existing test targets
at lines 58+ are NOT modified per D-15.

Do NOT modify any existing test executable declarations or link lines. Phase 20 does NOT
convert existing tests.

VERIFY: The root CMakeLists.txt diff should show exactly one line added.
  </action>
  <acceptance_criteria>
- tests/test-models/CMakeLists.txt exists
- Contains: add_library(test_models INTERFACE) with Eigen3::Eigen link only (no SpatialAlgebra)
- Contains: add_library(sa_test_adapter sa_adapter.cpp) linking test_models + SpatialAlgebra
- Contains: add_executable(compile_smoke_test) linking test_models only
- Contains: add_test(NAME compile_smoke_test COMMAND compile_smoke_test)
- Contains: install(TARGETS test_models EXPORT SpatialAlgebraTargets)
- Root CMakeLists.txt has add_subdirectory(tests/test-models) after enable_testing() and before existing test targets
- Existing add_executable lines for TestSpatialVector through TestDynamicsConsistency are UNCHANGED
  </acceptance_criteria>
  <verify>
    <automated>cmake -B build 2>&1 | tail -20 — must complete without error; grep "add_subdirectory(tests/test-models)" CMakeLists.txt — must return 1 match; grep -c "add_library(test_models INTERFACE)" tests/test-models/CMakeLists.txt — must be 1; grep "target_link_libraries(test_models INTERFACE Eigen3::Eigen)" tests/test-models/CMakeLists.txt — must return 1 match; grep "SpatialAlgebra" tests/test-models/CMakeLists.txt | grep -v "#" — must match ONLY on sa_test_adapter line (NOT on test_models line)</automated>
  </verify>
  <done>CMakeLists.txt at tests/test-models/ defines INTERFACE test_models (Eigen3 only), sa_test_adapter (test_models + SpatialAlgebra), and compile_smoke_test. Root CMakeLists.txt adds subdirectory. cmake configure succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_models headers → consumers | Including robot_model.h or robot_solver.h must NOT transitively pull in SpatialAlgebra types. Enforced by INTERFACE target linking only Eigen3::Eigen. |
| robot_solver.h API → callers | Public virtual methods accept untrusted Eigen vector/matrix inputs. Input validation (size checks, bounds) is deferred to adapter implementations in Plan 04. |
| CMakeLists.txt → build system | add_subdirectory placement must come after find_package(Eigen3) to resolve Eigen3::Eigen imported target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Denial of Service | robot_solver.h / RobotSolver | accept | Abstract interface has no implementation — no runtime attack surface. Size mismatch threats apply to adapter (Plan 04), not interface definition. |
| T-20-02 | Tampering | robot_model.h / JointSpec | accept | Pure data POD — no validation logic, no computation. Invalid values are caught downstream by adapters. |
| T-20-03 | Denial of Service | robot_model.h / Eigen includes | accept | NaN/Inf propagation is Eigen's default behavior. NaN values in JointSpec fields would produce NaN dynamics outputs — no worse than handwritten test models. |
| T-20-04 | Denial of Service | CMakeLists.txt / INTERFACE target | accept | Build-time only — no runtime memory allocation. INTERFACE target has no compiled objects. |
| T-20-SC | Tampering | None | N/A | No external packages added in this plan. Eigen3 is pre-verified project dependency (RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
After all 3 tasks complete:
1. Run `cmake -B build` — must succeed (even though compile_smoke_test .cpp still references SA headers — the target declaration is valid CMake; the build failure is Plan 04's problem)
2. Verify: `grep -rn "SpatialAlgebra" tests/test-models/robot_model.h tests/test-models/robot_solver.h` returns zero matches
3. Verify: `grep "target_link_libraries(test_models INTERFACE Eigen3::Eigen)" tests/test-models/CMakeLists.txt` returns exactly 1 match
4. Verify: `grep "add_subdirectory(tests/test-models)" CMakeLists.txt` returns exactly 1 match
5. Verify: Doxygen block comments on all 4 public declarations (JointType, JointSpec, RobotModel, RobotSolver)
</verification>

<success_criteria>
- robot_model.h: JointType enum with 3 values, JointSpec struct with 8 fields (correct Eigen types), RobotModel struct with joints vector + getDOF()
- robot_solver.h: RobotSolver abstract class with 10 pure virtual methods per D-02, zero SA includes
- test-models/CMakeLists.txt: INTERFACE test_models (Eigen3 only) + sa_test_adapter (test_models + SpatialAlgebra) + compile_smoke_test
- Root CMakeLists.txt: add_subdirectory(tests/test-models) placed correctly, no existing test changes
- cmake configure succeeds
- All public declarations have Doxygen @brief/@details
</success_criteria>

<output>
Create .planning/phases/20-test-model-library/20-01-SUMMARY.md when done
</output>
