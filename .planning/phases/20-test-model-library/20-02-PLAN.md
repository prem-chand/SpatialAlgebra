---
phase: 20-test-model-library
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-models/chains/spatial_vectors.h
  - tests/test-models/chains/plucker_transforms.h
  - tests/test-models/chains/rotation.h
  - tests/test-models/chains/lower_triangular.h
  - tests/test-models/chains/rigid_body_inertia.h
  - tests/test-models/chains/articulated_body.h
  - tests/test-models/chains/spatial_utils.h
autonomous: true
requirements:
  - TML-02

must_haves:
  truths:
    - "All 7 simple-domain chains/ headers compile using only robot_model.h and Eigen3"
    - "Each header declares 2-4 make*() functions returning RobotModel with valid JointSpec entries"
    - "Factory function parameters match existing test fixture construction patterns from the 11 test files"
    - "Doxygen docstrings explain kinematic layout and intended test usage on every function"
  artifacts:
    - path: "tests/test-models/chains/spatial_vectors.h"
      provides: "makeSingleLink() factory for spatial vector transform tests"
      contains: "makeSingleLink"
    - path: "tests/test-models/chains/plucker_transforms.h"
      provides: "makeIdentityTransform(), make90DegreeZRotation(), makePureTranslation(), makeCombinedTransform()"
      contains: "makeIdentityTransform"
    - path: "tests/test-models/chains/rotation.h"
      provides: "makeIdentityRotation(), make45DegreeXRotation(), make90DegreeZRotationFixture()"
      contains: "make45DegreeXRotation"
    - path: "tests/test-models/chains/lower_triangular.h"
      provides: "makeIdentityLT(), makeDiagonalLT(), makeArbitraryLT()"
      contains: "makeIdentityLT"
    - path: "tests/test-models/chains/rigid_body_inertia.h"
      provides: "makeRBIDefault(), makeRBIOffsetCOM(), makeRBIDiagonalInertia()"
      contains: "makeRBIDefault"
    - path: "tests/test-models/chains/articulated_body.h"
      provides: "makeABIIdentity(), makeABIReduced()"
      contains: "makeABIIdentity"
    - path: "tests/test-models/chains/spatial_utils.h"
      provides: "makeSkewFixture(), makeMotionVectorPair(), makeForceVectorPair()"
      contains: "makeSkewFixture"
  key_links:
    - from: "chains/spatial_vectors.h"
      to: "robot_model.h"
      via: "#include \"robot_model.h\""
      pattern: "#include \"robot_model\\.h\""
    - from: "chains/plucker_transforms.h"
      to: "robot_model.h"
      via: "JointSpec with non-identity parentToJoint"
      pattern: "parentToJoint"
---

<context>
@.planning/phases/20-test-model-library/20-RESEARCH.md (lines 391-505: Test Domain Extraction — model inventory for each domain)
@.planning/phases/20-test-model-library/20-CONTEXT.md (D-10 directory layout, D-11 one header per domain, D-07 pure data POD, D-08 JointSpec fields)

<interfaces>
<!-- Key types from Plan 01 that these factory functions consume -->

From tests/test-models/robot_model.h (created by Plan 01):
```cpp
namespace test_models {
    enum class JointType { REVOLUTE, PRISMATIC, FIXED };
    struct JointSpec {
        int parent = -1;
        Eigen::Matrix4d parentToJoint = Eigen::Matrix4d::Identity();
        Eigen::Vector3d jointAxis = Eigen::Vector3d::UnitZ();
        JointType type = JointType::REVOLUTE;
        double mass = 1.0;
        Eigen::Vector3d com = Eigen::Vector3d::Zero();
        Eigen::Matrix3d inertia = Eigen::Matrix3d::Identity();
        std::string name;
    };
    struct RobotModel {
        std::vector<JointSpec> joints;
        int getDOF() const;
    };
}
```

Factory functions MUST use the field-initializer pattern shown in RESEARCH.md lines 297-315:
```cpp
inline RobotModel makeExampleChain() {
    RobotModel model;
    JointSpec link;
    link.parent = -1;
    link.parentToJoint = Eigen::Matrix4d::Identity();
    link.jointAxis = Eigen::Vector3d::UnitZ();
    link.type = JointType::REVOLUTE;
    link.mass = 1.0;
    link.com = Eigen::Vector3d::Zero();
    link.inertia = Eigen::Matrix3d::Identity();
    link.name = "base";
    model.joints.push_back(link);
    return model;
}
```
NOT aggregate initialization — use explicit field assignment for readability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spatial_vectors.h, plucker_transforms.h, rotation.h</name>
  <files>tests/test-models/chains/spatial_vectors.h, tests/test-models/chains/plucker_transforms.h, tests/test-models/chains/rotation.h</files>
  <read_first>
Read these files BEFORE implementing — do not assume:
- tests/test-models/chains/spatial_vectors.h (target file; will not exist yet)
- tests/test-models/chains/plucker_transforms.h (target file; will not exist yet)
- tests/test-models/chains/rotation.h (target file; will not exist yet)
- tests/test-models/robot_model.h (created by Plan 01 — read for JointSpec/RobotModel API consumed by factory functions)
- .planning/phases/20-test-model-library/20-RESEARCH.md (lines 396-418: Domain 1 spatial vectors model requirements, Domain 2 plucker transforms model requirements, Domain 3 rotation model requirements)
  </read_first>
  <action>
Create THREE new headers in tests/test-models/chains/. Each uses #pragma once, includes only
"robot_model.h" and Eigen/Dense, places everything in namespace test_models.

All functions are inline (header-only per D-13). Every function has Doxygen @brief/@details
and @return documentation per project convention. Functions return RobotModel by value.

===== FILE 1: tests/test-models/chains/spatial_vectors.h =====

Function: inline RobotModel makeSingleLink()
- 1-DOF chain, Z-axis revolute, 1 link
- JointSpec: parent=-1, parentToJoint=I_4x4, jointAxis=UnitZ(), type=REVOLUTE,
  mass=1.0, com=Zero(), inertia=Identity_3x3, name="link0"
- Doxygen: "Single-link Z-revolute chain for basic spatial vector transform tests.
  Matches Featherstone Example 2.1 pattern."

===== FILE 2: tests/test-models/chains/plucker_transforms.h =====

Four functions, each producing a single-link chain (parent=-1, Z-revolute, mass=1, name="link0"
except where the transform itself varies):

1. inline RobotModel makeIdentityTransform()
   - JointSpec with parentToJoint=Eigen::Matrix4d::Identity()
   - Doxygen: "Identity transform (R=I, t=0). Tests PluckerTransform identity behavior."

2. inline RobotModel make90DegreeZRotation()
   - JointSpec with parentToJoint built from 90° Z rotation: use
     Eigen::AngleAxisd(M_PI_2, Eigen::Vector3d::UnitZ()).matrix() for the 3x3 R,
     then assemble into 4x4: topLeftCorner=AngleAxis result, topRightCorner=Zero,
     bottom row [0 0 0 1]
   - Doxygen: "90-degree Z-axis rotation transform. Tests non-identity PluckerTransform
     rotation component."

3. inline RobotModel makePureTranslation()
   - JointSpec with parentToJoint: identity rotation, translation Vector3d(1.0, 0.0, 0.0)
     in the topRightCorner of the 4x4
   - Doxygen: "Pure [1,0,0] translation transform with identity rotation.
     Tests PluckerTransform translation component."

4. inline RobotModel makeCombinedTransform()
   - JointSpec with parentToJoint: 90° Z rotation + [1,0,0] translation combined
     (same R as make90DegreeZRotation, same t as makePureTranslation)
   - Doxygen: "Combined 90-degree Z rotation + [1,0,0] translation.
     Tests full PluckerTransform composition."

===== FILE 3: tests/test-models/chains/rotation.h =====

Three functions, each producing a single-link chain:

1. inline RobotModel makeIdentityRotation()
   - parentToJoint with identity rotation, zero translation
   - Doxygen: "Zero rotation transform. Baseline for rotation comparison tests."

2. inline RobotModel make45DegreeXRotation()
   - parentToJoint using Eigen::AngleAxisd(M_PI_4, Eigen::Vector3d::UnitX()).matrix()
     for the 3x3 rotation part, zero translation
   - Doxygen: "45-degree X-axis rotation. Tests non-axial rotation behavior."

3. inline RobotModel make90DegreeZRotationFixture()
   - parentToJoint using Eigen::AngleAxisd(M_PI_2, Eigen::Vector3d::UnitZ()).matrix()
     for 3x3 rotation, zero translation
   - Doxygen: "90-degree Z rotation fixture. Matches PluckerTransform test patterns."
   - NOTE: Name uses "Fixture" suffix to distinguish from make90DegreeZRotation() in
     plucker_transforms.h (that one targets transform tests; this one targets
     rotation-only tests)

For ALL three headers: Use Eigen::AngleAxisd for rotation construction — it reads naturally
and matches existing test patterns. Build the 4x4 as:
```cpp
Eigen::Matrix4d T = Eigen::Matrix4d::Identity();
T.topLeftCorner<3,3>() = Eigen::AngleAxisd(angle, axis).matrix();
T.topRightCorner<3,1>() = Eigen::Vector3d::Zero(); // or translation vector
```
  </action>
  <acceptance_criteria>
- tests/test-models/chains/spatial_vectors.h exists with makeSingleLink() returning RobotModel
- tests/test-models/chains/plucker_transforms.h exists with 4 functions: makeIdentityTransform(), make90DegreeZRotation(), makePureTranslation(), makeCombinedTransform()
- tests/test-models/chains/rotation.h exists with 3 functions: makeIdentityRotation(), make45DegreeXRotation(), make90DegreeZRotationFixture()
- All 8 functions return RobotModel by value, declared inline
- All functions have Doxygen @brief/@details/@return
- All transform construction uses Eigen::AngleAxisd for rotation, Matrix4d for homogeneous assembly
- Each header includes ONLY robot_model.h and Eigen/Dense — grep for "SpatialAlgebra" returns zero
- All in namespace test_models
- All uses #pragma once
  </acceptance_criteria>
  <verify>
    <automated>ls tests/test-models/chains/spatial_vectors.h tests/test-models/chains/plucker_transforms.h tests/test-models/chains/rotation.h; for f in tests/test-models/chains/spatial_vectors.h tests/test-models/chains/plucker_transforms.h tests/test-models/chains/rotation.h; do grep -c "SpatialAlgebra" "$f" || true; done — all must be 0</automated>
  </verify>
  <done>Three chains/ headers created: spatial_vectors.h (1 func), plucker_transforms.h (4 funcs), rotation.h (3 funcs). All in namespace test_models, Doxygen on all, zero SA includes.</done>
</task>

<task type="auto">
  <name>Task 2: Create lower_triangular.h, rigid_body_inertia.h, articulated_body.h, spatial_utils.h</name>
  <files>tests/test-models/chains/lower_triangular.h, tests/test-models/chains/rigid_body_inertia.h, tests/test-models/chains/articulated_body.h, tests/test-models/chains/spatial_utils.h</files>
  <read_first>
Read these files BEFORE implementing — do not assume:
- tests/test-models/chains/lower_triangular.h (target file; will not exist yet)
- tests/test-models/chains/rigid_body_inertia.h (target file; will not exist yet)
- tests/test-models/chains/articulated_body.h (target file; will not exist yet)
- tests/test-models/chains/spatial_utils.h (target file; will not exist yet)
- tests/test-models/robot_model.h (created by Plan 01 — read for JointSpec/RobotModel API)
- .planning/phases/20-test-model-library/20-RESEARCH.md (lines 419-449: Domain 4 lower triangular, Domain 5 rigid body inertia, Domain 6 articulated body, Domain 7 spatial utils model requirements)
  </read_first>
  <action>
Create FOUR new headers in tests/test-models/chains/. Same conventions as Task 1: #pragma once,
include only "robot_model.h" and Eigen/Dense, namespace test_models, all functions inline.

===== FILE 1: tests/test-models/chains/lower_triangular.h =====

Three functions, each producing a single-link chain (parent=-1, Z-revolute, mass=1, name="link0"):

1. inline RobotModel makeIdentityLT()
   - inertia = Eigen::Matrix3d::Identity()
   - Doxygen: "Identity 3x3 lower triangular inertia. Tests LT identity construction."

2. inline RobotModel makeDiagonalLT(double value = 2.0)
   - inertia = value * Eigen::Matrix3d::Identity()
   - Doxygen: "@param value Diagonal value (default 2.0). Scaled identity inertia.
     Tests LT scalar operations."

3. inline RobotModel makeArbitraryLT()
   - inertia with non-trivial lower triangle values:
     Row 0: (1,0,0); Row 1: (2,3,0); Row 2: (4,5,6)
     Build as: M << 1,0,0, 2,3,0, 4,5,6 (dense, non-symmetric — the adapter symmetrizes)
   - Doxygen: "Non-trivial lower triangular inertia with values [1,0,0; 2,3,0; 4,5,6].
     Tests LT packing and unpacking."

===== FILE 2: tests/test-models/chains/rigid_body_inertia.h =====

Three functions, each producing a single-link chain (parent=-1, Z-revolute, name varies):

1. inline RobotModel makeRBIDefault()
   - mass=1.0, com=Zero(), inertia=Identity_3x3, name="rbi_default"
   - Doxygen: "Default rigid body inertia: mass=1, COM at origin, identity inertia.
     Baseline for RBI behavior tests."

2. inline RobotModel makeRBIOffsetCOM(double comX = 1.0, double comY = 2.0, double comZ = 3.0)
   - mass=5.0, com=Vector3d(comX, comY, comZ), inertia=Identity_3x3, name="rbi_offset"
   - Doxygen: "@param comX,comY,comZ COM offset components (default 1,2,3).
     Configurable-COM RBI. Mass=5. Tests COM offset effects on apply() and transforms."

3. inline RobotModel makeRBIDiagonalInertia(double mass = 1.0,
       double ix = 1.0, double iy = 2.0, double iz = 3.0)
   - mass from param, com=Zero(), inertia=diag(ix, iy, iz) built as Matrix3d::Zero()
     with diag values at (0,0)=ix, (1,1)=iy, (2,2)=iz
   - name="rbi_diag"
   - Doxygen: "@param mass Link mass. @param ix,iy,iz Inertia diagonal values.
     Configurable diagonal inertia RBI. Tests anisotropic inertia behavior."

===== FILE 3: tests/test-models/chains/articulated_body.h =====

Two functions, each producing a single-link chain (parent=-1, Z-revolute, mass=1, name varies):

1. inline RobotModel makeABIIdentity()
   - mass=1.0, com=Zero(), inertia=Identity_3x3, name="abi_identity"
   - Doxygen: "Identity-parameter ABI baseline. Mass=1, COM=origin, identity inertia.
     Equivalent to RBI identity for reduction tests."

2. inline RobotModel makeABIReduced()
   - mass=1.0, com=Zero(), inertia=Identity_3x3, name="abi_reduced"
   - Doxygen: "ABI configuration matching reduced case (H=0, M=I).
     Tests ABI→RBI reduction behavior. Inertia parameterization same as makeABIIdentity
     for comparison."

===== FILE 4: tests/test-models/chains/spatial_utils.h =====

Three functions:

1. inline RobotModel makeSkewFixture()
   - Single-link chain with non-identity parentToJoint so the transform can be used
     for skew-symmetric matrix construction tests
   - parentToJoint: 90° Z rotation (AngleAxisd(M_PI_2, UnitZ())), zero translation
   - name="skew_fixture"
   - Doxygen: "Fixture with 90-degree Z rotation for skew-symmetric matrix tests."

2. inline RobotModel makeMotionVectorPair()
   - Two-link chain: link0 (parent=-1, Z-revolute, mass=1, COM=[1,0,0], name="mv_link0"),
     link1 (parent=0, Z-revolute, parentToJoint with 1m X translation, mass=1, name="mv_link1")
   - Doxygen: "Two-link chain with offset COM for motion vector cross/dot product tests.
     Link 0 COM at [1,0,0] gives non-zero angular-linear coupling."

3. inline RobotModel makeForceVectorPair()
   - Two-link chain: link0 (parent=-1, Z-revolute, mass=2, COM=[0,1,0], name="fv_link0"),
     link1 (parent=0, Z-revolute, parentToJoint with 1m Y translation, mass=2, name="fv_link1")
   - Doxygen: "Two-link chain for force vector cross/dot product tests.
     Both links have mass=2, COM at [0,1,0]."

For multi-link chains: parent field for serial chain uses i-1 (link0 parent=-1, link1 parent=0).
parentToJoint translation for spacing: use a 4x4 with identity rotation and Vector3d for translation
in the topRightCorner. chain link0 translation is zero (parentToJoint=I), link1 translation is the
inter-link spacing vector.
  </action>
  <acceptance_criteria>
- tests/test-models/chains/lower_triangular.h exists with 3 functions: makeIdentityLT(), makeDiagonalLT(), makeArbitraryLT()
- tests/test-models/chains/rigid_body_inertia.h exists with 3 functions: makeRBIDefault(), makeRBIOffsetCOM(), makeRBIDiagonalInertia()
- tests/test-models/chains/articulated_body.h exists with 2 functions: makeABIIdentity(), makeABIReduced()
- tests/test-models/chains/spatial_utils.h exists with 3 functions: makeSkewFixture(), makeMotionVectorPair(), makeForceVectorPair()
- Multi-link chains have correct parent indexing (i-1 for serial)
- All 11 functions return RobotModel by value, declared inline, Doxygen on all
- Each header includes ONLY robot_model.h and Eigen/Dense
- All in namespace test_models with #pragma once
  </acceptance_criteria>
  <verify>
    <automated>ls tests/test-models/chains/lower_triangular.h tests/test-models/chains/rigid_body_inertia.h tests/test-models/chains/articulated_body.h tests/test-models/chains/spatial_utils.h; for f in tests/test-models/chains/lower_triangular.h tests/test-models/chains/rigid_body_inertia.h tests/test-models/chains/articulated_body.h tests/test-models/chains/spatial_utils.h; do grep -c "SpatialAlgebra" "$f" || true; done — all must be 0</automated>
  </verify>
  <done>Four chains/ headers created: lower_triangular.h (3 funcs), rigid_body_inertia.h (3 funcs), articulated_body.h (2 funcs), spatial_utils.h (3 funcs). Doxygen on all, zero SA includes, correct parent indexing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chains/*.h → robot_model.h | Factory functions construct RobotModel instances. Malformed JointSpec (negative mass, NaN COM, non-homogeneous 4x4) would produce invalid models. Not validated here — adapters validate on build(). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | DoS | chains/ headers | accept | Factory functions are compile-time only — no runtime input validation. Invalid models are caught downstream by adapter (Plan 04). |
| T-20-02 | Tampering | JointSpec.parent field | accept | Wrong parent indices would crash SA solver at runtime. Accept because this is test infrastructure — caller controls model construction. Default chains use -1/0/1 serial pattern which is correct. |
| T-20-03 | DoS | MakeX() with NaN args | accept | If caller passes NaN to parametrized functions (makeRBIOffsetCOM), it propagates through Eigen. Accept — same behavior as handwritten NaN test models in existing tests. |
| T-20-04 | DoS | Memory from large models | accept | constructors only produce small chains (1-3 links). Memory exhaustion not applicable to test models. |
| T-20-SC | Tampering | None | N/A | No external packages added. |
</threat_model>

<verification>
After both tasks complete:
1. Count files in tests/test-models/chains/ — must have exactly 7 headers (from this plan)
2. Verify: grep -rn "SpatialAlgebra" tests/test-models/chains/ — must return zero matches
3. Verify: Every header has #pragma once and namespace test_models
4. Verify: Every function has Doxygen @brief
5. Compile check: each header includes only robot_model.h and Eigen/Dense (no other project headers)
</verification>

<success_criteria>
- 7 chains/ headers created covering simple domains: spatial_vectors, plucker_transforms, rotation, lower_triangular, rigid_body_inertia, articulated_body, spatial_utils
- 19 factory functions total (1+4+3+3+3+2+3) — each returning RobotModel by value with correct JointSpec construction
- All functions declared inline in headers, zero .cpp files
- Doxygen @brief/@details/@return on every function
- Zero SpatialAlgebra includes — all Eigen3 types only
- All functions in namespace test_models
</success_criteria>

<output>
Create .planning/phases/20-test-model-library/20-02-SUMMARY.md when done
</output>
