---
phase: 15-eigen-5-x-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - .github/workflows/ci.yml
  - AGENTS.md
autonomous: true
requirements:
  - CI-01
  - CI-02
user_setup: []
must_haves:
  truths:
    - "CMake version range syntax `find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)` accepts both Eigen 3.4.x and 5.0.1"
    - "Library compiles without errors or warnings under Eigen 5.0.1 on both Ubuntu (g++) and macOS (clang++)"
    - "All 8 CI matrix jobs (2 OS × 2 compilers × 2 Eigen versions) run and pass"
    - "Coverage runs only on ubuntu-latest + g++ with Eigen 3.4"
    - "Eigen 5.x compilation failures do not cancel Eigen 3.4 jobs (fail-fast: false)"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "CMake 3.19 minimum + Eigen 3.4...5 version range syntax"
      contains: "cmake_minimum_required(VERSION 3.19)"
    - path: "CMakeLists.txt"
      provides: "Version range accepts both 3.4.x and 5.x"
      contains: "find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)"
    - path: ".github/workflows/ci.yml"
      provides: "Expanded 8-job matrix with Eigen version dimension"
      contains: "eigen-version"
    - path: ".github/workflows/ci.yml"
      provides: "Eigen 5.x failures do not cancel other jobs"
      contains: "fail-fast: false"
    - path: ".github/workflows/ci.yml"
      provides: "Coverage gated to Eigen 3.4 only"
      contains: "matrix.eigen-version == '3.4'"
  key_links:
    - from: "CMakeLists.txt"
      to: "Eigen3"
      via: "find_package version range syntax"
      pattern: "find_package\\(Eigen3 3\\.4\\.\\.\\.5"
    - from: ".github/workflows/ci.yml"
      to: "CMakeLists.txt"
      via: "cmake configure step with -DEigen3_DIR per version"
      pattern: "Eigen3_DIR"
    - from: ".github/workflows/ci.yml"
      to: "Eigen 5.0.1 source tarball"
      via: "curl download in CI install step"
      pattern: "eigen-5\\.0\\.1\\.tar\\.gz"
---

<objective>
Update the build system and CI pipeline to support and verify Eigen 5.x alongside existing Eigen 3.4.x support. After this plan, the library compiles and passes all tests under both Eigen versions, and the CI matrix runs 8 jobs (2 OS × 2 compilers × 2 Eigen versions) with coverage preserved on the existing ubuntu+g+++Eigen3.4 configuration.

**Purpose:** Ensure forward compatibility with Eigen 5.x while maintaining backward compatibility with Eigen 3.4.x, using explicit version range syntax and expanded CI coverage.
**Output:** Updated CMakeLists.txt with version range syntax, updated CI workflow with 8-job matrix, verified compilation under both Eigen versions, updated AGENTS.md guidance.
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-eigen-5-x-ci/15-CONTEXT.md
@.planning/phases/15-eigen-5-x-ci/15-RESEARCH.md

<interfaces>
<!-- Current CMakeLists.txt Eigen configuration — target for D-01 -->
CMakeLists.txt:1 → `cmake_minimum_required(VERSION 3.10)` — bump to VERSION 3.19
CMakeLists.txt:12 → `find_package(Eigen3 REQUIRED NO_MODULE)` — add version range `3.4...5`

<!-- Current CI workflow structure — target for D-02/D-03/D-05 -->
.github/workflows/ci.yml:12-14 → Current 2D matrix (os × compiler, no eigen-version)
.github/workflows/ci.yml:21-25 → macOS install: `brew install eigen googletest` — replace with source builds
.github/workflows/ci.yml:27-31 → Ubuntu install: `apt install libeigen3-dev libgtest-dev lcov` — add eigen 5.0.1 source build
.github/workflows/ci.yml:34 → Configure: coverage gated on `matrix.os == 'ubuntu-latest' && matrix.compiler == 'g++'` — add `&& matrix.eigen-version == '3.4'`
.github/workflows/ci.yml:42-47 → Coverage upload: gated on `matrix.os == 'ubuntu-latest' && matrix.compiler == 'g++'` — add `&& matrix.eigen-version == '3.4'`

<!-- Eigen version detection macros (RESEARCH.md §Eigen Version Detection) -->
`EIGEN_WORLD_VERSION` = 3 (both 3.4.x and 5.x)
`EIGEN_MAJOR_VERSION` = 4 (Eigen 3.4.x), 5 (Eigen 5.x) — the discriminator
Guard pattern: `#if EIGEN_MAJOR_VERSION >= 5` for Eigen 5.x code paths

<!-- Eigen 5.0.1 tarball URL -->
https://gitlab.com/libeigen/eigen/-/archive/5.0.1/eigen-5.0.1.tar.gz
Eigen 3.4.0 tarball URL:
https://gitlab.com/libeigen/eigen/-/archive/3.4.0/eigen-3.4.0.tar.gz
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CMakeLists.txt with version range syntax and bump minimum CMake version</name>
  <files>CMakeLists.txt, AGENTS.md</files>
  <read_first>
    CMakeLists.txt — full file (lines 1-157) to see current cmake_minimum_required, find_package, and the surrounding context
    AGENTS.md — lines 12-17 (Eigen version mismatch section that must be updated)
    .planning/phases/15-eigen-5-x-ci/15-RESEARCH.md — lines 222-238 (CMake version range change examples)
    .planning/phases/15-eigen-5-x-ci/15-CONTEXT.md — D-01 (locked decision for version range syntax)
  </read_first>
  <action>
    Per D-01: Make two changes to CMakeLists.txt, then update AGENTS.md to reflect the new guidance.

    **Part A — CMakeLists.txt (line 1):**
    Change `cmake_minimum_required(VERSION 3.10)` → `cmake_minimum_required(VERSION 3.19)`
    Reason: CMake's `...` version range syntax requires 3.19+. CI runners all satisfy this.

    **Part B — CMakeLists.txt (line 12):**
    Change `find_package(Eigen3 REQUIRED NO_MODULE)` → `find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)`
    Reason: D-01 — version range `3.4...5` means ">=3.4, <6", accepting both Eigen 3.4.x and 5.x. Eigen 5.x changed its CMake version-compatibility logic, making bare version pins fail. The range syntax avoids this.

    **Part C — AGENTS.md (lines 12-17):**
    Replace the entire "Eigen version mismatch" section (lines 12-17, from the header to the blank line after the second bullet) with new guidance that references the version range syntax as the solution:

    ```
    ### Eigen version compatibility

    CMakeLists.txt uses `find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)` which accepts Eigen 3.4.x through 5.x. Requires CMake 3.19+.
    ```
    Reason: The old workaround (removing version pin or setting Eigen3_DIR) is superseded by the version range syntax in D-01.
  </action>
  <verify>
    <automated>grep -n "cmake_minimum_required\|find_package" CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cmake_minimum_required" CMakeLists.txt` shows `VERSION 3.19`
    - `grep "find_package" CMakeLists.txt | grep Eigen` shows `Eigen3 3.4...5 REQUIRED NO_MODULE`
    - AGENTS.md Eigen section uses `find_package(Eigen3 3.4...5 ...)` text, no longer references `find_package(Eigen3 3.3 REQUIRED)` or the old workaround
    - `cmake -B build` succeeds (verifies CMakeLists.txt syntax — uses whatever Eigen is already installed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify compilation under Eigen 5.x and expand CI matrix to 8 jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml — full file (lines 1-47) to see current matrix and install steps
    .planning/phases/15-eigen-5-x-ci/15-RESEARCH.md — lines 111-163 (CI matrix structure pattern and code example), lines 241-317 (full CI workflow code example)
    .planning/phases/15-eigen-5-x-ci/15-CONTEXT.md — D-02, D-03, D-04, D-05 (all locked decisions for CI expansion)
  </read_first>
  <action>
    Per D-02, D-03, D-04, D-05. Two major parts:

    **Part A — Verify compilation under Eigen 5.x locally (D-04 discovery):**
    (a) Download and build Eigen 5.0.1 from source, installing to a local prefix:
    ```
    curl -sL https://gitlab.com/libeigen/eigen/-/archive/5.0.1/eigen-5.0.1.tar.gz | tar xz
    cmake -B eigen-5.0.1/build -S eigen-5.0.1 -DCMAKE_INSTALL_PREFIX=$HOME/eigen-5.0.1
    cmake --build eigen-5.0.1/build
    cmake --install eigen-5.0.1/build
    ```
    (b) Configure and build the project with Eigen 5.x:
    ```
    cmake -B build-eigen5 -DEigen3_DIR=$HOME/eigen-5.0.1/share/eigen3/cmake
    cmake --build build-eigen5 2>&1
    ```
    (c) If compilation succeeds WITHOUT errors or warnings → no code changes needed. Proceed to Part B.
    (d) If compilation fails with errors related to Eigen 5.x API changes → add `#if EIGEN_MAJOR_VERSION >= 5` guards in the affected header files. The guard pattern is:
    ```cpp
    #include <Eigen/Core>  // defines EIGEN_MAJOR_VERSION
    #if EIGEN_MAJOR_VERSION >= 5
        // Eigen 5.x code path
    #else
        // Eigen 3.4.x code path (existing)
    #endif
    ```
    After adding guards, re-run steps (a)-(b) and verify compilation succeeds.
    (e) Run tests to confirm all pass under Eigen 5.x:
    ```
    cd build-eigen5 && ctest --output-on-failure
    ```
    (f) Document any header files modified in the SUMMARY deviations section.

    **Part B — Expand CI workflow (D-02, D-03, D-05):**
    Rewrite `.github/workflows/ci.yml` with these specific changes:

    (1) Add `fail-fast: false` to `jobs.build.strategy` — prevents Eigen 5.x failures from cancelling Eigen 3.4 jobs.

    (2) Add `eigen-version: [3.4, 5.0]` to `jobs.build.strategy.matrix` — produces 8 jobs (2 OS × 2 compilers × 2 Eigen versions).

    (3) Replace the single "Install dependencies (macOS)" step with two conditional steps:
    - **Eigen 3.4 (macOS)**: Build from source tarball (3.4.0). Install prefix: `$HOME/eigen-3.4.0`. Also `brew install googletest`.
      Gate: `if: runner.os == 'macOS' && matrix.eigen-version == '3.4'`
    - **Eigen 5.0.1 (macOS)**: Build from source tarball (5.0.1). Install prefix: `$HOME/eigen-5.0.1`. Also `brew install googletest`.
      Gate: `if: runner.os == 'macOS' && matrix.eigen-version == '5.0'`
    Reason (D-03): Both versions built from source for determinism, avoiding Homebrew tap ambiguity (Eigen@3.4 may not exist).

    (4) Replace the single "Install dependencies (Ubuntu)" step with two conditional steps:
    - **Eigen 3.4 (Ubuntu)**: `sudo apt-get install -y libeigen3-dev libgtest-dev lcov`
      Gate: `if: runner.os == 'Linux' && matrix.eigen-version == '3.4'`
    - **Eigen 5.0.1 (Ubuntu)**: Build from source tarball (5.0.1). Install prefix: `$HOME/eigen-5.0.1`. Also `sudo apt-get install -y libgtest-dev lcov`.
      Gate: `if: runner.os == 'Linux' && matrix.eigen-version == '5.0'`
    Reason (D-03): Ubuntu apt provides Eigen 3.4.x, but Eigen 5.0.1 must be built from source.

    (5) Modify the "Configure" step:
    - Add a shell variable `EIGEN_DIR_FLAG` that points `-DEigen3_DIR` to the correct install prefix based on `matrix.eigen-version`. For `3.4`: `$HOME/eigen-3.4.0/share/eigen3/cmake`; for `5.0`: `$HOME/eigen-5.0.1/share/eigen3/cmake`.
    - Gate the `-DENABLE_COVERAGE=ON` flag on `matrix.os == 'ubuntu-latest' && matrix.compiler == 'g++' && matrix.eigen-version == '3.4'` (D-05).
    - The cmake command becomes: `cmake -B build -DCMAKE_CXX_COMPILER=${{ matrix.compiler }} $EIGEN_DIR_FLAG $COVERAGE_FLAG`

    (6) Modify the "Upload coverage" step (D-05):
    - Add `&& matrix.eigen-version == '3.4'` to the if-condition.
    - Full gate: `if: matrix.os == 'ubuntu-latest' && matrix.compiler == 'g++' && matrix.eigen-version == '3.4'`
    - Keep `codecov/codecov-action@v4` and the existing input arguments.

    (7) Keep all other steps (checkout, Build, Test) unchanged — they are version-agnostic.

    **Part C — Verify CI YAML validity:**
    Run `cat .github/workflows/ci.yml | python3 -c "import sys,yaml; yaml.safe_load(sys.stdin); print('YAML valid')"` to confirm YAML syntax. If python3 is available, this verifies the workflow parses correctly. If python3 is not available, use `grep -c "eigen-version" .github/workflows/ci.yml` as a basic sanity check.
  </action>
  <verify>
    <automated>test -d $HOME/eigen-5.0.1/share/eigen3/cmake && cmake -B build-eigen5 -DEigen3_DIR=$HOME/eigen-5.0.1/share/eigen3/cmake && cmake --build build-eigen5 && cd build-eigen5 && ctest --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Local Eigen 5.x compilation**: `cmake --build build-eigen5` succeeds with zero errors and zero warnings
    - **All tests pass under Eigen 5.x**: `ctest --output-on-failure` in build-eigen5 exits 0
    - **CI matrix expanded**: `.github/workflows/ci.yml` contains `eigen-version: [3.4, 5.0]` and `fail-fast: false`
    - **8 unique job combinations**: The matrix generates 2 × 2 × 2 = 8 job configurations
    - **Conditional install steps**: Each OS × version combination has a dedicated install step with correct gate
    - **Coverage gated**: Coverage upload conditional includes `matrix.eigen-version == '3.4'`
    - **Both versions point to correct -DEigen3_DIR**: Configure step sets `$HOME/eigen-3.4.0/...` for version `3.4` and `$HOME/eigen-5.0.1/...` for version `5.0`
    - **YAML valid**: `yaml.safe_load(ci.yml)` succeeds (or grep confirms `eigen-version` present as basic check)
    - **If compilation errors occurred and headers were modified**: Each guard is documented in SUMMARY deviations with file:line and the specific Eigen 5.x API difference that triggered it
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build matrix → artifact | Untrusted CI runner executes build steps; CMake resolves Eigen dependency at configure time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-SC | Tampering | Eigen tarball download in CI | accept | Tarball downloaded via HTTPS from gitlab.com (TLS-verified). Eigen is an established library (10+ years) — supply-chain risk accepted. |
</threat_model>

<verification>
- `cmake -B build` succeeds with CMakeLists.txt changes
- `cmake -B build-eigen5 -DEigen3_DIR=$HOME/eigen-5.0.1/share/eigen3/cmake && cmake --build build-eigen5` succeeds with zero errors and zero warnings
- `cd build-eigen5 && ctest --output-on-failure` — all tests pass
- `.github/workflows/ci.yml` parses as valid YAML
- `grep "find_package.*Eigen3" CMakeLists.txt` shows `3.4...5` version range syntax
- `grep "eigen-version" .github/workflows/ci.yml` confirms matrix has the new dimension
- `grep "fail-fast" .github/workflows/ci.yml` shows `false`
- `grep "coverage" .github/workflows/ci.yml` shows gate includes `eigen-version == '3.4'`
</verification>

<success_criteria>
1. CMakeLists.txt uses `cmake_minimum_required(VERSION 3.19)` and `find_package(Eigen3 3.4...5 REQUIRED NO_MODULE)` — per D-01, CI-01
2. CI workflow has `eigen-version: [3.4, 5.0]` matrix dimension, `fail-fast: false`, and 8 job combinations — per D-02, CI-02
3. Eigen 5.0.1 builds from source in CI on both macOS and Ubuntu (D-03); Eigen 3.4 builds from source on macOS, via apt on Ubuntu
4. Coverage upload gated on `eigen-version == '3.4'` — per D-05
5. Library compiles and all tests pass under Eigen 5.0.1 locally — confirms D-04 (no guards needed, or guards added if compilation errors found)
6. AGENTS.md reflects the version range syntax solution instead of the old workaround
</success_criteria>

<output>
Create `.planning/phases/15-eigen-5-x-ci/15-01-SUMMARY.md` when done
</output>
