---
phase: 08-test-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/TestSpatialOperations.cpp, CMakeLists.txt]
autonomous: true
requirements: [TST-01, TST-04]

must_haves:
  truths:
    - "TestSpatialOperations.cpp contains working GTest tests"
    - "All three SpatialOperations methods are tested"
    - "Tests compile and pass"
  artifacts:
    - path: "tests/TestSpatialOperations.cpp"
      provides: "GTest test suite for SpatialOperations class"
      min_lines: 100
    - path: "CMakeLists.txt"
      provides: "Test executable registration"
      contains: "TestSpatialOperations"
  key_links:
    - from: "tests/TestSpatialOperations.cpp"
      to: "include/SpatialOperations.h"
      via: "#include"
      pattern: "#include.*SpatialOperations.h"
    - from: "CMakeLists.txt"
      to: "tests/TestSpatialOperations.cpp"
      via: "add_executable"
      pattern: "add_executable\\(TestSpatialOperations"
---

<objective>
Implement GTest test suite for empty TestSpatialOperations.cpp file and register it in CMakeLists.txt

Purpose: TST-01 requires all empty test stub files to be implemented with GTest. TestSpatialOperations.cpp is the only remaining empty test file (0 lines).
Output: Working test file with comprehensive tests for SpatialOperations::crossProductMotion, crossProductForce, and transformInertia
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/TestSpatialUtils.cpp
@include/SpatialOperations.h
@include/SpatialVector.h
@include/PluckerTransform.h
@include/RigidBodyInertia.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TestSpatialOperations.cpp with GTest</name>
  <files>tests/TestSpatialOperations.cpp</files>
  <action>
    Create comprehensive GTest test suite for SpatialOperations class with three test groups:
    
    1. CrossProductMotion tests (2-3 tests):
       - Test with simple rotation vectors
       - Verify result matches expected cross product formula
       - Property test for anti-commutativity
    
    2. CrossProductForce tests (2-3 tests):
       - Test with simple force/torque vectors
       - Verify result matches expected formula
       - Property test for anti-commutativity
    
    3. TransformInertia tests (2-3 tests):
       - Test with identity transform (should preserve inertia)
       - Test with rotation transform (should rotate inertia)
       - Test with translation transform (should shift COM)
    
    Follow existing test patterns from TestSpatialUtils.cpp (which already tests these).
    Use same test structure: TEST(TestSpatialOperations, TestName) with Arrange-Act-Assert pattern.
    Include main() function for GTest runner.
  </action>
  <verify>
    <automated>cmake --build build && build/TestSpatialOperations</automated>
  </verify>
  <done>TestSpatialOperations.cpp has 100+ lines with 6+ tests covering all three SpatialOperations methods, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Register TestSpatialOperations in CMakeLists.txt</name>
  <files>CMakeLists.txt</files>
  <action>
    Add TestSpatialOperations executable to CMakeLists.txt following the existing pattern:
    
    1. Add add_executable(TestSpatialOperations tests/TestSpatialOperations.cpp) after line 94
    2. Add target_link_libraries(TestSpatialOperations SpatialAlgebra GTest::GTest GTest::Main)
    3. Add add_test(NAME TestSpatialOperations COMMAND TestSpatialOperations)
    
    Follow the exact same pattern as TestArticulatedBodyInertia (lines 68-93).
  </action>
  <verify>
    <automated>cmake -B build && cmake --build build 2>&1 | grep -i "TestSpatialOperations"</automated>
  </verify>
  <done>CMakeLists.txt contains TestSpatialOperations executable definition, libraries, and CTest registration; build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Library | Test code calls library functions — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Test assertions | accept | Tests verify library behavior; no security impact |
| T-08-02 | Information disclosure | Test output | accept | Test output contains no sensitive data |
</threat_model>

<verification>
- [ ] cmake --build build succeeds without errors
- [ ] build/TestSpatialOperations runs and all tests pass
- [ ] ctest -R TestSpatialOperations passes
- [ ] TestSpatialOperations.cpp has tests for all three SpatialOperations methods
</verification>

<success_criteria>
- TestSpatialOperations.cpp is no longer empty (100+ lines)
- All three SpatialOperations static methods have test coverage
- Tests compile and link successfully
- All tests pass when executed
- CMakeLists.txt properly registers the test executable
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-infra/08-01-SUMMARY.md`
</output>
