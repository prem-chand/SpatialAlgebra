---
phase: 08-test-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/TestPluckerTransform.cpp]
autonomous: true
requirements: [TST-06]

must_haves:
  truths:
    - "PluckerTransform::inverse() method is tested"
    - "PluckerTransform::multiply() method is tested"
    - "PluckerTransform::apply(PluckerTransform) method is tested"
    - "PluckerTransform::print() method is tested"
  artifacts:
    - path: "tests/TestPluckerTransform.cpp"
      provides: "Additional tests for untested PluckerTransform methods"
      contains: "inverse(), multiply(), apply(), print()"
  key_links:
    - from: "tests/TestPluckerTransform.cpp"
      to: "include/PluckerTransform.h"
      via: "#include"
      pattern: "#include.*PluckerTransform.h"
---

<objective>
Add missing tests for PluckerTransform methods: inverse(), multiply(), apply(PluckerTransform), and print()

Purpose: TST-06 requires all PluckerTransform methods to have tests. Currently transformMotion/Force, inverseTransformMotion/Force, tformRBI/ABI, invtformRBI/ABI are tested, but inverse(), multiply(), apply(), and print() are not.
Output: Extended TestPluckerTransform.cpp with 4+ new test cases
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@tests/TestPluckerTransform.cpp
@include/PluckerTransform.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for PluckerTransform::inverse() method</name>
  <files>tests/TestPluckerTransform.cpp</files>
  <action>
    Add 2-3 tests for the inverse() method:
    
    1. Test inverse produces identity when multiplied:
       - Create transform with rotation + translation
       - Compute inverse: X_inv = X.inverse()
       - Verify X * X_inv ≈ identity (check rotation ≈ I, translation ≈ 0)
    
    2. Test double inverse returns original:
       - Create transform X
       - Compute X_double_inv = X.inverse().inverse()
       - Verify X_double_inv has same rotation and translation as X
    
    3. Test inverse of identity is identity:
       - Create identity transform
       - Verify inverse() returns identity
  </action>
  <verify>
    <automated>cmake --build build && build/TestPluckerTransform --gtest_filter="*Inverse*"</automated>
  </verify>
  <done>TestPluckerTransform.cpp has 2-3 new tests for inverse() method, all pass</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for PluckerTransform::multiply() and apply(PluckerTransform)</name>
  <files>tests/TestPluckerTransform.cpp</files>
  <action>
    Add 2-3 tests for composition methods:
    
    1. Test multiply() composes transforms:
       - Create X1 (rotation Z 90°) and X2 (translation [1,0,0])
       - Compute X_combined = X1.multiply(X2)
       - Apply to test motion vector
       - Verify result matches sequential application: X1.transformMotion(X2.transformMotion(v))
    
    2. Test apply(PluckerTransform) is alias for multiply:
       - Create X1 and X2
       - Verify X1.multiply(X2) == X1.apply(X2)
    
    3. Test associativity:
       - Create X1, X2, X3
       - Verify (X1 * X2) * X3 == X1 * (X2 * X3)
  </action>
  <verify>
    <automated>cmake --build build && build/TestPluckerTransform --gtest_filter="*Multiply*"</automated>
  </verify>
  <done>TestPluckerTransform.cpp has 2-3 new tests for multiply/apply methods, all pass</done>
</task>

<task type="auto">
  <name>Task 3: Add test for PluckerTransform::print() method</name>
  <files>tests/TestPluckerTransform.cpp</files>
  <action>
    Add 1 test for print() method:
    
    1. Test print produces output:
       - Create transform with known rotation and translation
       - Capture std::cout output using testing::internal::CaptureStdout()
       - Call print()
       - Verify output contains expected values (rotation matrix elements, translation values)
       - Use GetCapturedStdout() to retrieve and assert
    
    Alternative simpler approach:
    - Just call print() to ensure it doesn't crash
    - Add comment that visual verification may be needed
  </action>
  <verify>
    <automated>cmake --build build && build/TestPluckerTransform --gtest_filter="*Print*"</automated>
  </verify>
  <done>TestPluckerTransform.cpp has 1 test for print() method that verifies it executes without crashing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Library | Test code calls library functions — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | Test assertions | accept | Tests verify library behavior; no security impact |
| T-08-04 | Information disclosure | Test output | accept | Test output contains no sensitive data |
</threat_model>

<verification>
- [ ] cmake --build build succeeds
- [ ] build/TestPluckerTransform runs all tests including new ones
- [ ] All new tests pass
- [ ] inverse() method has test coverage
- [ ] multiply() method has test coverage
- [ ] apply(PluckerTransform) method has test coverage
- [ ] print() method has test coverage
</verification>

<success_criteria>
- PluckerTransform::inverse() is tested with 2+ tests
- PluckerTransform::multiply() is tested with 2+ tests
- PluckerTransform::apply(PluckerTransform) is tested
- PluckerTransform::print() is tested (at minimum doesn't crash)
- All existing tests continue to pass
- TestPluckerTransform.cpp has 35+ total tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-infra/08-02-SUMMARY.md`
</output>
