---
phase: 18-robot-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/example_robot_2link.cpp
  - examples/example_robot_3link.cpp
  - examples/CMakeLists.txt
autonomous: true
requirements: [EX-01, EX-02]
user_setup: []

must_haves:
  truths:
    - "User can compile both example executables with cmake --build build (no linker errors)"
    - "Running build/example_robot_2link prints sectioned output showing FD accelerations, ID gravity torques, and FD+ID cross-validation with residual |qddot| < 1e-10"
    - "Running build/example_robot_3link prints sectioned output showing FD accelerations, ID gravity torques, and FD+ID cross-validation with residual |qddot| < 1e-10"
    - "The 2-link Z-Z arm output explains that both joints rotate about Z and gravity acts parallel to both joint axes → zero gravity torque"
    - "The 3-link Z-Y-Z arm output shows non-zero gravity torque (~30 N·m on joint 2 shoulder pitch) when arm is horizontal"
    - "Both executables produce physical interpretation commentary after each computation section"
  artifacts:
    - path: "examples/example_robot_2link.cpp"
      provides: "Z-Z planar arm dynamics example with UR5-derived parameters"
      min_lines: 180
    - path: "examples/example_robot_3link.cpp"
      provides: "Z-Y-Z spatial RRR arm dynamics example with UR5-derived parameters"
      min_lines: 220
    - path: "examples/CMakeLists.txt"
      provides: "Build target registration for both new executables"
      contains: "example_robot_2link"
  key_links:
    - from: "examples/example_robot_2link.cpp"
      to: "ForwardDynamics.h"
      via: "#include"
      pattern: "#include \"ForwardDynamics.h\""
    - from: "examples/example_robot_3link.cpp"
      to: "InverseDynamics.h"
      via: "#include"
      pattern: "#include \"InverseDynamics.h\""
    - from: "examples/CMakeLists.txt"
      to: "examples/example_robot_2link.cpp"
      via: "add_executable"
      pattern: "add_executable\\(example_robot_2link"
    - from: "examples/CMakeLists.txt"
      to: "examples/example_robot_3link.cpp"
      via: "add_executable"
      pattern: "add_executable\\(example_robot_3link"
---

<objective>
Create two self-contained example executables demonstrating real-world robot dynamics with gravity cross-validation.

**Purpose:** Show the SpatialAlgebra library being used with realistic UR5-derived robot parameters. Both examples demonstrate forward dynamics (ABA via `ForwardDynamics::computeAccelerations`) and inverse dynamics (RNEA via `InverseDynamics::computeTorques`) with a FD+ID cross-validation pattern that verifies solver consistency. The output includes physical interpretation of results.

**Output:** 
- `examples/example_robot_2link.cpp` — 2-link Z-Z planar arm (demos FD, ID with gravity, cross-validation per D-09)
- `examples/example_robot_3link.cpp` — 3-link Z-Y-Z spatial RRR arm (demos FD, ID with gravity, cross-validation per D-09)
- `examples/CMakeLists.txt` — updated with two new `add_executable` entries
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-robot-examples/18-CONTEXT.md (locked decisions D-01 through D-12)
@.planning/phases/18-robot-examples/18-RESEARCH.md (UR5 parameters, physics analysis, pitfalls, API signatures)

<interfaces>
Key types and API signatures the executor needs. Read the actual header files for full signatures, but these are the critical contracts from the existing source.

From include/ForwardDynamics.h (lines 79-114):
- `struct Link` (aka `ForwardDynamicsLink`): parent, X (PluckerTransform), I (RigidBodyInertia), S (MotionVector), q/qdot/qddot (double), v/c (MotionVector), f (ForceVector), Ia (ArticulatedBodyInertia), pa (ForceVector)
- `class ForwardDynamics`: `std::vector<Link> links`, `computeAccelerations(const VectorXd& tau, const Vector3d& gravity = Vector3d::Zero())`

From include/InverseDynamics.h (lines 75-100):
- `struct InverseDynamicsLink`: parent, X, I, S, q/qdot/qddot, v (MotionVector), a (MotionVector)
- `class InverseDynamics`: `std::vector<InverseDynamicsLink> links`, `computeTorques(const VectorXd& qddot, const Vector3d& gravity = Vector3d::Zero())` returns `VectorXd`

From include/RigidBodyInertia.h (line 41):
- `RigidBodyInertia(double mass, const Vector3d& com, const lt& inertiaMatrixLT)`

From include/PluckerTransform.h (line 91):
- `PluckerTransform(const Rotation& rotation, const Vector3d& translation)`

From include/MotionVector.h (line 86):
- `MotionVector(const Vector3d& angular, const Vector3d& linear)`

From include/LowerTriangular.h:
- `lt(int size)` — zero-initialized packed matrix, access via `operator()(i,j)` where i>=j
- `lt::Identity(int size)` — identity matrix

Existing example patterns (read the full files before implementing):
- `examples/dynamics.cpp` — 2-link ABA example showing Link setup, solver usage, output pattern
- `examples/basic_vectors.cpp` — Verbose output pattern with section headers
- `examples/CMakeLists.txt` — `add_executable` + `target_link_libraries` pattern

Import path for examples: `#include "ForwardDynamics.h"` works because root CMakeLists.txt uses `include_directories(include)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 2-link Z-Z planar arm example (example_robot_2link.cpp)</name>

  <files>examples/example_robot_2link.cpp</files>

  <read_first>
    examples/dynamics.cpp         — File structure, includes, Link setup, solver usage, output pattern
    examples/basic_vectors.cpp    — Doxygen file comment pattern, type alias style, verbose output
    include/ForwardDynamics.h     — Link struct definition, computeAccelerations API
    include/InverseDynamics.h     — InverseDynamicsLink, computeTorques API
    include/RigidBodyInertia.h    — Constructor signature for inertia
    include/PluckerTransform.h    — Constructor, Rotation pattern
    include/MotionVector.h        — Joint screw axis construction
    include/LowerTriangular.h     — Packed inertia tensor construction
    18-RESEARCH.md §"Concrete Parameter Tables" — UR5 parameter values for 2-link arm
    18-RESEARCH.md §"Gravity Cross-Validation Math" — Physics analysis (Z-Z arm has zero gravity torque)
  </read_first>

  <action>
    Create examples/example_robot_2link.cpp following D-05 (self-contained), D-06 (filename), D-08 (verbose output), D-09 (five-section structure), D-10 (Z-Z joint config), D-12 (cross-validation sign convention).

    File structure (matching existing example pattern):
    1. Doxygen block comment at top describing the example: file brief, what it demonstrates, robot configuration (2-link Z-Z planar arm operating in XY plane, gravity [0,0,-9.81]).
    2. Includes: "ForwardDynamics.h", "InverseDynamics.h", "RigidBodyInertia.h", "PluckerTransform.h", "Rotation.h", "LowerTriangular.h", &lt;iostream&gt;, &lt;iomanip&gt;, &lt;Eigen/Dense&gt;
    3. `using namespace SpatialAlgebra;` then type aliases: `using lt = LowerTriangular; using mv = MotionVector; using fv = ForceVector; using plux = PluckerTransform; using rbi = RigidBodyInertia;`
    4. `int main()` — set `std::cout &lt;&lt; std::fixed &lt;&lt; std::setprecision(12);`

    Section 1 — Model Setup (per D-09 item 1):
    Build `ForwardDynamics` and `InverseDynamics` solvers separately (per Pitfall 2 from RESEARCH.md — Link vs InverseDynamicsLink are distinct structs).
    
    For Link 0 (UR5 upper arm adapted):
    - parent=-1, mass=8.393, COM=[0.2125, 0, 0.02], inertia lower-triangular elements: Ixx=0.0149, Ixy=0, Ixz=0, Iyy=0.3564, Iyz=0, Izz=0.3553
    - X=PluckerTransform(Rotation identity, [0,0,0])
    - S=MotionVector([0,0,1], [0,0,0]) — Z-axis revolute
    - q=0.0, qdot=0.0

    For Link 1 (UR5 forearm adapted):
    - parent=0, mass=2.33, COM=[0.15, 0, 0.02], inertia elements: Ixx=0.0025, Ixy=0, Ixz=0, Iyy=0.0551, Iyz=0, Izz=0.0546
    - X=PluckerTransform(Rotation identity, [0.425, 0, 0]) — translation along X at link length
    - S=MotionVector([0,0,1], [0,0,0]) — Z-axis revolute
    - q=0.0, qdot=0.0

    Build `fd.links` vector of `Link` and `id.links` vector of `InverseDynamicsLink` with IDENTICAL parameter values. Print each link's parameters with explanatory text (mass, COM, joint axis, transform).

    Section 2 — Forward Dynamics (per D-09 item 2):
    Apply torques tau=[10.0, 5.0] N·m with zero gravity: `fd.computeAccelerations(tau)`. Print joint accelerations with labels and units (rad/s²). Add physical interpretation: coupling between joints, which accelerates more and why.

    Apply torques tau=[10.0, -5.0] N·m as a second FD test case. Print and interpret.

    Section 3 — Inverse Dynamics Gravity Torques (per D-09 item 3):
    Use static pose q=[0,0], qdot=[0,0], qddot=[0,0] with gravity Vector3d(0, 0, -9.81) per D-02.
    Compute: `Eigen::VectorXd tau_g = id.computeTorques(VectorXd::Zero(2), Vector3d(0, 0, -9.81))`.
    Print gravity compensation torques.

    Key physics explanation (from RESEARCH.md §"2-Link Z-Z Arm: Zero Gravity Torque"): Both joint axes are Z (parallel to gravity). The cross product r × (m·g) has zero component along Z. Therefore gravity compensation torques are exactly [0, 0]. This is physically correct for a horizontal SCARA-like configuration.

    Repeat with q=[M_PI/4, M_PI/3] to verify gravity torque remains zero regardless of pose.

    Section 4 — Cross-Validation (per D-09 item 4, D-12):
    `fd.computeAccelerations(tau_g, Vector3d(0, 0, -9.81))`
    Print `fd.links[i].qddot` for i=0,1. Note that qddot ≈ [0, 0] within ~1e-12.
    Check with explicit if-print: "Cross-validation: |qddot| &lt; 1e-10 → PASS".

    Section 5 — Summary (per D-09 item 5):
    Explain what was demonstrated: FD computes qddot from tau, ID computes tau_g for gravity compensation at static pose, cross-validation confirms solver consistency. Note that Z-Z arm gravity torque is zero as expected.

    Output format requirements per D-08:
    - Section headers: `=== Title ===` (matching existing examples)
    - Explanatory text before each computation describing what's being computed and why
    - Print all intermediate results with units (rad/s² for acceleration, N·m for torques)
    - Physical interpretation after each computation
    - Summary at end
    - `std::cout &lt;&lt; std::fixed &lt;&lt; std::setprecision(12);` at start for readable output
    - Return 0 from main
  </action>

  <verify>
    <automated>test -f examples/example_robot_2link.cpp && wc -l examples/example_robot_2link.cpp && grep -c "ForwardDynamics" examples/example_robot_2link.cpp && grep -c "InverseDynamics" examples/example_robot_2link.cpp && grep -c "computeAccelerations" examples/example_robot_2link.cpp && grep -c "computeTorques" examples/example_robot_2link.cpp && grep -c "8.393" examples/example_robot_2link.cpp && grep -c "0.0149" examples/example_robot_2link.cpp</automated>
  </verify>

  <acceptance_criteria>
    - File exists at examples/example_robot_2link.cpp with ≥180 lines
    - Includes "ForwardDynamics.h" and "InverseDynamics.h"
    - Contains both `computeAccelerations` and `computeTorques` calls
    - UR5-derived parameters present: mass=8.393 in link 0, inertia Ixx=0.0149
    - Cross-validation pattern present with gravity vector [0,0,-9.81]
    - Model setup builds BOTH ForwardDynamics and InverseDynamics solvers with identical parameters
  </acceptance_criteria>
  <done>Self-contained 2-link Z-Z planar arm example written with UR5 parameters, includes FD and ID solver setup, cross-validation loop, and physical interpretation commentary</done>
</task>

<task type="auto">
  <name>Task 2: Create 3-link Z-Y-Z spatial RRR arm example (example_robot_3link.cpp)</name>

  <files>examples/example_robot_3link.cpp</files>

  <read_first>
    examples/dynamics.cpp         — File structure, includes, Link setup, solver usage, output pattern
    examples/basic_vectors.cpp    — Doxygen file comment pattern, type alias style, verbose output
    include/ForwardDynamics.h     — Link struct definition, computeAccelerations API
    include/InverseDynamics.h     — InverseDynamicsLink, computeTorques API
    include/RigidBodyInertia.h    — Constructor signature for inertia
    include/PluckerTransform.h    — Constructor, Rotation pattern
    include/MotionVector.h        — Joint screw axis construction
    include/LowerTriangular.h     — Packed inertia tensor construction
    18-RESEARCH.md §"Concrete Parameter Tables" — UR5 parameters for 3-link Z-Y-Z arm
    18-RESEARCH.md §"Gravity Cross-Validation Math" — Expected ~30.7 N·m on joint 2
    18-RESEARCH.md §"Anti-Patterns to Avoid" — Separate Link vs InverseDynamicsLink structs
  </read_first>

  <action>
    Create examples/example_robot_3link.cpp following D-05 (self-contained), D-06 (filename), D-08 (verbose output), D-09 (five-section structure), D-11 (Z-Y-Z joint config), D-12 (cross-validation sign convention).

    File structure (matching existing example pattern):
    1. Doxygen block comment at top: describes a 3-link Z-Y-Z spatial RRR arm (waist yaw / shoulder pitch / elbow roll) with UR5-like parameters, demonstrating FD, ID with gravity, and cross-validation.
    2. Includes, using declarations, type aliases — identical pattern to Task 1.
    3. `int main()` — set fixed precision.

    Section 1 — Model Setup:
    Build BOTH `ForwardDynamics` fd and `InverseDynamics` id with identical structure.

    Link 0 (UR5 shoulder, waist yaw):
    - parent=-1, mass=3.7, COM=[0, -0.02561, 0.00193]
    - Inertia elements: Ixx=0.0067, Ixy=0, Ixz=0, Iyy=0.0064, Iyz=0, Izz=0.0067
    - X=PluckerTransform(Rotation identity, [0,0,0])
    - S=MotionVector([0,0,1], [0,0,0]) — Z axis (waist yaw)
    - q=0.0, qdot=0.0

    Link 1 (UR5 upper arm, shoulder pitch):
    - parent=0, mass=8.393, COM=[0.2125, 0, 0.11336]
    - Inertia elements: Ixx=0.0149, Ixy=0, Ixz=0, Iyy=0.3564, Iyz=0, Izz=0.3553
    - X=PluckerTransform(Rotation identity, [0, 0, 0.089]) — translation along Z (UR5 shoulder height d1)
    - S=MotionVector([0,1,0], [0,0,0]) — Y axis (shoulder pitch per D-11)
    - q=0.0, qdot=0.0

    Link 2 (UR5 forearm, elbow roll):
    - parent=1, mass=2.33, COM=[0.15, 0, 0.0265]
    - Inertia elements: Ixx=0.0025, Ixy=0, Ixz=0.0034, Iyy=0.0551, Iyz=0, Izz=0.0546
    - X=PluckerTransform(Rotation identity, [0.425, 0, 0]) — translation along X (UR5 upper arm length a2)
    - S=MotionVector([0,0,1], [0,0,0]) — Z axis (elbow roll per D-11)
    - q=0.0, qdot=0.0

    Print each link's parameters with explanatory text.

    Section 2 — Forward Dynamics (per D-09 item 2):
    Apply torques tau=[20.0, 10.0, 5.0] N·m with zero gravity. Print joint accelerations. Add interpretation.

    Apply torques tau=[20.0, -10.0, 5.0] N·m as second FD test. Print and interpret.

    Section 3 — Inverse Dynamics Gravity Torques (per D-09 item 3):
    Static pose q=[0,0,0] (arm extended horizontally), qdot=[0,0,0], qddot=[0,0,0].
    gravity = Vector3d(0, 0, -9.81).
    `Eigen::VectorXd tau_g = id.computeTorques(VectorXd::Zero(3), gravity)`.

    Print gravity compensation torques with physical interpretation from RESEARCH.md:
    - Joint 1 (Z waist yaw): τ≈0 N·m (gravity parallel to joint axis → zero torque)
    - Joint 2 (Y shoulder pitch): τ≈30.7 N·m (gravity perpendicular to moment arm → non-zero torque = m₂·g·x₂ + m₃·g·(L₂+x₃) ≈ 8.393×9.81×0.2125 + 2.33×9.81×0.575 ≈ 30.7)
    - Joint 3 (Z elbow roll): τ≈0 N·m (gravity parallel to joint axis → zero torque)

    Repeat with q=[0, M_PI/4, 0] (arm at 45° angle). Show gravity torque is reduced on joint 2.

    Section 4 — Cross-Validation (per D-09 item 4, D-12):
    `fd.computeAccelerations(tau_g, gravity)`
    Print each qddot. Should be ≈ [0, 0, 0] within 1e-12.
    Check with explicit if-print: "Cross-validation: all |qddot| &lt; 1e-10 → PASS".

    Section 5 — Summary (per D-09 item 5):
    Summarize all three demonstrations. Explain that the Z-Y-Z arm is a standard anthropomorphic configuration where only the Y-axis joint (shoulder pitch) bears gravity load, and the cross-validation confirms solver consistency.

    Output format per D-08: same as Task 1 (=== Title === headers, explanatory text, intermediate results with units, physical interpretation, summary, fixed 12-digit precision).
  </action>

  <verify>
    <automated>test -f examples/example_robot_3link.cpp && wc -l examples/example_robot_3link.cpp && grep -c "ForwardDynamics" examples/example_robot_3link.cpp && grep -c "InverseDynamics" examples/example_robot_3link.cpp && grep -c "computeAccelerations" examples/example_robot_3link.cpp && grep -c "computeTorques" examples/example_robot_3link.cpp && grep -c "0.089" examples/example_robot_3link.cpp && grep -c "0.0034" examples/example_robot_3link.cpp</automated>
  </verify>

  <acceptance_criteria>
    - File exists at examples/example_robot_3link.cpp with ≥220 lines
    - Includes "ForwardDynamics.h" and "InverseDynamics.h"
    - Contains both FD and ID solver usage
    - Three links with correct: Z axis on link 0, Y axis on link 1, Z axis on link 2 per D-11
    - UR5 parameters: link 0 mass=3.7, link 1 mass=8.393, link 2 Ixz=0.0034
    - Transform translations: link 1 at [0,0,0.089], link 2 at [0.425,0,0]
    - Cross-validation: computeTorques returns tau → computeAccelerations(tau, gravity)
    - Physics explanation of joint 2 gravity torque
  </acceptance_criteria>
  <done>Self-contained 3-link Z-Y-Z spatial arm example written with UR5 parameters, Z-Y-Z joint axis configuration, FD and ID solver usage, cross-validation, and joint 2 gravity torque physics explanation</done>
</task>

<task type="auto">
  <name>Task 3: Register both examples in CMakeLists.txt, build, and verify runtime correctness</name>

  <files>examples/CMakeLists.txt</files>

  <read_first>
    examples/CMakeLists.txt  — Current content with 4 existing targets
  </read_first>

  <action>
    Append two new executable targets to examples/CMakeLists.txt, matching the existing `add_executable` + `target_link_libraries` pattern (per D-07):

    ```
    # Robot example: 2-link Z-Z planar arm
    add_executable(example_robot_2link example_robot_2link.cpp)
    target_link_libraries(example_robot_2link SpatialAlgebra Eigen3::Eigen)

    # Robot example: 3-link Z-Y-Z spatial arm
    add_executable(example_robot_3link example_robot_3link.cpp)
    target_link_libraries(example_robot_3link SpatialAlgebra Eigen3::Eigen)
    ```

    Then execute the build and runtime verification:
    1. `cmake -B build` — rerun cmake configuration (detects new source files via add_executable).
    2. `cmake --build build` — compile both new executables. Must succeed with no errors.
    3. Run both executables and check return code:
       - `build/example_robot_2link` — prints output to stdout, returns 0 on success
       - `build/example_robot_3link` — prints output to stdout, returns 0 on success
  </action>

  <verify>
    <automated>cmake -B build && cmake --build build && build/example_robot_2link && build/example_robot_3link && echo "ALL EXAMPLES PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - examples/CMakeLists.txt contains both `example_robot_2link` and `example_robot_3link` add_executable blocks
    - Both targets link `SpatialAlgebra Eigen3::Eigen`
    - `cmake -B build` succeeds (zero exit code)
    - `cmake --build build` produces both executables with zero errors
    - `build/example_robot_2link` runs to completion and returns 0
    - `build/example_robot_3link` runs to completion and returns 0
  </acceptance_criteria>
  <done>CMakeLists.txt updated with both new targets, build succeeds, both executables run to completion and return exit code 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries exist in this phase. Both example executables run locally with no user input, no network access, and no persistent state. They output text to stdout and exit.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | cmake/native toolchain | accept | Examples use only the existing built library. No package installs, no new dependencies. Build system is locally controlled. |
</threat_model>

<verification>
### Automated

1. File existence and content: `test -f examples/example_robot_2link.cpp && test -f examples/example_robot_3link.cpp`
2. Build: `cmake -B build && cmake --build build` (no errors, no warnings)
3. Runtime: `build/example_robot_2link` (returns 0), `build/example_robot_3link` (returns 0)

### Human (post-execution)

The executor should present the output of both executables to the user for inspection:
- Output should include === Title === section headers matching existing example style
- Physical interpretation text should follow each computation
- Cross-validation qddot values should be ≈ 0 (1e-12 to 1e-10 precision)
- Z-Z arm should show zero gravity torque with explanation
- Z-Y-Z arm should show ~30 N·m on joint 2 with explanation
</verification>

<success_criteria>
- [ ] `build/example_robot_2link` exists and returns 0 when run
- [ ] `build/example_robot_3link` exists and returns 0 when run
- [ ] Both executables print cross-validation results with |qddot| < 1e-10
- [ ] All existing library tests still pass (no regressions from CMake changes): `cd build && ctest --output-on-failure`
- [ ] User has inspected output and confirmed physical interpretation is correct
</success_criteria>

<output>
Create `.planning/phases/18-robot-examples/18-01-SUMMARY.md` when done.
</output>
