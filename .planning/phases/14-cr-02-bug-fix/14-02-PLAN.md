---
phase: 14-cr-02-bug-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/TestDynamicsConsistency.cpp
  - tests/TestForwardDynamics.cpp
autonomous: true
requirements: [BFIX-01]
user_setup: []
gap_closure: true
gaps_addressed:
  - "ROADMAP SC1: No non-zero COM multi-link consistency tests exist"
  - "ROADMAP SC2: ThreeLinkNumericalValidation not updated for non-zero COM round-trip"

must_haves:
  truths:
    - "ThreeLinkSerialChainNonZeroCOM test exists and FAILS with current 3-phase inwardPass()"
    - "BranchingYNonZeroCOM test exists and FAILS with current 3-phase inwardPass()"
    - "TwoLinkGravityNonZeroCOM test exists and FAILS with current 3-phase inwardPass()"
    - "ThreeLinkNumericalValidation updated with non-zero COM round-trip — FAILS with current code"
    - "All 7 existing consistency tests still pass (zero regression from new test code)"
    - "All 14 existing ForwardDynamics tests still pass (ThreeLinkNumericalValidation is the 15th and now FAILS)"
  artifacts:
    - path: tests/TestDynamicsConsistency.cpp
      provides: "Three new non-zero COM round-trip tests: ThreeLinkSerialChainNonZeroCOM, BranchingYNonZeroCOM, TwoLinkGravityNonZeroCOM"
      min_lines: 750
    - path: tests/TestForwardDynamics.cpp
      provides: "ThreeLinkNumericalValidation updated with non-zero COM and round-trip ID→FD check"
      min_lines: 800
  key_links:
    - from: "ThreeLinkSerialChainNonZeroCOM"
      to: "InverseDynamics::computeTorques"
      via: "Round-trip: ID(qddot_input=[1.0,0.5,0.25]) → tau → FD(tau) → EXPECT_NEAR(qddot_output, qddot_input, 1e-8)"
      pattern: "computeTorques.*computeAccelerations"
    - from: "BranchingYNonZeroCOM"
      to: "InverseDynamics::computeTorques"
      via: "Branching Y round-trip + symmetric branch check expectations"
      pattern: "computeTorques.*computeAccelerations"
    - from: "ThreeLinkNumericalValidation"
      to: "InverseDynamics::computeTorques"
      via: "Round-trip: FD(tau) → qddot_output → ID(qddot_output) → tau' → FD(tau') → EXPECT_NEAR(qddot_roundtrip, qddot_output, 1e-8)"
      pattern: "computeTorques.*computeAccelerations"
---

<objective>
Add non-zero COM multi-link consistency tests that exercise the currently-hidden ABA inward pass bug. These tests MUST FAIL with the current 3-phase inwardPass() code, proving the Phase 3 correction bug documented in VERIFICATION.md SC1 and SC2. The tests serve as the RED phase of TDD — once Plan 14-03 restructures inwardPass(), these same tests will turn GREEN.

**Purpose:** All existing multi-link tests use `RigidBodyInertia(mass, Vector3d::Zero(), ...)` — zero COM makes the inertia matrix block-diagonal, masking the Phase 3 correction double-counting. Non-zero COM creates non-block-diagonal Ia matrices via `skew(com)*mass` coupling terms, which expose the bug. These tests are the missing evidence the re-verification report requires.

**Output:** Three new failing tests in TestDynamicsConsistency.cpp and one updated failing test in TestForwardDynamics.cpp. All existing tests continue to pass — only the new/updated tests fail. Build succeeds.
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-cr-02-bug-fix/14-CONTEXT.md
@.planning/phases/14-cr-02-bug-fix/14-VERIFICATION.md
@.planning/phases/14-cr-02-bug-fix/14-PATTERNS.md
@.planning/phases/14-cr-02-bug-fix/14-RESEARCH.md

<interfaces>
<!-- Key contracts for test construction. No codebase exploration needed. -->

**From RigidBodyInertia.h:**
- `RigidBodyInertia(double mass, Vector3d com, lt inertiaLT)` — constructor accepting com offset

**From InverseDynamics.h:**
- `InverseDynamicsLink` struct: fields parent(int), X(PluckerTransform), I(RigidBodyInertia), S(MotionVector), q(double), qdot(double)
- `id.computeTorques(Eigen::VectorXd qddot, Vector3d gravity = Zero()) -> Eigen::VectorXd` — RNEA for ID→FD round-trip

**From ForwardDynamics.h:**
- `Link` (aka `ForwardDynamicsLink`): fields parent, X, I, S, q, qdot, f(ForceVector), qddot(output)
- `fd.computeAccelerations(Eigen::VectorXd tau, Vector3d gravity = Zero())` — ABA for FD side of round-trip

**From PluckerTransform.h:**
- `PluckerTransform(Rotation, Vector3d translation)` — construct with identity rotation + offset

**From MotionVector.h:**
- `MotionVector(Vector3d angular, Vector3d linear)` — construct Z revolute joint: `MotionVector(Vector3d(0,0,1), Vector3d::Zero())`

**From LowerTriangular.h:**
- `lt::Identity(3)` — 3×3 identity in LT format for inertia matrix
- `lt operator*(double)` — scale inertia for branching children

**Existing test patterns in TestDynamicsConsistency.cpp:**
- `TEST(ConsistencyTest, ThreeLinkSerialChain)` at line 100 — 3-link chain, zero COM, ID→FD round-trip with EXPECT_NEAR
- `TEST(ConsistencyTest, BranchingYConfiguration)` at line 184 — branching Y, zero COM, round-trip + symmetric check
- `TEST(ConsistencyTest, TwoLinkRoundTripWithGravity)` at line 309 — 2-link with gravity, zero COM, round-trip
- `TEST(ConsistencyTest, RoundTripABA_RNEA_DirectComparison)` at line 387 — single-link with COM(0,0.5,0), shows non-zero COM pattern

**Existing test in TestForwardDynamics.cpp:**
- `TEST(ForwardDynamicsTest, ThreeLinkNumericalValidation)` at line 259 — 3-link, zero COM, finiteness + ordering assertions only
</interfaces>

## Current State (2026-06-17)

**Existing tests** (from VERIFICATION.md analysis):
- TestDynamicsConsistency.cpp: 8 tests, ALL use zero COM on multi-link tests
- TestForwardDynamics.cpp: 15 tests, ThreeLinkNumericalValidation uses zero COM + no round-trip
- ctest: 11/11 pass (all with zero COM; bug path not exercised)

**Bug isolation** (from CONTEXT.md lines 12-13): All 22 existing ABA/FD/consistency tests pass because multi-link tests use `Vector3d::Zero()` for COM. The Phase 3 correction bug only triggers with non-zero COM where `skew(com)*mass` coupling terms are non-zero.

**Target state after this plan:** 4 new/updated tests exist and FAIL, proving the inwardPass bug is real and detectable. Zero regressions in existing tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three non-zero COM consistency tests that FAIL with current inwardPass()</name>

  <files>tests/TestDynamicsConsistency.cpp</files>

  <read_first>
  - tests/TestDynamicsConsistency.cpp lines 100-177 (ThreeLinkSerialChain — template for new ThreeLinkSerialChainNonZeroCOM; copy its EXACT structure, change only COM and test name)
  - tests/TestDynamicsConsistency.cpp lines 184-261 (BranchingYConfiguration — template for BranchingYNonZeroCOM)
  - tests/TestDynamicsConsistency.cpp lines 309-364 (TwoLinkRoundTripWithGravity — template for TwoLinkGravityNonZeroCOM)
  - tests/TestDynamicsConsistency.cpp lines 387-423 (RoundTripABA_RNEA_DirectComparison — demonstrates non-zero COM RigidBodyInertia constructor with Vector3d(0, 0.5, 0))
  - include/RigidBodyInertia.h (constructor signature: RigidBodyInertia(double mass, Vector3d com, lt inertiaLT))
  - .planning/phases/14-cr-02-bug-fix/14-CONTEXT.md lines 20-28 (TDD strategy — new tests MUST FAIL with current code)
  - .planning/phases/14-cr-02-bug-fix/14-PATTERNS.md lines 476-748 (pattern map with exact code templates for all three new tests)
  </read_first>

  <action>
    Add three new GTest tests to TestDynamicsConsistency.cpp. Each is a copy-paste variant of an existing zero-COM test with only these changes: COM values and test names. Place each new test immediately after its zero-COM counterpart. The existing zero-COM tests MUST remain unchanged.

    **Insertion point 1 — After ThreeLinkSerialChain (ends at line 177), before BranchingYConfiguration doc comment (line 179):**

    Add `TEST(ConsistencyTest, ThreeLinkSerialChainNonZeroCOM)`:
    - Copy lines 100-177 EXACTLY
    - Change test name to `ThreeLinkSerialChainNonZeroCOM`
    - Replace ALL six occurrences of `RigidBodyInertia(1.0, Vector3d::Zero(), lt::Identity(3))` (lines 108, 118, 128, 147, 156, 165) with `RigidBodyInertia(1.0, Vector3d(0.1, 0, 0), lt::Identity(3))`
    - Add Doxygen comment block: `@brief Three-link serial chain round-trip with non-zero COM` with `@details` explaining that COM offset [0.1, 0, 0] creates non-block-diagonal Ia matrices requiring correct condensation (Phase 14 CR-02). Per CONTEXT.md, this test MUST FAIL with the current 3-phase inwardPass.
    - Keep everything else IDENTICAL: qddot_input=[1.0, 0.5, 0.25], identity rotation, 1m X offsets, Z revolute joints, EPSILON=1e-8, EXPECT_NEAR assertions

    **Insertion point 2 — After BranchingYConfiguration (ends at line 261), before RoundTripWithGravity doc comment (line 263):**

    Add `TEST(ConsistencyTest, BranchingYNonZeroCOM)`:
    - Copy lines 184-261 EXACTLY
    - Change test name to `BranchingYNonZeroCOM`
    - Replace base link COM: `RigidBodyInertia(1.0, Vector3d(0.05, 0, 0), lt::Identity(3))` (lines 191, 228)
    - Replace children COM: `RigidBodyInertia(0.5, Vector3d(0.05, 0, 0), lt::Identity(3) * 0.5)` (lines 200, 209, 237, 246)
    - Add Doxygen block explaining non-zero COM on branching Y verifies condensation correctness for multi-child parents
    - Keep everything else IDENTICAL: qddot_input=[1.0, 0.5, 0.5], ±1m X offsets, symmetric branch check

    **Insertion point 3 — After TwoLinkRoundTripWithGravity (ends at line 364), before RoundTripABA_RNEA_DirectComparison doc comment (line 366):**

    Add `TEST(ConsistencyTest, TwoLinkGravityNonZeroCOM)`:
    - Copy lines 309-364 EXACTLY  
    - Change test name to `TwoLinkGravityNonZeroCOM`
    - Replace ALL four occurrences of `RigidBodyInertia(1.0, Vector3d::Zero(), lt::Identity(3))` (lines 318, 327, 345, 354) with `RigidBodyInertia(1.0, Vector3d(0.1, 0, 0), lt::Identity(3))`
    - Add Doxygen block explaining gravity × COM coupling exercises condensation with non-zero bias forces
    - Keep everything else IDENTICAL: gravity=(0,0,-9.81), qddot_input=[1.0, 0.5], 1m X offset

    **No other changes to the file.** The existing 8 tests, the `main()` function, `#include` block, `using namespace`, and `EPSILON=1e-8` constant are untouched.
  </action>

  <acceptance_criteria>
    <criteria id="AC1-1">File tests/TestDynamicsConsistency.cpp contains exactly three new tests named `ThreeLinkSerialChainNonZeroCOM`, `BranchingYNonZeroCOM`, `TwoLinkGravityNonZeroCOM` — grep for each test name returns exactly one match</criteria>
    <criteria id="AC1-2">File tests/TestDynamicsConsistency.cpp contains EXACTLY the number of `RigidBodyInertia(.*Vector3d\((0\.1|0\.05).*lt::Identity` occurrences needed for the three new tests (12 occurrences: 6 for 3-link + 6 for branching + 4 for 2-link = 16) — baseline count of `RigidBodyInertia` before edit is for existing tests with Vector3d::Zero()</criteria>
    <criteria id="AC1-3">Original tests ThreeLinkSerialChain, BranchingYConfiguration, TwoLinkRoundTripWithGravity still use Vector3d::Zero() COM — UNCHANGED from current code</criteria>
    <criteria id="AC1-4">`cmake -B build && cmake --build build` succeeds with zero errors</criteria>
    <criteria id="AC1-5">`cd build && ./TestDynamicsConsistency` reports 11 total tests (8 existing + 3 new). The 8 existing tests PASS, the 3 new tests FAIL with qddot mismatches beyond EPSILON=1e-8</criteria>
    <criteria id="AC1-6">`cd build && ./TestDynamicsConsistency --gtest_filter=*NonZeroCOM*` shows 3 tests, all FAILED</criteria>
    <criteria id="AC1-7">New tests follow existing patterns: same PluckerTransform construction, same MotionVector joint axis, same EXPECT_NEAR assertions, same two-chain (ID+FD) setup style</criteria>
  </acceptance_criteria>

  <verify>
    <automated>
      cmake -B build 2>&1 | tail -3 && cmake --build build 2>&1 | tail -5 && cd build && ./TestDynamicsConsistency --gtest_filter=*NonZeroCOM* 2>&1 | tail -25
    </automated>
  </verify>

  <done>
    Build succeeds. TestDynamicsConsistency has 11 tests: 8 existing PASS, 3 new (ThreeLinkSerialChainNonZeroCOM, BranchingYNonZeroCOM, TwoLinkGravityNonZeroCOM) FAIL with qddot mismatch > 1e-8. Zero regressions. RED phase complete — Plan 14-03 code fix is justified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ThreeLinkNumericalValidation with non-zero COM round-trip check</name>

  <files>tests/TestForwardDynamics.cpp</files>

  <read_first>
  - tests/TestForwardDynamics.cpp lines 255-303 (existing ThreeLinkNumericalValidation — understand current zero-COM implementation before replacing)
  - tests/TestForwardDynamics.cpp lines 1-7 (includes: ForwardDynamics.h, gtest/gtest.h, Eigen/Dense — verify InverseDynamics.h is NOT currently included; it needs to be added)
  - tests/TestDynamicsConsistency.cpp lines 100-177 (ThreeLinkSerialChain round-trip pattern — model the new round-trip check on this)
  - include/InverseDynamics.h (InverseDynamicsLink struct layout: parent, X, I, S, q, qdot matching ForwardDynamics Link fields)
  - include/RigidBodyInertia.h (constructor signature)
  - .planning/phases/14-cr-02-bug-fix/14-CONTEXT.md lines 26-28 (VERIFY step: ThreeLinkNumericalValidation must use round-trip ID→FD check)
  - .planning/phases/14-cr-02-bug-fix/14-VERIFICATION.md lines 31-41 (SC2 gap: test still uses zero COM, no round-trip)
  - .planning/phases/14-cr-02-bug-fix/14-PATTERNS.md lines 327-472 (exact code template for the updated test)
  </read_first>

  <action>
    Update the existing `TEST(ForwardDynamicsTest, ThreeLinkNumericalValidation)` test at lines 255-303 of TestForwardDynamics.cpp. The test name, location, and surrounding tests remain the same — only the body changes:

    **Before changing the test body, verify:** `#include "InverseDynamics.h"` must be present in the include block. If not, add it alongside the existing `#include "ForwardDynamics.h"` (same pattern as TestDynamicsConsistency.cpp line 1).

    **Replace the entire test body (lines 259-303) with a round-trip version:**

    1. **Define qddot_input:** `Eigen::VectorXd qddot_input(3); qddot_input[0] = 1.0; qddot_input[1] = 0.5; qddot_input[2] = 0.25;`

    2. **RNEA: qddot_input → tau.** Build an InverseDynamics chain with the SAME physical parameters as the ForwardDynamics chain:
       - `InverseDynamics id;`
       - Link 0 (base): parent=-1, X=Identity transform, `I = RigidBodyInertia(1.0, Vector3d(0.1, 0, 0), lt::Identity(3))`, `S = MotionVector(Vector3d(0,0,1), Vector3d::Zero())`, q=0.0, qdot=0.0
       - Link 1: parent=0, `X = PluckerTransform(Rotation(Identity), Vector3d(1, 0, 0))`, `I = RigidBodyInertia(1.0, Vector3d(0.1, 0, 0), lt::Identity(3))`, same S/q/qdot
       - Link 2: parent=1, `X = PluckerTransform(Rotation(Identity), Vector3d(1, 0, 0))`, `I = RigidBodyInertia(1.0, Vector3d(0.1, 0, 0), lt::Identity(3))`, same S/q/qdot
       - `Eigen::VectorXd tau = id.computeTorques(qddot_input);`

    3. **ABA: tau → qddot_output.** Build a ForwardDynamics chain with IDENTICAL parameters:
       - Same 3-link setup as current code (lines 260-290) but with `Vector3d(0.1, 0, 0)` instead of `Vector3d::Zero()` on ALL three `RigidBodyInertia` constructors
       - `fd.computeAccelerations(tau);`

    4. **Round-trip verification:** Replace the old finiteness + ordering assertions (lines 299-303) with:
       - `EXPECT_NEAR(fd.links[0].qddot, qddot_input[0], EPSILON);` where `constexpr double EPSILON = 1e-8;` must already exist in file scope — if not, use `1e-8` literal or define a local constant
       - `EXPECT_NEAR(fd.links[1].qddot, qddot_input[1], EPSILON);`
       - `EXPECT_NEAR(fd.links[2].qddot, qddot_input[2], EPSILON);`
       - `EXPECT_TRUE(std::isfinite(fd.links[0].qddot));` (keep finiteness as a sanity check)
       - `EXPECT_TRUE(std::isfinite(fd.links[1].qddot));`
       - `EXPECT_TRUE(std::isfinite(fd.links[2].qddot));`
       - `EXPECT_LT(fd.links[0].qddot, fd.links[2].qddot);` (keep ordering check)

    **What the old test did (REMOVED):**
    - Built FD chain with `Vector3d::Zero()` COM on all links
    - Called `computeAccelerations(tau=[1.0, 0.5, 0.25])`
    - Asserted finiteness and `qddot[0] < qddot[2]` — no round-trip

    **What the new test does (ADDED):**
    - Builds both ID and FD chains with `Vector3d(0.1, 0, 0)` COM
    - Runs RNEA(qddot_input) → tau, then ABA(tau) → qddot_output
    - Verifies round-trip within EPSILON=1e-8
    - Also checks finiteness and qddot ordering as additional invariants

    **No other tests in this file are modified.** The other 14 tests (SingleLinkPendulum through ZeroMassEdgeCase) remain exactly as-is.

    **EPSILON note:** Check if a file-level `constexpr double EPSILON = 1e-10;` exists at the top of TestForwardDynamics.cpp. If so, use `1e-8` explicitly in the EXPECT_NEAR calls (do NOT change the file-level constant as other tests depend on 1e-10).
  </action>

  <acceptance_criteria>
    <criteria id="AC2-1">`grep "#include \"InverseDynamics.h\"" tests/TestForwardDynamics.cpp` returns exactly one match (line added if missing)</criteria>
    <criteria id="AC2-2">`grep "Vector3d::Zero()" tests/TestForwardDynamics.cpp` count within ThreeLinkNumericalValidation function (lines 259-303) is 0 — no zero COM in updated test</criteria>
    <criteria id="AC2-3">`grep "Vector3d(0.1, 0, 0)" tests/TestForwardDynamics.cpp` returns ≥6 matches (3 ID + 3 FD link constructions)</criteria>
    <criteria id="AC2-4">Updated test contains both `computeTorques(qddot_input)` and `computeAccelerations(tau)` — round-trip pattern is present</criteria>
    <criteria id="AC2-5">Updated test contains `EXPECT_NEAR(fd.links[0].qddot, qddot_input[0],` assertions — round-trip verification</criteria>
    <criteria id="AC2-6">`cmake -B build && cmake --build build` succeeds with zero errors</criteria>
    <criteria id="AC2-7">`cd build && ./TestForwardDynamics` reports 15 tests: 14 existing PASS, ThreeLinkNumericalValidation FAILS (qddot mismatch > 1e-8)</criteria>
    <criteria id="AC2-8">`cd build && ./TestForwardDynamics --gtest_filter=*ThreeLinkNumericalValidation*` FAILS — confirms non-zero COM exposes the bug</criteria>
    <criteria id="AC2-9">All 14 other ForwardDynamics tests still PASS — zero regressions from test update</criteria>
  </acceptance_criteria>

  <verify>
    <automated>
      cmake -B build 2>&1 | tail -3 && cmake --build build 2>&1 | tail -5 && cd build && ./TestForwardDynamics --gtest_filter=*ThreeLinkNumerical* 2>&1 | tail -15 && ./TestForwardDynamics --gtest_filter=-*ThreeLinkNumerical* 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Build succeeds. ThreeLinkNumericalValidation FAILS with non-zero COM round-trip mismatch > 1e-8. All 14 other ForwardDynamics tests PASS. Combined with Task 1 output (3 new consistency tests also FAIL), SC1 and SC2 gaps are demonstrated. RED phase complete — Plan 14-03 inwardPass restructuring is justified by test evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → computeTorques/computeAccelerations | Test-crafted kinematic chains and torque values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | Test data values | accept | Test constructs deterministic chains with hardcoded masses/COMs/torques — no external input. No threat. |
| T-14-02-02 | Denial of Service | Eigen memory in tests | accept | 3-link chains allocate trivial memory; Eigen uses RAII. No threat. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs involved. |
</threat_model>

<verification>
## RED Phase Verification

### Pre-execution baseline (current state):
```
build/TestForwardDynamics        → 15 pass, 0 fail  (all zero-COM, ThreeLinkNumericalValidation uses Zero COM)
build/TestDynamicsConsistency    → 8 pass, 0 fail   (all zero-COM multi-link)
ctest                            → 11/11 pass, 0 fail
```

### After Task 1 (new consistency tests added):
```
build/TestDynamicsConsistency    → 8 pass, 3 fail   (ThreeLinkSerialChainNonZeroCOM, BranchingYNonZeroCOM, TwoLinkGravityNonZeroCOM FAIL)
```

### After Task 2 (ThreeLinkNumericalValidation updated):
```
build/TestForwardDynamics        → 14 pass, 1 fail  (ThreeLinkNumericalValidation FAILS on non-zero COM round-trip)
ctest                            → 9/11 pass, 2 fail
```

### Gap closure verification:
- SC1 (VERIFICATION.md line 18): Three failing non-zero COM tests exist → GAP DEMONSTRATED
- SC2 (VERIFICATION.md line 31): ThreeLinkNumericalValidation uses non-zero COM + round-trip and FAILS → GAP DEMONSTRATED
</verification>

<success_criteria>
1. Three new non-zero COM tests in TestDynamicsConsistency.cpp exist and FAIL: ThreeLinkSerialChainNonZeroCOM, BranchingYNonZeroCOM, TwoLinkGravityNonZeroCOM
2. ThreeLinkNumericalValidation updated with Vector3d(0.1,0,0) COM and round-trip ID→FD check — FAILS
3. Build succeeds with zero errors
4. All existing tests (8 consistency, 14 FD) continue to PASS — zero regressions
5. New tests follow existing code patterns exactly (PluckerTransform construction, MotionVector joints, EXPECT_NEAR assertions)
6. InverseDynamics.h included in TestForwardDynamics.cpp if not already present
</success_criteria>

<output>
Create `.planning/phases/14-cr-02-bug-fix/14-02-SUMMARY.md` when done
</output>
