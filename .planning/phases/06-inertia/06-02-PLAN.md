---
phase: 06
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified: [tests/TestRigidBodyInertia.cpp, CMakeLists.txt]
autonomous: true
requirements: [INR-01, INR-02]

must_haves:
  truths:
    - "RigidBodyInertia can be constructed with mass, COM, inertia tensor"
    - "RigidBodyInertia::apply() returns correct ForceVector for given MotionVector"
    - "Tests compile and link with GTest"
  artifacts:
    - path: "tests/TestRigidBodyInertia.cpp"
      provides: "Comprehensive GTest test suite"
      min_lines: 100
    - path: "CMakeLists.txt"
      provides: "Test executable registration"
      contains: "add_executable(TestRigidBodyInertia"
  key_links:
    - from: "tests/TestRigidBodyInertia.cpp"
      to: "include/RigidBodyInertia.h"
      via: "#include"
      pattern: "#include.*RigidBodyInertia.h"
---

<objective>
Create comprehensive GTest test suite for RigidBodyInertia

Purpose: Verify RigidBodyInertia construction, operations, and apply() method with mathematical correctness. Tests serve as executable specification.

Output: TestRigidBodyInertia executable with full coverage
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@include/RigidBodyInertia.h
@tests/TestPluckerTransform.cpp
@tests/TestRotation.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register TestRigidBodyInertia in CMakeLists.txt</name>
  <files>CMakeLists.txt</files>
  <action>
    Add test executable registration following the pattern of existing tests (lines 31-67):
    
    1. Add: add_executable(TestRigidBodyInertia tests/TestRigidBodyInertia.cpp)
    2. Add target_link_libraries block linking SpatialAlgebra, GTest::GTest, GTest::Main
    3. Add: add_test(NAME TestRigidBodyInertia COMMAND TestRigidBodyInertia)
    
    Place after TestSpatialUtils entries (after line 67).
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | grep -i "TestRigidBodyInertia" | head -5</automated>
  </verify>
  <done>Test executable registered, cmake configuration succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RigidBodyInertia construction and accessor tests</name>
  <files>tests/TestRigidBodyInertia.cpp</files>
  <behavior>
    - Test: Default constructor creates zero inertia (mass=0, com=0, inertia=0)
    - Test: Parameterized constructor stores mass, COM, inertia correctly
    - Test: getMass() returns stored mass value
    - Test: getCom() returns stored center of mass vector
    - Test: getInertiaMatrixLT() returns stored lower triangular matrix
    - Test: operator+ combines two inertias correctly (mass addition, COM addition, inertia addition)
    - Test: operator* scales all components uniformly
  </behavior>
  <action>
    Write GTest tests following the pattern from TestRotation.cpp or TestLowerTriangular.cpp:
    
    1. Include headers: RigidBodyInertia.h, MotionVector.h, ForceVector.h, GTest
    2. Create test fixture or use TEST() macros directly
    3. Use EXPECT_DOUBLE_EQ for scalar comparisons
    4. Use Eigen::Matrix comparisons with .isApprox() for vectors/matrices
    5. Test edge cases: zero values, negative values (if physically meaningful)
    
    Follow existing test style: descriptive test names, Arrange-Act-Assert pattern.
  </action>
  <verify>
    <automated>cd build && ctest -R TestRigidBodyInertia --output-on-failure</automated>
  </verify>
  <done>All construction and accessor tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create RigidBodyInertia::apply() tests</name>
  <files>tests/TestRigidBodyInertia.cpp</files>
  <behavior>
    - Test: apply() with pure rotation (ω≠0, v=0) produces torque from Iω and force from -com×ω
    - Test: apply() with pure translation (ω=0, v≠0) produces torque from com×v and force from m*v
    - Test: apply() with combined motion produces correct superposition
    - Test: apply() with zero motion produces zero force
    - Test: Verify formula: [Iω + com×v; m*v - com×ω] matches expected output
  </behavior>
  <action>
    Add tests for the apply() method:
    
    1. Create known MotionVector inputs with specific ω and v
    2. Create RigidBodyInertia with known mass, COM, inertia
    3. Compute expected ForceVector manually using the formula
    4. Compare apply() output to expected using EXPECT_TRUE(result.isApprox(expected))
    
    Include at least 3 test cases covering different motion combinations.
    Reference Featherstone textbook formula in comments.
  </action>
  <verify>
    <automated>cd build && ctest -R TestRigidBodyInertia --output-on-failure</automated>
  </verify>
  <done>All apply() tests pass with correct mathematical results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Library | Tests verify but don't affect production code correctness |
| Mathematical formula | Tests must correctly implement expected results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | RigidBodyInertia::apply() tests | mitigate | Cross-verify test expected values against Featherstone textbook formula |
| T-06-04 | Elevation of Privilege | Test assertions | accept | Test failures are visible, no privilege escalation risk |
</threat_model>

<verification>
- cmake --build build succeeds
- ctest -R TestRigidBodyInertia passes all tests
- Test coverage includes construction, operators, and apply() method
</verification>

<success_criteria>
- TestRigidBodyInertia executable builds and links
- All tests pass with zero failures
- Test coverage includes: constructors, getters, operator+, operator*, apply()
- Tests follow GTest conventions from existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/06-inertia/06-02-SUMMARY.md`
</output>
