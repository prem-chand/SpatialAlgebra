---
phase: 06
plan: 03
type: tdd
wave: 2
depends_on: [01]
files_modified: [tests/TestArticulatedBodyInertia.cpp, CMakeLists.txt, include/ArticulatedBodyInertia.h]
autonomous: true
requirements: [INR-03, INR-04]

must_haves:
  truths:
    - "ArticulatedBodyInertia can be constructed with I, H, M matrices"
    - "ArticulatedBodyInertia::apply() returns correct ForceVector"
    - "ArticulatedBodyInertia operations (add, scale) work correctly"
  artifacts:
    - path: "tests/TestArticulatedBodyInertia.cpp"
      provides: "Comprehensive GTest test suite"
      min_lines: 100
    - path: "CMakeLists.txt"
      provides: "Test executable registration"
      contains: "add_executable(TestArticulatedBodyInertia"
  key_links:
    - from: "tests/TestArticulatedBodyInertia.cpp"
      to: "include/ArticulatedBodyInertia.h"
      via: "#include"
      pattern: "#include.*ArticulatedBodyInertia.h"
---

<objective>
Create comprehensive GTest test suite for ArticulatedBodyInertia

Purpose: Verify ArticulatedBodyInertia construction, operations, and apply() method. Tests validate the mathematical correctness of the articulated body inertia representation.

Output: TestArticulatedBodyInertia executable with full coverage
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@include/ArticulatedBodyInertia.h
@include/RigidBodyInertia.h
@tests/TestRigidBodyInertia.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register TestArticulatedBodyInertia in CMakeLists.txt</name>
  <files>CMakeLists.txt</files>
  <action>
    Add test executable registration following the pattern:
    
    1. Add: add_executable(TestArticulatedBodyInertia tests/TestArticulatedBodyInertia.cpp)
    2. Add target_link_libraries block linking SpatialAlgebra, GTest::GTest, GTest::Main
    3. Add: add_test(NAME TestArticulatedBodyInertia COMMAND TestArticulatedBodyInertia)
    
    Place after TestRigidBodyInertia entries.
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | grep -i "TestArticulatedBodyInertia" | head -5</automated>
  </verify>
  <done>Test executable registered, cmake configuration succeeds</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ArticulatedBodyInertia construction and accessor tests</name>
  <files>tests/TestArticulatedBodyInertia.cpp</files>
  <behavior>
    - Test: Default constructor creates zero inertia (I=0, H=0, M=0)
    - Test: Parameterized constructor stores I, H, M correctly
    - Test: getInertia() returns stored rotational inertia
    - Test: getH() returns stored coupling matrix
    - Test: getM() returns stored mass matrix
    - Test: operator+ combines two articulated inertias correctly
    - Test: operator+(RigidBodyInertia) adds rigid body to articulated body
    - Test: operator* scales all components uniformly
  </behavior>
  <action>
    Write GTest tests following existing patterns:
    
    1. Include headers: ArticulatedBodyInertia.h, RigidBodyInertia.h, GTest
    2. Use TEST() macros with descriptive names
    3. Use EXPECT_TRUE(matrix.isApprox(expected)) for matrix comparisons
    4. Test the operator+(RigidBodyInertia) conversion carefully
    5. Verify physical constraints (symmetric positive definite) where applicable
    
    Follow Arrange-Act-Assert pattern with clear test names.
  </action>
  <verify>
    <automated>cd build && ctest -R TestArticulatedBodyInertia --output-on-failure</automated>
  </verify>
  <done>All construction and accessor tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ArticulatedBodyInertia::apply() tests</name>
  <files>tests/TestArticulatedBodyInertia.cpp</files>
  <behavior>
    - Test: apply() with pure rotation produces torque from Iω + Hv
    - Test: apply() with pure translation produces force from Hᵀω + Mv
    - Test: apply() with combined motion produces correct superposition
    - Test: apply() with zero motion produces zero force
    - Test: Verify formula: [Iω + Hv; Hᵀω + Mv] matches expected output
    - Test: Compare with RigidBodyInertia::apply() for reduced case (H=0, M=mass*I)
  </behavior>
  <action>
    Add tests for the apply() method:
    
    1. Create ArticulatedBodyInertia with known I, H, M matrices
    2. Create MotionVector with specific ω and v components
    3. Compute expected ForceVector using formula: torque = I*ω + H*v, force = Hᵀ*ω + M*v
    4. Compare apply() output to expected
    5. Include edge case: when H=0 and M is diagonal, behavior should match RigidBodyInertia
    
    Use Eigen's isApprox() for floating-point comparisons.
  </action>
  <verify>
    <automated>cd build && ctest -R TestArticulatedBodyInertia --output-on-failure</automated>
  </verify>
  <done>All apply() tests pass with correct mathematical results</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Library | Tests verify mathematical correctness |
| Formula implementation | Tests must match Featherstone specification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | ArticulatedBodyInertia::apply() tests | mitigate | Verify test expected values against textbook formula f = [Iω + Hv; Hᵀω + Mv] |
| T-06-06 | Information Disclosure | ArticulatedBodyInertia::print() | accept | Debug output only, no sensitive data |
</threat_model>

<verification>
- cmake --build build succeeds
- ctest -R TestArticulatedBodyInertia passes all tests
- Test coverage includes: constructors, getters, all operators, apply() method
</verification>

<success_criteria>
- TestArticulatedBodyInertia executable builds and links
- All tests pass with zero failures
- Test coverage includes: constructors, getters, operator+, operator+(RigidBodyInertia), operator*, apply()
- Tests follow GTest conventions from existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/06-inertia/06-03-SUMMARY.md`
</output>
