---
phase: 17-benchmark-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/common/random_state.h
  - benchmarks/common/random_state.cpp
  - benchmarks/bench_all.cpp
autonomous: true
requirements: [BENCH-01, BENCH-02, BENCH-03]
user_setup: []
must_haves:
  truths:
    - "RandomState exposes randomTorques(nDOF) returning std::vector<double> in uniform range [-10, 10] Nm (D-16)"
    - "RandomState exposes randomAccelerations(nDOF) returning std::vector<double> in uniform range [-5, 5] rad/s² (D-17)"
    - "RandomState exposes fillPositions(double*, int), fillVelocities(double*, int), fillTorques(double*, int), fillAccelerations(double*, int) (D-19)"
    - "bench_all.cpp registers all 19 core microbenchmarks without DOF sweep (no '/{N}DOF' suffix)"
    - "bench_all.cpp ABA/RNEA DOF sweep loop n=1..20 remains unchanged"
    - "All 21 forward declarations in bench_all.cpp match function signatures in benchmark source files"
    - "BM_PluckerTransform and BM_CrossProduct old declarations and registrations are removed"
  artifacts:
    - path: "benchmarks/common/random_state.h"
      provides: "6 new method declarations with Doxygen comments"
      contains: "randomTorques, randomAccelerations, fillPositions, fillVelocities, fillTorques, fillAccelerations"
    - path: "benchmarks/common/random_state.cpp"
      provides: "6 new method implementations with correct distribution ranges"
      contains: "RandomState::randomTorques, RandomState::randomAccelerations, RandomState::fillPositions, RandomState::fillVelocities, RandomState::fillTorques, RandomState::fillAccelerations"
    - path: "benchmarks/bench_all.cpp"
      provides: "Forward declarations and registrations for all 19 core microbenchmarks + 2 solver benchmarks"
      contains: "21 void BM_* declarations and corresponding RegisterBenchmark calls"
  key_links:
    - from: "bench_all.cpp forward decls"
      to: "core benchmark functions"
      via: "function signature match"
      pattern: "void BM_(TransformMotion|TransformForce|InverseTransformMotion|InverseTransformForce|TformRBI|InvtformRBI|TformABI|InvtformABI|CrossMvMv|CrossMvFv|CrossFvMv|CrossFvFv|RBIApply|RBIAdd|RBIScale|ABIApply|ABIAdd|ABIAddRBI|ABIScale)(benchmark::State&)"
    - from: "RandomState::fillTorques"
      to: "ABA benchmark (Plan 03)"
      via: "fillTorques(tau.data(), nDOF)"
      range: "[-10, 10] Nm per D-16"
    - from: "RandomState::fillAccelerations"
      to: "RNEA benchmark (Plan 03)"
      via: "fillAccelerations(qddot.data(), nDOF)"
      range: "[-5, 5] rad/s² per D-17"
---

<objective>
**Shared benchmark infrastructure — RandomState extensions and bench_all.cpp registration updates.**

**Purpose:** Provide the random state generation methods needed by ABA and RNEA solver benchmarks (torque and acceleration generators with zero-alloc fill variants), and update the unified benchmark entry point with 19 individual core microbenchmark registrations.

**Output:**
- `benchmarks/common/random_state.h` — 6 new method declarations
- `benchmarks/common/random_state.cpp` — 6 new method implementations
- `benchmarks/bench_all.cpp` — 19 individual core benchmark registrations replacing 2 placeholders
</objective>

<execution_context>
@/Users/premchand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/premchand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-benchmark-implementation/17-CONTEXT.md
@.planning/phases/17-benchmark-implementation/17-RESEARCH.md
@.planning/phases/17-benchmark-implementation/17-PATTERNS.md

<interfaces>
**New RandomState methods** (from 17-PATTERNS.md §Pattern 4):
```cpp
// Declarations to add to random_state.h (after randomVelocities, before private:)
std::vector<double> randomTorques(int nDOF);          // D-16: [-10, 10] Nm
std::vector<double> randomAccelerations(int nDOF);    // D-17: [-5, 5] rad/s²
void fillPositions(double* buf, int n);
void fillVelocities(double* buf, int n);
void fillTorques(double* buf, int n);
void fillAccelerations(double* buf, int n);
```

**Existing ModelFactory API** (from benchmarks/common/model_factory.h):
```cpp
ForwardDynamics createFD(int nDOF, const JointConfig& cfg = JointConfig());
InverseDynamics createID(int nDOF, const JointConfig& cfg = JointConfig());
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RandomState with torque/acceleration generation and zero-alloc fill methods</name>
  <files>
    benchmarks/common/random_state.h
    benchmarks/common/random_state.cpp
  </files>

  <read_first>
    - benchmarks/common/random_state.h (existing 60 lines — see current declarations, include pattern, Doxygen style, namespace)
    - benchmarks/common/random_state.cpp (existing 32 lines — see implementation pattern, uniform_real_distribution usage, loop style)
    - 17-PATTERNS.md §Pattern 4 (lines 20-61 for header additions, lines 97-139 for implementation additions)
    - 17-RESEARCH.md §Pattern 4 (lines 583-685 for full implementation detail including distribution range constants)
  </read_first>

  <action>
    **Scope:** Add 6 new methods to RandomState: 2 allocating + 4 zero-alloc fill variants. NO changes to existing methods.

    **Header changes (random_state.h):**
    Add declarations AFTER `randomVelocities(int nDOF)` declaration (line 54), BEFORE `private:` member (line 56). Add an `#include <Eigen/Dense>` after `#include <vector>` on line 17.

    New declarations with Doxygen `@brief`:
    - `randomTorques(int nDOF)` returning `std::vector<double>` — `@brief` "Generate nDOF random joint torques uniform random in [-10, 10] Nm (D-16)"
    - `randomAccelerations(int nDOF)` returning `std::vector<double>` — `@brief` "Generate nDOF random joint accelerations uniform random in [-5, 5] rad/s² (D-17)"
    - `fillPositions(double* buf, int n)` — `@brief` "Zero-alloc fill n random positions into pre-allocated buffer (D-19). Distribution matches randomPositions for identical sequences."
    - `fillVelocities(double* buf, int n)` — `@brief` "Zero-alloc fill n random velocities into pre-allocated buffer (D-19). Distribution matches randomVelocities."
    - `fillTorques(double* buf, int n)` — `@brief` "Zero-alloc fill n random torques into pre-allocated buffer (D-19). Distribution matches randomTorques."
    - `fillAccelerations(double* buf, int n)` — `@brief` "Zero-alloc fill n random accelerations into pre-allocated buffer (D-19). Distribution matches randomAccelerations."

    Follow existing Doxygen style — block comment with `@brief` / `@details` / `@param` / `@return` where applicable. Group new methods with a section comment: `// Phase 17: Torque/acceleration generation (D-16, D-17, D-19)`.

    **Implementation changes (random_state.cpp):**
    Add implementations AFTER `randomVelocities` function body, BEFORE the closing `}` of namespace SpatialAlgebra::Bench.

    New implementations (each creates `std::uniform_real_distribution<double>` locally per RESEARCH.md §Pitfall 4 — identically to the corresponding returning variant to guarantee identical sequences):
    - `randomTorques`: `std::uniform_real_distribution<double> dist(-10.0, 10.0)` per D-16
    - `randomAccelerations`: `std::uniform_real_distribution<double> dist(-5.0, 5.0)` per D-17
    - `fillPositions`: `std::uniform_real_distribution<double> dist(-M_PI_2, M_PI_2)` (same as randomPositions)
    - `fillVelocities`: `std::uniform_real_distribution<double> dist(-5.0, 5.0)` (same as randomVelocities)
    - `fillTorques`: `std::uniform_real_distribution<double> dist(-10.0, 10.0)` (same as randomTorques)
    - `fillAccelerations`: `std::uniform_real_distribution<double> dist(-5.0, 5.0)` (same as randomAccelerations)

    Critical constraints:
    - Each fill method MUST create `std::uniform_real_distribution` locally each call (not reuse a stored distribution) to produce identical sequences as the returning variant for the same n (Pitfall 4, RESEARCH.md §Pattern 4 line 687).
    - All methods use the SAME existing `rng_` member (seed 42, D-11) — do NOT create new RNG instances.
    - Returning variants allocate `std::vector<double>(n)` and fill via loop.
    - Fill variants write directly through `double* buf` pointer with loop over `i < n`.

    Use Doxygen block comments on each implementation matching the header style.
  </action>

  <acceptance_criteria>
    - random_state.h contains 6 new method declarations after `randomVelocities` and before `private:`
    - random_state.cpp contains 6 `RandomState::` prefixed method implementations
    - Doxygen `@brief` on each new declaration
    - `-10.0, 10.0` for torque distributions, `-5.0, 5.0` for acceleration/velocity, `-M_PI_2, M_PI_2` for positions
    - bench_common static library compiles with no errors
  </acceptance_criteria>

  <verify>
    <automated>
      # Verify declarations exist in header (must return 6)
      grep -c 'randomTorques\|randomAccelerations\|fillPositions\|fillVelocities\|fillTorques\|fillAccelerations' benchmarks/common/random_state.h
      # Verify implementations exist in source (must return 6)
      grep -c 'RandomState::randomTorques\|RandomState::randomAccelerations\|RandomState::fillPositions\|RandomState::fillVelocities\|RandomState::fillTorques\|RandomState::fillAccelerations' benchmarks/common/random_state.cpp
      # Verify correct torque range constant
      grep -n '10\.0.*10\.0' benchmarks/common/random_state.cpp
      # Build check
      cmake --build build_bench --target bench_common 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    - random_state.h contains 6 new method declarations with Doxygen comments
    - random_state.cpp contains 6 new implementations with correct uniform distribution ranges
    - `cmake --build build_bench --target bench_common` succeeds (bench_common static lib compiles and links)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update bench_all.cpp with 19 individual core microbenchmark registrations</name>
  <files>
    benchmarks/bench_all.cpp
  </files>

  <read_first>
    - benchmarks/bench_all.cpp (existing 52 lines — see current forward decls, DOF sweep loop pattern, RegisterBenchmark calls)
    - 17-PATTERNS.md §Pattern for bench_all.cpp (lines 535-616 — exact forward declarations and registration list)
    - 17-RESEARCH.md §Pattern 5 (lines 691-763 — full replacement example)
  </read_first>

  <action>
    **Scope:** Replace old core placeholder forward declarations and registrations with 19 individual ones. Keep ABA/RNEA DOF sweep loop and main boilerplate unchanged.

    **Forward declaration changes:**
    REMOVE lines 23-24 (`void BM_PluckerTransform(benchmark::State& state, int nDOF);` and `void BM_CrossProduct(...)`).
    ADD 19 individual forward declarations AFTER the existing ABA/RNEA declarations (after line 22). All use `(benchmark::State&)` signature — no `int nDOF` parameter.

    The 19 core functions (per D-18, with note that inertia count is 7 not 8):
    Plücker transforms (8): BM_TransformMotion, BM_TransformForce, BM_InverseTransformMotion, BM_InverseTransformForce, BM_TformRBI, BM_InvtformRBI, BM_TformABI, BM_InvtformABI
    Cross products (4): BM_CrossMvMv, BM_CrossMvFv, BM_CrossFvMv, BM_CrossFvFv
    Inertia operations (7): BM_RBIApply, BM_RBIAdd, BM_RBIScale, BM_ABIApply, BM_ABIAdd, BM_ABIAddRBI, BM_ABIScale

    **Registration changes:**
    REMOVE the `BM_PluckerTransform` and `BM_CrossProduct` RegisterBenchmark calls from the DOF sweep loop (delete lines 38-45).
    ADD 19 individual RegisterBenchmark calls AFTER the DOF sweep `for` loop brace (after the closing `}` of the for loop at line ~46).

    Registration pattern (no DOF suffix):
    ```cpp
    benchmark::RegisterBenchmark("BM_TransformMotion", BM_TransformMotion);
    ```

    The DOF sweep loop's `BM_ABA_ForwardDynamics` and `BM_RNEA_InverseDynamics` registrations MUST remain unchanged (including the `"/{N}DOF"` suffix pattern).

    **Important:** Remove `#include "common/model_factory.h"` and `#include "common/random_state.h"` from bench_all.cpp (lines 17-18) — these are no longer needed here (core benchmarks don't use them; ABA/RNEA source files include them directly).
  </action>

  <acceptance_criteria>
    - bench_all.cpp contains 21 `void BM_` forward declarations (ABA, RNEA, + 19 core)
    - 0 occurrences of `BM_PluckerTransform` or `BM_CrossProduct` in bench_all.cpp
    - 19 `RegisterBenchmark` calls WITHOUT `DOF` suffix
    - 40 `RegisterBenchmark` calls WITH `DOF` suffix (20 ABA + 20 RNEA)
    - bench_all links against bench_common, benchmark::benchmark, SpatialAlgebra, Eigen3::Eigen
  </acceptance_criteria>

  <verify>
    <automated>
      # Verify no old placeholder forward decls remain (must return 0)
      grep -c 'BM_PluckerTransform\|BM_CrossProduct' benchmarks/bench_all.cpp
      # Verify count of BM_ forward declarations (21 = 2 solver + 19 core)
      grep -c 'void BM_' benchmarks/bench_all.cpp
      # Verify no DOF suffix on core registrations
      grep 'RegisterBenchmark' benchmarks/bench_all.cpp | grep -v 'DOF' | wc -l | xargs
      # Build check (bench_all must link)
      cmake --build build_bench 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    - bench_all.cpp contains 21 forward declarations (2 solver + 19 core)
    - BM_PluckerTransform and BM_CrossProduct are fully removed (no declarations, no registrations)
    - 19 core benchmarks registered without DOF suffix
    - ABA/RNEA DOF sweep loop unchanged (n=1..20 with DOF suffix)
    - `cmake --build build_bench` succeeds (bench_all links against bench_common, benchmark, SpatialAlgebra)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — benchmark-only code with no network, no user input, no data serialization.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | N/A | all | accept | Benchmark-only phase. No security-sensitive operations. All code runs locally with deterministic inputs. |
</threat_model>

<verification>
- [ ] Plan 01 complete: RandomState extended, bench_all.cpp updated
- [ ] `cmake --build build_bench --target bench_common` succeeds
- [ ] `cmake --build build_bench` succeeds (bench_all links)
</verification>

<success_criteria>
- RandomState exposes 6 new methods: randomTorques, randomAccelerations, fillPositions, fillVelocities, fillTorques, fillAccelerations
- bench_all.cpp has 21 forward declarations and 40+ benchmark registrations (20 ABA + 20 RNEA + 19 core)
- No BM_PluckerTransform or BM_CrossProduct declarations/registrations remain
- bench_all links against SpatialAlgebra, benchmark::benchmark, Eigen3::Eigen
</success_criteria>

<output>
Create `.planning/phases/17-benchmark-implementation/17-01-SUMMARY.md` when done
</output>
