---
phase: 01-foundation-vectors
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified: [tests/TestSpatialVector.cpp]
autonomous: false
requirements: [VEC-01, VEC-02, VEC-03, VEC-04]
user_setup: []

must_haves:
  truths:
    - "All spatial vector tests pass"
    - "Property-based tests verify mathematical invariants"
    - "Textbook examples verify numerical correctness"
  artifacts:
    - path: "tests/TestSpatialVector.cpp"
      provides: "Comprehensive test suite with property-based tests"
      contains: "TEST(TestSpatialVector, Property" or "TEST(TestMotionVector, Property"
  key_links:
    - from: "tests/TestSpatialVector.cpp"
      to: "src/SpatialVector.cpp"
      via: "tests implementation"
      pattern: "TEST.*SpatialVector"
---

<objective>
Add property-based tests and textbook examples to verify mathematical correctness per D-04.

Purpose: Combined verification approach — specific numerical values from Featherstone examples AND general property tests (anti-commutativity, distributivity).
Output: Enhanced test suite with 5-10 additional property-based and textbook verification tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-vectors/01-CONTEXT.md

<interfaces>
<!-- Property-based test patterns from TestPluckerTransform.cpp -->

From tests/TestPluckerTransform.cpp (GTest example):
```cpp
TEST(TestPluckerTransform, TransformMotion) {
    // Example test structure
    PluckerTransform transform(...);
    MotionVector v(...);
    MotionVector result = transform.transformMotion(v);
    EXPECT_DOUBLE_EQ(result.getAngular()[0], expected_value);
}
```

Properties to test:
- Cross product anti-commutativity: a × b = -(b × a)
- Cross product distributivity: a × (b + c) = a × b + a × c
- Dot product commutativity: a · b = b · a
- Scalar multiplication associativity: (k*a) × b = k*(a × b)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add property-based tests for spatial vectors</name>
  <files>tests/TestSpatialVector.cpp</files>
  <action>
    Add property-based tests to verify mathematical invariants:
    
    1. TEST(TestSpatialVector, CrossProductAntiCommutativity)
       - Create two spatial vectors a, b with different values
       - Verify: a.crossMotion(b) == -(b.crossMotion(a))
       - Use EXPECT_DOUBLE_EQ on each component
    
    2. TEST(TestSpatialVector, DotProductCommutativity)
       - Verify: a.dot(b) == b.dot(a)
    
    3. TEST(TestMotionVector, CrossProductDistributivity)
       - Create three motion vectors a, b, c
       - Verify: a.crossMotion(b + c) == a.crossMotion(b) + a.crossMotion(c)
    
    4. TEST(TestMotionVector, ScalarMultiplicationProperty)
       - Verify: (k*a).crossMotion(b) == k*(a.crossMotion(b))
       - Test with k = 2.5, a, b with specific values
    
    5. TEST(TestForceVector, CrossProductAntiCommutativity)
       - Same as #1 but for ForceVector::crossForce
  </action>
  <verify>
    <automated>cd build && ./TestSpatialVector --gtest_filter="*Property*"</automated>
  </verify>
  <done>
    All 5 property-based tests pass with [  PASSED  ] status
  </done>
</task>

<task type="auto">
  <name>Task 2: Add textbook example tests</name>
  <files>tests/TestSpatialVector.cpp</files>
  <action>
    Add tests with specific numerical values from Featherstone textbook examples:
    
    1. TEST(TestSpatialVector, FeatherstoneExample2_1)
       - Use values from Featherstone Chapter 2, Example 2.1 (if available)
       - Or create representative example:
         - SpatialVector v1(Vector3d(1,0,0), Vector3d(0,1,0))  // rotation about X, translation along Y
         - SpatialVector v2(Vector3d(0,1,0), Vector3d(0,0,1))  // rotation about Y, translation along Z
         - Verify cross product result matches manual calculation
    
    2. TEST(TestMotionVector, TwistExample)
       - Create twist representing screw motion:
         - ω = (0, 0, 1) rad/s (rotation about Z)
         - v = (1, 0, 0) m/s (translation along X)
       - Verify crossMotion with another twist
    
    3. TEST(TestForceVector, WrenchExample)
       - Create wrench representing force and torque:
         - τ = (1, 0, 0) N⋅m (torque about X)
         - f = (0, 1, 0) N (force along Y)
       - Verify crossForce with another wrench
    
    Include comments citing "Featherstone 2008, Chapter 2" for each test
  </action>
  <verify>
    <automated>cd build && ./TestSpatialVector --gtest_filter="*Featherstone*:Example*"</automated>
  </verify>
  <done>
    All textbook example tests pass with expected numerical values
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify test suite completeness</name>
  <what-built>
    Complete GTest test suite for Phase 1 with:
    - Basic operation tests (constructors, getters, operators)
    - Cross product tests (crossMotion, crossForce)
    - Property-based tests (anti-commutativity, distributivity)
    - Textbook example tests with specific numerical values
  </what-built>
  <how-to-verify>
    1. Run full test suite: `cd build && ./TestSpatialVector`
    2. Verify all tests pass (expect 20-30 tests total)
    3. Check test output is readable and shows individual test names
    4. Verify test coverage:
       - SpatialVector: constructors, operators, crossMotion, crossForce, dot
       - MotionVector: constructors, operators, crossMotion, crossForce, dot
       - ForceVector: constructors, operators, crossMotion, crossForce, dot
       - Property tests: anti-commutativity, distributivity, scalar properties
       - Textbook examples: at least 2-3 specific numerical examples
    
    5. Review test code quality:
       - Clear test names
       - Comments explaining formulas
       - No code duplication (consider test fixtures if repeated)
  </how-to-verify>
  <resume-signal>Type "approved" if test suite is complete and all tests pass, or describe missing tests/issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Implementation | Property tests verify mathematical correctness — bugs indicate implementation errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | tests/TestSpatialVector.cpp | accept | Test code verifies correctness, low risk |
| T-01-08 | Repudiation | tests/TestSpatialVector.cpp | mitigate | Tests cite Featherstone textbook as source |
</threat_model>

<verification>
- Full test suite passes: `./TestSpatialVector` shows `[  PASSED  ] 20+ tests`
- Property tests verify mathematical invariants
- Textbook examples verify numerical correctness
- No test failures or flaky tests
- Test output is clear and readable
- Code coverage >85% for SpatialVector, MotionVector, ForceVector (optional)
</verification>

<success_criteria>
- [ ] 5+ property-based tests added and passing
- [ ] 3+ textbook example tests with specific numerical values
- [ ] All tests pass (20-30 total tests)
- [ ] Test output shows clear pass/fail per test
- [ ] Human verification checkpoint passed
- [ ] Test code has no warnings or errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vectors/01-04-SUMMARY.md` with:
- Total test count and breakdown by class
- Property tests added
- Textbook examples used
- Test execution time
- Any remaining gaps or TODOs
</output>
