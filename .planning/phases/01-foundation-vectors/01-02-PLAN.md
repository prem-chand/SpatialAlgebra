---
phase: 01-foundation-vectors
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/MotionVector.cpp]
autonomous: true
requirements: [VEC-02]
user_setup: []

must_haves:
  truths:
    - "MotionVector::crossMotion uses correct Featherstone formula"
    - "Bug in line 42-46 of src/MotionVector.cpp is fixed"
  artifacts:
    - path: "src/MotionVector.cpp"
      provides: "Corrected crossMotion implementation"
      contains: "linear.cross(other.angular) + angular.cross(other.linear)"
  key_links:
    - from: "src/MotionVector.cpp"
      to: "include/SpatialVector.h"
      via: "inherits crossMotion formula"
      pattern: "class MotionVector : public SpatialVector"
---

<objective>
Fix MotionVector::crossMotion bug to match correct Featherstone formula implemented in SpatialVector base class.

Purpose: Ensure mathematical correctness of motion cross product operation per D-02. Current implementation `[ω1×ω2; v1×v2]` is wrong — should be `[ω1×ω2; ω1×v2 + v1×ω2]`.
Output: Corrected src/MotionVector.cpp with proper crossMotion formula.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-vectors/01-CONTEXT.md

<interfaces>
<!-- Key contracts from existing codebase -->

From src/SpatialVector.cpp:42-46 (CORRECT formula):
```cpp
SpatialVector SpatialAlgebra::SpatialVector::crossMotion(const SpatialVector &other) const
{
    // V1 x V2 = [w1 x w2, w1 x v2 + v1 x w2]
    return SpatialVector(angular.cross(other.angular), linear.cross(other.angular) + angular.cross(other.linear));
}
```

From src/MotionVector.cpp:42-46 (BUG LOCATION):
```cpp
MotionVector MotionVector::crossMotion(const MotionVector &other) const
{
    return MotionVector(angular.cross(other.angular), linear.cross(other.linear));  // BUG: should be linear.cross(other.angular) + angular.cross(other.linear)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MotionVector::crossMotion implementation</name>
  <files>src/MotionVector.cpp</files>
  <action>
    1. Read src/MotionVector.cpp lines 42-46 to locate the bug
    2. Replace the crossMotion implementation:
       - OLD: `return MotionVector(angular.cross(other.angular), linear.cross(other.linear));`
       - NEW: `return MotionVector(angular.cross(other.angular), linear.cross(other.angular) + angular.cross(other.linear));`
    3. Verify the formula matches SpatialVector base class (src/SpatialVector.cpp:42-46)
    4. Add a comment explaining the formula: `// [ω1×ω2; ω1×v2 + v1×ω2] per Featherstone`
    
    This implements decision D-02 from CONTEXT.md.
  </action>
  <verify>
    <automated>cd build && make SpatialAlgebra && echo "Build successful"</automated>
  </verify>
  <done>
    src/MotionVector.cpp line 45 contains: `linear.cross(other.angular) + angular.cross(other.linear)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify fix with existing tests</name>
  <files>tests/TestSpatialVector.cpp</files>
  <action>
    1. Run the GTest tests created in Plan 01
    2. Verify that TestMotionVector.CrossMotion now passes
    3. If test still fails, debug by:
       - Checking the formula matches exactly
       - Verifying no typos in variable names
       - Running with --gtest_print_time=0 for cleaner output
  </action>
  <verify>
    <automated>cd build && ./TestSpatialVector --gtest_filter="TestMotionVector.CrossMotion"</automated>
  </verify>
  <done>
    TestMotionVector.CrossMotion test passes with [       OK ] status
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library → User code | Mathematical operations must be correct — bugs cause incorrect physics simulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | src/MotionVector.cpp | mitigate | Formula verified against Featherstone textbook and SpatialVector base class |
| T-01-04 | Repudiation | src/MotionVector.cpp | mitigate | Comment cites Featherstone as source of formula |
</threat_model>

<verification>
- Bug fix compiles without warnings: `cmake --build build 2>&1 | grep -i warning` returns nothing
- TestMotionVector.CrossMotion test passes: `./TestSpatialVector --gtest_filter="TestMotionVector.CrossMotion"` shows [       OK ]
- Formula in code matches comment: `// [ω1×ω2; ω1×v2 + v1×ω2]`
- All other MotionVector tests still pass (regression check)
</verification>

<success_criteria>
- [ ] MotionVector::crossMotion uses formula: `linear.cross(other.angular) + angular.cross(other.linear)`
- [ ] TestMotionVector.CrossMotion test passes
- [ ] No regression in other MotionVector tests
- [ ] Implementation matches SpatialVector base class formula
- [ ] Comment documents the Featherstone formula
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vectors/01-02-SUMMARY.md` with:
- Bug description and fix applied
- Test results before/after fix
- Verification that formula matches Featherstone
</output>
