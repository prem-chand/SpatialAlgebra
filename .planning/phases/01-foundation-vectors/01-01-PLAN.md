---
phase: 01-foundation-vectors
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [tests/TestSpatialVector.cpp]
autonomous: true
requirements: [VEC-01, VEC-02, VEC-03, VEC-04]
user_setup: []

must_haves:
  truths:
    - "TestSpatialVector.cpp uses GTest framework with TEST() macros"
    - "Tests verify SpatialVector creation with angular and linear components"
    - "Tests verify MotionVector creation and component access"
    - "Tests verify ForceVector creation and component access"
    - "Tests verify vector addition, subtraction, and scalar multiplication"
  artifacts:
    - path: "tests/TestSpatialVector.cpp"
      provides: "Comprehensive GTest test suite for spatial vectors"
      exports: ["TEST(TestSpatialVector, *)", "TEST(TestMotionVector, *)", "TEST(TestForceVector, *)"]
  key_links:
    - from: "tests/TestSpatialVector.cpp"
      to: "include/SpatialVector.h"
      via: "#include"
      pattern: "#include.*SpatialVector.h"
    - from: "tests/TestSpatialVector.cpp"
      to: "include/MotionVector.h"
      via: "#include"
      pattern: "#include.*MotionVector.h"
    - from: "tests/TestSpatialVector.cpp"
      to: "include/ForceVector.h"
      via: "#include"
      pattern: "#include.*ForceVector.h"
---

<objective>
Replace assert()-based tests with comprehensive GTest suite for SpatialVector, MotionVector, and ForceVector classes.

Purpose: Establish test-driven development foundation with better assertions, test reporting, and verification from the start (per D-01).
Output: Complete GTest test file with 15-20 test cases covering all vector operations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-vectors/01-CONTEXT.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From include/SpatialVector.h:
```cpp
class SpatialVector {
protected:
    Vector3d angular;
    Vector3d linear;
public:
    SpatialVector();
    SpatialVector(const Vector3d &angular, const Vector3d &linear);
    SpatialVector(const SpatialVector &other);
    Vector3d getAngular() const;
    Vector3d getLinear() const;
    SpatialVector operator+(const SpatialVector &other) const;
    SpatialVector operator-(const SpatialVector &other) const;
    SpatialVector operator*(double scalar) const;
    SpatialVector crossMotion(const SpatialVector &other) const;
    SpatialVector crossForce(const SpatialVector &other) const;
    double dot(const SpatialVector &other) const;
    void print() const;
};
```

From include/MotionVector.h:
```cpp
class MotionVector : public SpatialVector {
public:
    MotionVector();
    MotionVector(const Vector3d &angular, const Vector3d &linear);
    MotionVector(const SpatialVector &other);
    Vector3d getAngular() const;
    Vector3d getLinear() const;
    MotionVector operator+(const MotionVector &other) const;
    MotionVector operator-(const MotionVector &other) const;
    MotionVector operator*(double scalar) const;
    MotionVector crossMotion(const MotionVector &other) const;
    MotionVector crossForce(const MotionVector &other) const;
    double dot(const MotionVector &other) const;
    void print() const;
};
using mv = MotionVector;
```

From include/ForceVector.h:
```cpp
class ForceVector : public SpatialVector {
public:
    ForceVector();
    ForceVector(const Vector3d &angular, const Vector3d &linear);
    ForceVector(const SpatialVector &other);
    Vector3d getAngular() const;
    Vector3d getLinear() const;
    ForceVector operator+(const ForceVector &other) const;
    ForceVector operator-(const ForceVector &other) const;
    ForceVector operator*(double scalar) const;
    ForceVector crossMotion(const ForceVector &other) const;
    ForceVector crossForce(const ForceVector &other) const;
    double dot(const ForceVector &other) const;
    void print() const;
};
using fv = ForceVector;
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write GTest scaffolding and SpatialVector tests</name>
  <files>tests/TestSpatialVector.cpp</files>
  <action>
    1. Remove existing assert()-based test code
    2. Add GTest includes: `#include <gtest/gtest.h>`
    3. Create test fixtures or use TEST() macros directly
    4. Write tests for SpatialVector:
       - TEST(TestSpatialVector, Constructor) — verify zero initialization and component initialization
       - TEST(TestSpatialVector, Getters) — verify getAngular() and getLinear() return correct values
       - TEST(TestSpatialVector, Addition) — verify component-wise addition
       - TEST(TestSpatialVector, Subtraction) — verify component-wise subtraction
       - TEST(TestSpatialVector, ScalarMultiplication) — verify scaling of both components
       - TEST(TestSpatialVector, DotProduct) — verify ω1·ω2 + v1·v2 formula
    
    Use specific numerical values for verification (e.g., Vector3d(1,2,3), Vector3d(4,5,6)).
    Use EXPECT_DOUBLE_EQ for floating point comparisons.
  </action>
  <verify>
    <automated>cd build && cmake .. && make TestSpatialVector && ./TestSpatialVector --gtest_filter="TestSpatialVector.*"</automated>
  </verify>
  <done>
    All 6 SpatialVector tests compile and pass with clear GTest output showing [ RUN      ], [       OK ], [  PASSED  ]
  </done>
</task>

<task type="tdd">
  <name>Task 2: Write MotionVector and ForceVector tests</name>
  <files>tests/TestSpatialVector.cpp</files>
  <action>
    Write GTest tests for MotionVector:
    - TEST(TestMotionVector, Constructor) — verify construction from components and SpatialVector
    - TEST(TestMotionVector, Getters) — verify component access
    - TEST(TestMotionVector, Operations) — verify +, -, * operators
    - TEST(TestMotionVector, CrossMotion) — verify crossMotion formula (will expose bug per D-02)
    - TEST(TestMotionVector, DotProduct) — verify dot product
    
    Write GTest tests for ForceVector:
    - TEST(TestForceVector, Constructor) — verify construction
    - TEST(TestForceVector, Getters) — verify component access
    - TEST(TestForceVector, Operations) — verify +, -, * operators
    - TEST(TestForceVector, CrossForce) — verify crossForce formula
    - TEST(TestForceVector, DotProduct) — verify dot product
    
    Use same numerical values as SpatialVector tests for consistency.
    Include at least one property-based test (e.g., anti-commutativity: a × b = -(b × a)).
  </action>
  <verify>
    <automated>cd build && make TestSpatialVector && ./TestSpatialVector --gtest_filter="TestMotionVector.*:TestForceVector.*"</automated>
  </verify>
  <done>
    All 10+ MotionVector and ForceVector tests compile and pass. CrossMotion test may fail if bug exists (expected per D-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Library | Test code calls library functions — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tests/TestSpatialVector.cpp | accept | Test code only — no production impact, low-value target |
| T-01-02 | Information Disclosure | tests/TestSpatialVector.cpp | accept | No secrets in test code, mathematical formulas are public knowledge |
</threat_model>

<verification>
- All tests compile without warnings: `cmake --build build --target TestSpatialVector 2>&1 | grep -i warning` returns nothing
- All tests pass: `./TestSpatialVector` shows `[  PASSED  ] 16 tests` (or similar count)
- Test output is readable and shows individual test names
- Code coverage report shows >80% coverage for SpatialVector, MotionVector, ForceVector (optional verification)
</verification>

<success_criteria>
- [ ] tests/TestSpatialVector.cpp converted to GTest with 15-20 test cases
- [ ] All tests pass (except crossMotion if bug not yet fixed)
- [ ] Test output shows clear pass/fail status per test case
- [ ] Tests cover constructors, getters, operators (+, -, *), cross products, dot product
- [ ] No compiler warnings in test code
- [ ] Build time < 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-vectors/01-01-SUMMARY.md` with:
- Test count and coverage summary
- Any test failures discovered (especially crossMotion bug)
- Patterns established (test naming, fixture usage, assertion style)
</output>
