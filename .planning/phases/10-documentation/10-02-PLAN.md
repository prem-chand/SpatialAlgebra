---
phase: 10-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docs/]
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "Doxygen documentation can be generated without errors"
    - "HTML documentation exists in docs/html/"
    - "All classes and methods are documented"
  artifacts:
    - path: "docs/html/index.html"
      provides: "Generated HTML documentation"
      min_lines: 50
  key_links:
    - from: "Doxyfile"
      to: "include/*.h"
      via: "INPUT = include src"
      pattern: "INPUT.*include"
---

<objective>
Generate Doxygen documentation and verify completeness.

Purpose: DOC-02 — Users need browsable API documentation.
Output: Fresh Doxygen HTML/LaTeX output in docs/ directory.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@Doxyfile
@include/SpatialVector.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Doxygen and verify output</name>
  <files>docs/html/, docs/latex/</files>
  <action>
    1. Run `doxygen Doxyfile` from project root
    2. Check for warnings/errors in output
    3. Verify docs/html/index.html exists
    4. Verify docs/html/ contains class documentation pages
    
    If warnings about undocumented members:
    - Check if EXTRACT_ALL=YES in Doxyfile (currently NO per line 527)
    - Either set EXTRACT_ALL=YES or ensure all members have @brief/@details
    
    Per Doxyfile config:
    - INPUT = include src (line 968)
    - OUTPUT_DIRECTORY = docs (line 77)
    - GENERATE_HTML = YES (default)
    - GENERATE_LATEX = YES (default)
  </action>
  <verify>
    <automated>test -f docs/html/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    Doxygen runs without errors, docs/html/index.html exists with class documentation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify documentation quality</name>
  <what-built>
    Doxygen HTML documentation generated in docs/html/
  </what-built>
  <how-to-verify>
    1. Open docs/html/index.html in browser
    2. Navigate to Classes → Class List
    3. Verify these classes have documentation:
       - SpatialVector, MotionVector, ForceVector
       - Rotation, PluckerTransform
       - RigidBodyInertia, ArticulatedBodyInertia
       - ForwardDynamics, InverseDynamics
       - LowerTriangular
    4. Check that methods show @brief and @details content
    5. Verify namespaces are documented (SpatialAlgebra)
  </how-to-verify>
  <resume-signal>
    Type "approved" if documentation looks complete, or describe missing items
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase, no code execution boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | Generated docs | accept | Documentation is public API surface, no secrets exposed |
</threat_model>

<verification>
- Doxygen command completes without FATAL errors
- docs/html/index.html exists and is non-empty
- All major classes appear in class list
- Method documentation includes @brief and @details
</verification>

<success_criteria>
- Doxygen generates complete HTML documentation
- All SpatialAlgebra classes are documented
- User can browse API reference at docs/html/index.html
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-02-SUMMARY.md`
</output>
