---
phase: 10-documentation
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified: [examples/, README.md]
autonomous: true
requirements: [DOC-03]

must_haves:
  truths:
    - "User can compile and run examples"
    - "Examples demonstrate vectors, transforms, inertia, ABA"
    - "Examples are referenced from README.md"
  artifacts:
    - path: "examples/basic_vectors.cpp"
      provides: "Vector operations example"
      min_lines: 30
    - path: "examples/transforms.cpp"
      provides: "Plücker transform example"
      min_lines: 40
    - path: "examples/dynamics.cpp"
      provides: "Forward dynamics ABA example"
      min_lines: 50
  key_links:
    - from: "README.md"
      to: "examples/*.cpp"
      via: "example references"
      pattern: "examples/"
    - from: "examples/CMakeLists.txt"
      to: "include/*.h"
      via: "include_directories"
      pattern: "include_directories"
---

<objective>
Create compilable usage examples demonstrating core operations.

Purpose: DOC-03 — Users learn by seeing working code examples.
Output: examples/ directory with 4 example programs + CMake integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@CMakeLists.txt
@include/SpatialVector.h
@include/PluckerTransform.h
@include/RigidBodyInertia.h
@include/ForwardDynamics.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples directory and CMakeLists.txt</name>
  <files>examples/CMakeLists.txt</files>
  <action>
    Create examples/CMakeLists.txt that:
    1. Sets C++17 standard
    2. Finds Eigen3 package
    3. Includes project headers
    4. Links against libSpatialAlgebra.a
    5. Creates executables for each example file
    
    Pattern to follow (similar to tests/ in root CMakeLists.txt):
    ```cmake
    add_executable(example_vectors basic_vectors.cpp)
    target_link_libraries(example_vectors SpatialAlgebra Eigen3::Eigen)
    ```
  </action>
  <verify>
    <automated>test -f examples/CMakeLists.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    examples/CMakeLists.txt configures all example executables
  </done>
</task>

<task type="auto">
  <name>Task 2: Create basic_vectors.cpp example</name>
  <files>examples/basic_vectors.cpp</files>
  <action>
    Create example demonstrating:
    1. Creating MotionVector (twist) with angular and linear components
    2. Creating ForceVector (wrench) with torque and force components
    3. Vector operations: add, subtract, scale
    4. Cross products: crossMotion, crossForce
    5. Using type aliases (mv, fv)
    6. Calling print() method
    
    Include comments explaining each operation.
    Use namespace SpatialAlgebra and type aliases per CONVENTIONS.md.
  </action>
  <verify>
    <automated>cd /Users/premchand/Documents/GitHub/SpatialAlgebra && cmake -B build && cmake --build build --target example_vectors 2>&1 | tail -5</automated>
  </verify>
  <done>
    basic_vectors.cpp compiles successfully, demonstrates MotionVector and ForceVector
  </done>
</task>

<task type="auto">
  <name>Task 3: Create transforms.cpp example</name>
  <files>examples/transforms.cpp</files>
  <action>
    Create example demonstrating:
    1. Creating Rotation from angle-axis or quaternion
    2. Creating PluckerTransform from rotation + translation
    3. Using transformMotion() to transform motion vector
    4. Using transformForce() to transform force vector
    5. Using inverse() to compute inverse transform
    6. Chaining transforms
    
    Show coordinate frame transformation use case.
    Include comments explaining Plücker coordinate concepts.
  </action>
  <verify>
    <automated>cd /Users/premchand/Documents/GitHub/SpatialAlgebra && cmake --build build --target example_transforms 2>&1 | tail -5</automated>
  </verify>
  <done>
    transforms.cpp compiles successfully, demonstrates PluckerTransform operations
  </done>
</task>

<task type="auto">
  <name>Task 4: Create inertia.cpp example</name>
  <files>examples/inertia.cpp</files>
  <action>
    Create example demonstrating:
    1. Creating RigidBodyInertia with mass, COM, inertia tensor
    2. Creating ArticulatedBodyInertia from RigidBodyInertia
    3. Using apply() to compute force from motion
    4. Using tformRBI() and tformABI() to transform inertias
    5. Printing inertia properties
    
    Show physical interpretation (mass, center of mass, rotational inertia).
  </action>
  <verify>
    <automated>cd /Users/premchand/Documents/GitHub/SpatialAlgebra && cmake --build build --target example_inertia 2>&1 | tail -5</automated>
  </verify>
  <done>
    inertia.cpp compiles successfully, demonstrates inertia operations
  </done>
</task>

<task type="auto">
  <name>Task 5: Create dynamics.cpp example (ABA)</name>
  <files>examples/dynamics.cpp</files>
  <action>
    Create example demonstrating:
    1. Creating ForwardDynamics link chain (2-3 links)
    2. Setting up link properties (inertia, transform, joint axis)
    3. Calling computeAccelerations() with joint torques
    4. Printing resulting accelerations
    
    Show forward dynamics pipeline for articulated body.
    Reference Featherstone Algorithm 7.3 in comments.
  </action>
  <verify>
    <automated>cd /Users/premchand/Documents/GitHub/SpatialAlgebra && cmake --build build --target example_dynamics 2>&1 | tail -5</automated>
  </verify>
  <done>
    dynamics.cpp compiles successfully, demonstrates ABA forward dynamics
  </done>
</task>

<task type="auto">
  <name>Task 6: Update README.md with example references</name>
  <files>README.md</files>
  <action>
    Add "Examples" section to README.md (created in Plan 01) that:
    1. Lists available examples (basic_vectors, transforms, inertia, dynamics)
    2. Shows how to build examples: `cmake --build build --target example_*`
    3. Shows how to run: `./build/example_vectors`
    4. Links examples to learning objectives (vectors → transforms → inertia → dynamics)
  </action>
  <verify>
    <automated>grep -q "examples/" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    README.md references all examples with build/run instructions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verify examples compile and run</name>
  <what-built>
    Four example programs in examples/ directory demonstrating core operations
  </what-built>
  <how-to-verify>
    1. Build all examples: `cd build && cmake .. && cmake --build build --target example_vectors example_transforms example_inertia example_dynamics`
    2. Run each example and verify output:
       - `./build/example_vectors` — shows vector operations
       - `./build/example_transforms` — shows coordinate transforms
       - `./build/example_inertia` — shows inertia properties
       - `./build/example_dynamics` — shows ABA accelerations
    3. Verify output is sensible (no NaN, reasonable values)
    4. Check that examples are educational (comments explain what's happening)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all examples compile and produce sensible output, or describe issues
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase, no code execution boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Example code accuracy | mitigate | Examples must compile and produce physically correct results |
</threat_model>

<verification>
- All 4 examples compile without errors
- Examples run and produce output (no crashes, no NaN)
- README.md references examples with instructions
- CMakeLists.txt properly configures example targets
</verification>

<success_criteria>
- examples/ directory contains 4 working example programs
- Each example demonstrates a core concept (vectors, transforms, inertia, ABA)
- User can build and run examples following README instructions
- DOC-03 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-03-SUMMARY.md`
</output>
