---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "User can find build instructions in README"
    - "User can see basic usage examples in README"
    - "User understands what SpatialAlgebra provides"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: "include/*.h"
      via: "usage examples"
      pattern: "#include.*SpatialAlgebra"
---

<objective>
Create comprehensive README.md with build instructions and usage examples.

Purpose: DOC-01 — Users need clear instructions to build and use the library.
Output: Complete README.md replacing the single-line placeholder.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with build instructions and examples</name>
  <files>README.md</files>
  <action>
    Create comprehensive README.md with these sections:
    
    1. **Project description** — One paragraph explaining SpatialAlgebra (6D spatial vector algebra for rigid body dynamics, Featherstone formulation)
    
    2. **Features** — Bullet list: SpatialVector/MotionVector/ForceVector, Rotation, PluckerTransform, RigidBodyInertia/ArticulatedBodyInertia, ForwardDynamics (ABA), InverseDynamics (RNEA)
    
    3. **Requirements** — Eigen3, Google Test, CMake 3.10+, C++17 compiler
    
    4. **Build instructions**:
       ```sh
       cmake -B build
       cmake --build build
       ```
    
    5. **Usage examples** — Code blocks showing:
       - Creating MotionVector and ForceVector
       - Creating PluckerTransform and transforming vectors
       - Creating RigidBodyInertia and applying to motion
       - Using ForwardDynamics class for ABA
    
    6. **Testing** — How to run tests: `cd build && ctest --output-on-failure`
    
    7. **Documentation** — How to generate Doxygen: `doxygen Doxyfile`
    
    8. **License** — Placeholder section
    
    Use code conventions from CONVENTIONS.md (namespace SpatialAlgebra, type aliases mv/fv/plux).
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '{if($1 >= 100) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>
    README.md contains: project description, features, requirements, build steps, 4+ usage examples, test instructions, Doxygen instructions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase, no code execution boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | README.md examples | mitigate | Verify all code examples compile against actual headers before committing |
</threat_model>

<verification>
- README.md has 100+ lines
- Build instructions match CMakeLists.txt
- Usage examples use correct class names and namespaces
- All referenced files exist (Doxyfile, build directory)
</verification>

<success_criteria>
- README.md replaces single-line placeholder with complete documentation
- User can build library by following README instructions
- User can see concrete examples of core operations
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-01-SUMMARY.md`
</output>
